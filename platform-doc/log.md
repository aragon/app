# Log

- **2026-08-28** — Commissioned reconcile-product-releases-since-1-35 as one finite catch-up over every official app release after the prior 1.35 documentation baseline through the newest release available when the task starts. Official releases currently run through `@aragon/app@1.38.0`. The task dispositions every release item against canonical pages and source, preserves the complete Permission Viewer launch pass as already-mined coverage, and must finish before the page-purpose/topology audit consumes the graph. **Board delta:** task added — reconcile-product-releases-since-1-35 (`ready`, next actor `agent`, first in Ready to run); blocked-task trigger extended — audit-page-purpose-and-repository-topology; no owner ask, gap, draft, opportunity, or page question changed.

- **2026-08-28** — Completed Slice 2 and retired mine-permission-viewer-and-draft-marketing-brief. Delivered a self-contained, copy-ready Permission Viewer marketing handoff in the completion response, using the Linked Accounts reference only for its nine-question progression from availability through next action. The brief leads with counterparties performing due diligence, keeps builders and operators reviewing indexed configuration as the secondary audience, and translates the canonical capability into the user journey, bounded value propositions, two concrete use cases, the documented internal data flow, and the full interpretation boundary. No Linked Accounts fact or prose carried over. An exact launch date was not invented because the source establishes only that the feature is live now; no external-partner claim was added beyond the documented Aragon app, platform-backend, and OSx-event flow. Per the task boundary, no durable marketing wiki entry was created. The structural reference `raw/linked-accounts-marketing-brief.md` was fully applied and then consumed; together with the Slice 1 Permission Viewer dump, both raw inputs are now retired. **Board delta:** task retired — mine-permission-viewer-and-draft-marketing-brief (board row removed, file deleted); blocked-task trigger reconciled — audit-page-purpose-and-repository-topology now names the semantic-anchor review as its sole remaining prerequisite; no owner ask, gap, draft, opportunity, or page question changed.

- **2026-08-28** — Completed Slice 1 of mine-permission-viewer-and-draft-marketing-brief. Added the draft [Permission Viewer](./access-control/permission-viewer.md) capability and linked it from the access-control and root indexes, [scoped authority](./access-control/scoped-authority.md), and the indexed-state principle. The owner briefing supplied the launch ruling, due-diligence and self-audit audiences, and neutral-inspection boundary. Verification against `app@16d34dc3` and `app-backend@107103b4` grounded the read-only account scope, navigation, dashboard and Settings entry points, list and graph behavior, default presentation filters, entity and condition details, and active-grant event reduction. The owner's launch ruling controls availability for every app-supported organization, so the implemented rollout flag does not qualify canonical product truth or revive the retired gate opportunity.

  **Claim disposition:** protocol tuples, permission conditions, account semantics, process semantics, and governed configuration changes remain in their existing canonical pages and are linked rather than restated. The briefing's shorthand was narrowed where the source model requires it: an account and the OSx `DAO` contract are not interchangeable categories; permission IDs name capabilities rather than universally naming function selectors; and “all permissions” means the records the platform index currently returns as active for the selected account, not external-contract authority or a runtime authorization result. Cross-version coverage was narrowed to app-supported indexed accounts whose DAO contracts emit the configured `Granted` and `Revoked` signatures; the represented DAO and DAOv2 artifacts share those signatures, but the source does not establish every historical OSx version. Competitive comparisons, the open-source rationale for neutrality, and promotional adjectives were rejected as unsupported canonical claims. The product facts needed for Slice 2 now live on Permission Viewer; its question-led positioning remains marketing work. The Permission Viewer dump was consumed and deleted, while the Linked Accounts structural reference remains untouched for Slice 2. Source verification also found that cross-network linked-account detail presenters retain primary-account context and that an overlap-only data set disables both default hide filters; the current boundaries are documented and the improvements are routed to [Align Permission Viewer details with the selected account](./access-control/align-permission-viewer-details-with-selected-account.md) and [Let overlapping Permission Viewer filters be cleared](./access-control/let-overlapping-permission-filters-be-cleared.md). **Board delta:** task parked — mine-permission-viewer-and-draft-marketing-brief (`in-progress`, next actor `agent`, Slice 2 remaining); draft added — Permission Viewer, taking the inventory to 75; opportunities added — align-permission-viewer-details-with-selected-account and let-overlapping-permission-filters-be-cleared (both `candidate`); blocked-task prerequisite cleared — Permission Viewer mining for audit-page-purpose-and-repository-topology; no owner ask, gap, or page question changed.

- **2026-08-28** — Commissioned mine-permission-viewer-and-draft-marketing-brief as one ordered launch task: first mine the owner's `raw/raw-permission-dump.md` into the canonical access-control graph with Permission Viewer treated as live for every app-supported organization, then derive a copy-ready marketing-team handoff from the nine-part structure in `raw/linked-accounts-marketing-brief.md`. The old permissions-page-gate opportunity is retired rather than carried into the task: its finding that no code read the flag is stale in the current app checkout (`16d34dc3`), which gates the route and entry points, while the owner's launch ruling controls product availability. The page-purpose audit now waits for this source-mining pass as well as the semantic-anchor review. **Board delta:** task added — mine-permission-viewer-and-draft-marketing-brief (`ready`, next actor `agent`, first in Ready to run); opportunity retired — permissions-page-gate; blocked-task trigger extended — audit-page-purpose-and-repository-topology; no owner ask, gap, draft, or page question changed.

- **2026-08-11** — Synchronized the root index's pinned-upstream notice with the existing `protocol-doc` submodule pin at `800da8d`, after verifying that commit remains the latest upstream `main`. The submodule itself was already current; this corrects stale snapshot metadata left after the 2026-08-10 update. **Board delta:** none — no task, ask, gap, opportunity, draft, or page question touched.

- **2026-08-10** — Updated the `protocol-doc` submodule to upstream `main` (`3e4cd0d` → `800da8d`, user-requested): the update is purely additive — a generated `abi/` reference tree covering every plugin's contract ABIs with cross-links into the existing protocol pages, plus the generation scripts — with no renames or deletions, so no platform-page link into `protocol-doc/` moved. The new `protocol-doc/scripts/README.md` is tooling documentation rather than a wiki entry, so `wiki.toml` ignores `protocol-doc/scripts/**`; `wiki check` passes. **Board delta:** none — no task, ask, gap, opportunity, draft, or page question touched.

- **2026-08-07** — Humanizer pass over all 106 canonical pages (owner-requested): every page checked against the AI-writing-pattern catalog with voice.md holding precedence, through nine batch editors, nine adversarial reviewers, and three senior-review escalations. The corpus came back largely clean — 91 pages needed nothing. Of the edits that landed, the honest verdict the owner asked for: roughly half were cosmetic (curly-to-straight quote glyphs on nine pages), and the substantive remainder is small — one filler trim ([Every element makes a claim](./design/every-element-makes-a-claim.md)), a voice-rule-backed minimizer removal ([Surface unlock eligibility upfront](./governance/surface-unlock-eligibility-upfront.md)), a rhetorical-question and cleft rewrite ([Action simulation](./governance/action-simulation.md)), and a handful of copula fixes. The verification earned its cost three ways: it caught and repaired the pass's one real meaning error ("presents as a single consolidated proposal" on [Staged proposals](./governance/staged-proposals.md) is a presentation-layer claim, not an identity claim — restored); it identified three copula swaps that de-synced body-relationship phrasing from [Body](./governance/body.md)'s canonical "serve as"/"represents" wording ([Safe as a body](./governance/safe-as-a-body.md), [Target](./governance/target.md), [veLocker](./governance/velocker.md) — reverted); and it surfaced two defects already merged in the terminology/contrast pass (e9ece93), now repaired — [Account creation](./accounts/account-creation.md) recovers the deliberate-omission statement for its network list, and [Aragon Names](./accounts/aragon-names.md) recovers a spliced sentence and the Member Registry documentation-scope boundary. Excluded, as in the contrast audit: `protocol-doc/` (read-only), `inbox/` and `raw/` (source fidelity), `research/` (owner-private), `log.md` history, operating docs. **Board delta:** none — no task, ask, gap, opportunity, draft, or page question touched.

- **2026-08-07** — Owner ruling on the filtered-to-nothing collapse (the input row from the page-and-list-state pass): the collapse is intended. Tab and chip narrowing is a view of a collection rather than a search — the visible selection supplies the context the empty copy leaves out — and the component library's distinct filtered-to-nothing presentation is opinionated toward search-style filtering, which no datalist page offers; the state is reserved for whenever real search reaches those pages. [Datalist page](./design/datalist-page.md) restates the collapse as ruled product behavior instead of a non-decision and cites the ruling in its `source:` tail; the page stays `status: draft` for its full review. **Board delta:** one owner ask resolved (the filtered-to-nothing input row removed from Ready for your input); tasks, questions, gaps, drafts, and opportunities unchanged.

- **2026-08-07** — Closed out review-dao-terminology-violations: the owner ruled on all 38 ledger items in a walkthrough, and every ruling is applied. The [value proposition](./value-proposition.md) statement is resettled on the owner-picked access-control-led form — governing **who may do what** across treasuries and protocols — built on the original sentence with minimal change, after a research pass over positioning practice and aragon.org's current articulation. The copy rule now lives in one place: [Account](./accounts/account.md) owns account-by-default with the two allowed `DAO` contexts (the OSx contract or an exact protocol identifier, and an explicitly token-governed use case with its token context visible), voice.md's protected-terms row and the [rollout opportunity](./design/account-terminology-rollout.md) restate it, and the redundant Account terminology pattern page is deleted. The docs also stop mirroring legacy UI labels — target copy is written directly (**All accounts** on [Explore page](./accounts/explore-page.md), **Create account** on [full-screen wizard](./design/full-screen-wizard.md) and [metadata input](./design/metadata-input.md)) while the rollout opportunity owns the product-side divergence. DAO presentation merged into [Account](./accounts/account.md) as "How the app names and presents an account" — every claim, qualifier, and citation carried over, the page deleted with all inbound links retargeted, and an adversarial verification pass run over the merge; its two content findings (a strengthened evidence disclaimer, a pre-existing "DAO account") were fixed, and its suggestion to link the rollout opportunity from the page was declined per the one-way board discipline. DAO slots keeps its name as the technical term with client-specific framing ([DAO slots](./design/dao-slots.md), [plugin slots](./design/plugin-slots.md), [principles](./principles.md), design index); the explore page drops its slot-implementation aside (capability pages describe functionality, not implementation); the linked-account and treasury sub-DAO metaphors are rephrased as explicit examples; the guardian-DAO guide candidate keeps its name with its LP-token-governed framing made visible on the board row. **Board delta:** task retired — review-dao-terminology-violations (row removed, file deleted); owner ask added — the semantic-anchor review moved from Not yet ready to Ready for your input (its trigger fired; it is now the top row); drafts inventory 76 → 74 (DAO presentation merged away, Account terminology deleted; the anchor-dependency line re-derived to 46 of 65); no gap or opportunity added or removed.

- **2026-08-07** — Contrast-framing audit across the reader-facing base (owner-requested): every in-scope use of "not" judged against its context for the "X, not Y" negated-foil pattern — a foil earns its place only when the surrounding text gives the reader a real reason to hold the excluded reading. 53 sentences and headings across 36 files rewritten (foil deleted where the positive claim already carried it, restated positively where the negation hid a fact, or expanded to explain the misreading where it is real); roughly 400 further occurrences verified and left standing as plain negation, vocabulary-boundary contrast, security inference-blockers, evidence limits, or required planning labels. No product claim changed meaning; two heading renames (`governance-designer.md`, two identity headings) had their inbound anchors updated. Excluded: `protocol-doc/` (read-only), `inbox/` (source fidelity), `log.md` history, operating docs, worktrees. [voice.md](./voice.md) hardened against recurrence: new global rule `V-R017` (attach a negated foil only to a real in-context misreading, with a grep-based review check), `V-D01`'s "explicit contrasts" signal tightened to "contrasts only where they resolve a real ambiguity," new calibration example `V-E06`, `policy_version` 1.0.0 → 1.1.0 — `last_reviewed` deliberately unchanged pending owner review of the new rule. Full before/after report published as a session artifact (claude.ai/code/artifact/11337b9f-5c7a-446e-a237-7938994f0bbe); git diff on `agent/next-docs-pass` holds the exact edits. **Board delta:** none — no task, ask, gap, opportunity, draft, or page question touched.

- **2026-08-07** — Owner ruling on the wizard exit prompt's chrome and reach (the promoted input row on [Wizard](./design/wizard.md)): the browser-chrome challenge is accepted as a low-cost implementation choice with no design intent attached — not a dialog-taxonomy inconsistency to fix — and a dialog wizard's own close control discarding a dirty sub-flow without challenge is deliberate, because a dialog wizard holds a bounded sub-flow with little content. Both rulings folded into the wizard page's exit-guard section and cited as a product-owner ruling in its `source:` tail; the page stays `status: draft` for its full review. The guard's two known leaks remain tracked on the [Keep the wizard exit guard armed](./design/keep-the-wizard-exit-guard-armed.md) opportunity, which the ruling does not affect. **Board delta:** one owner ask resolved (the wizard exit prompt chrome-and-reach input row removed from Ready for your input); tasks, questions, gaps, drafts, and opportunities unchanged.

- **2026-08-07** — Drafted [Harden token governance against governance attacks](./guides/harden-token-governance-against-attacks.md) (user-requested): a security guide synthesizing already-canonical coverage into one attack-resistance pass — voting-power custody and the wrapper snapshot-and-exit exposure, creation-eligibility defaults and narrowing (one-whole-token basic default, per-body membership, composed `VotingPowerCondition`), participation-threshold denominators under supply policy, decision timing as defender reaction time, staged safeguards (pre-vote gate, post-vote approval or veto, bodyless timelock), stage visibility over direct grants, and per-proposal action review and simulation. No new product claims: every claim traces to the linked platform or protocol-doc pages, so owner review is about framing and emphasis rather than fact-checking. Linked from the guides index (new "Protect a governance process" goal group) and the root index's guide route. Board delta: drafts inventory 75 → 76 (guide added to the Guides line); tasks, questions, gaps, and opportunities unchanged.

- **2026-08-06** — Rebased the working branch onto the merged recognition-limits and governance-derivation passes and reconciled the one collision the merge exposed: the recognition pass had given [DAO slots](./design/dao-slots.md) its worked instance from a capital-flow account-specific control, and the owner ruling recorded in [Product scope exclusions](./product-scope-exclusions.md) excludes exactly those controls from canonical pages — the worked instance and the function-slot qualifier are retracted, while the page keeps its answered shipping model (slot content compiled in and hand-registered, Aragon-team work per release), which rests on the live branding registrations. The board's mining section closed out: all six audit rows are complete across the two merged lines of work, and the interrupted token-holder completion (log closed, file deleted, row still present) was finished per the interrupted-completion rule. **Board delta:** Ready to run emptied — the recognition and derivation rows removed as merged-in completions, the stale token-holder row removed; question and draft counts reconciled against the merged graph (19 across 16 platform pages; 75 drafts); one terminology-ledger item retired — its target was the deleted recognition task file, leaving 38; no task, ask, gap, opportunity, or draft added or removed.

- **2026-08-06** — Closed out document-page-and-list-state: one Opus evidence pass over `app@122f1bd1` (pivotal library behaviors verified against the installed gov-ui-kit 2.9.0 rather than the lagging 2.8.1 local checkout), two parallel writers, and one adversarial Opus review whose findings were applied, run in an isolated worktree and hand-merged onto the day's board reshape and the three concurrently completing passes. The review's two blockers guarded the same line: the filtered-to-nothing collapse must read as a non-decision, never a product choice. **Two of the task's own claims did not survive the code.** List filter state is not unpersisted — chip, tab, and switcher selections are written to the URL, restored on reload or from a shared link, and used deliberately as a cross-page hand-off (the dashboard's view-all links carry the selected body into the collection pages); what makes selection volatile is that the app deletes the parameter on leaving the page and writes it without history entries. And "never an in-place retry" holds only for read surfaces — transaction flows retry in six-plus places, so the page scopes the rule to reads and hands retry to [transaction submission](./design/transaction-submission.md).

  [Datalist page](./design/datalist-page.md) now states the list-state vocabulary: three loading states kept apart (first load, refresh with rows on screen, loading more taking precedence); emptiness derived from state plus row count rather than a state of its own; and the filtered-to-nothing collapse stated as fact, with the component library's distinct filtered state and presentation named as built and unconnected — a non-decision, with the intent question flagged for the owner on the board rather than answered on the page. Failure gets the page-level not-found-versus-generic split (a failed list draws no distinction at all), the scope rule that a page failure replaces the page while a valid empty result replaces only the list region, and leave-or-support recovery with the app switching the query layer's three automatic retries off product-wide. Search and sort are written as the deliberate non-decision the task wanted intact — sort declined absolutely, search declined on the four collection pages with the asset-selection picker and the [explore page](./accounts/explore-page.md) named as out-of-pattern surfaces, and the members order left with [token panel](./governance/token-panel.md). Pagination is load-more as the library's only offer, with app-chosen batch sizes of 10/18/20/20; the library's exactly-one tab-bar collapse is attributed as library behavior beside the app's own rules — the zero-option guard, a lone tab's label promoted into the card title, and the two-or-more thresholds on the toggle switchers and chip rows that actually carry a datalist page's tabs.

  [Alert severity](./design/alert-severity.md) answers both of its original open questions in its body: advisory cards and inline advisories carry the library's four visual variants under the product's two-severity rule (the same toolkit-options-do-not-extend-the-rule move the dialog section already made); field errors are typed to warning and critical and hardcoded critical by the shared form layer; the app's banner strip carries no severity choice; the product has no toast mechanism — not a declined offer, since the library ships none either, and the only auto-dismissing notices belong to the third-party wallet SDK inside its own modal — and failure-versus-emptiness is answered as scope. The task's criterion that the page end with no `## Open questions` section predates the chain-mismatch question the interrupted-flows pass added; that question is not answered by this pass and stays open, so the section remains with exactly it. New draft [Dashboard](./accounts/dashboard.md) (accounts, capability) states the fixed Proposals/Members/Assets order, the shared recognized-plugin gate on the first two with Assets ungated, the always-present details aside as the page's authoritative-record drill-down, the onboarding state that replaces header and main column (owned by [admin flow](./accounts/admin-flow.md)), the account-keyed header slot (mechanism with [DAO slots](./design/dao-slots.md)), and the three/six/three previews routing into the datalist pages; it is linked from the accounts map. [Every element makes a claim](./design/every-element-makes-a-claim.md) gains a one-sentence empty-versus-failed pointer.

  The pre-reshape board's expectation that this row rewrites [abstract, then offer a drill-down](./design/abstract-then-drill-down.md) did not hold: the task file scoped that page as unblocked only, and its authoritative-fact definition would not admit the dashboard's preview-to-collection links as an instance — the dashboard page instead names its details aside as the pattern's real instance on that surface. Recorded without filing, three findings: the optimistic filter-chip policy (chips show while their availability probe is pending and stay on probe failure, disappearing only on a confirmed zero); an offline-paused read presenting as settled rather than loading; and the asset-selection picker's exact-match search (a term matches only the full token name or symbol, filtering runs client-side over already-loaded pages, and load-more is suppressed while a term is active) — the treasury pass retired its task before this one could route the fact there, and its pages do not cover that picker's search semantics, so it stays here for the owner to route. **Board delta:** task retired — document-page-and-list-state (row removed, file deleted); owner ask added — confirm the filtered-to-nothing collapse is intended (input row, last by unblock value); draft added — Dashboard (accounts), inventory 74 → 75; page questions resolved — two on alert severity, platform aggregate 20 across 17 pages; no knowledge gap or opportunity touched.

- **2026-08-05** — Ran document-governance-derivation-and-availability at `app@122f1bd1`: six parallel writers, one adversarial verification pass over the combined diff, one fix iteration, then a rebase-time reconciliation with the same-day owner-review passes — the advanced-availability correction this task originally carried had been retired from it by the owner's self-serve ruling, and nothing from it is re-stated. **The one remaining correction landed first.** `governance/staged-proposals.md` now distinguishes the advance flag the app configures at install time (the best-effort attempt, set for every body) from the one report transaction it constructs itself — a manual, non-plugin body's report, which never requests same-transaction advancement — so after such a report the stage waits for an explicit advance. `governance/proposal-status.md` names **FAILED** as the twelfth vocabulary member: defined with a label and color in gov-ui-kit 2.9.0 as installed, produced by no derivation in the app (shared tree, per-plugin wrappers, and stage-level tree all verified; only test mocks reference it) — a status the app is equipped to display but can never arrive at, a latent defect no user sees. `governance/proposal-creation.md` states the designer's defaults: eligibility preselected on Members with every listed body toggled on (plugin bodies and Safes; a manual body that is not a Safe never appears in the step), yielding a rule condition that ORs one membership condition per listed body on a staged process; on a basic process, a multisig installs restricted to its listed members, and Token Voting / Lock to Vote install a one-whole-token proposer minimum (held vs locked respectively). `governance/process.md` gains "Which processes a user sees": the visible set is derived — interpretable process plugins only, the App CMS per-account hide list, a feature-flag gate on linked-account processes — then deliberately ordered, primary account first, then Token Voting → Multisig → Lock to Vote → the rest, with ties keeping listing order. Cross-action coherence went to `governance/action.md`, not the pre-empt-protocol-rejected-configuration opportunity: that family is documented-invalid field values, while this is order-dependent batch coherence with no invalid value — the page now states that no validation layer checks a proposal's actions against each other, with the multisig membership/threshold case in both directions (a conflicting batch is composable and reverts whole at execution; a batch adding members cannot express the higher threshold they would make valid) and opt-in, non-blocking action simulation as the only whole-batch surface. Corrected in passing: `app-cms.md`'s further-feeds enumeration said two files where the code ships three — the third is the per-plugin-type address whitelist the governance designer's body-type picker reads. **Board delta:** task retired — document-governance-derivation-and-availability (row removed, file deleted); no owner ask, gap, opportunity, draft, or page question added or removed.

- **2026-08-05** — Closed out document-recognition-and-handoff-limits at `app@122f1bd1`, run as four parallel writer passes (two Sonnet for the deterministic lookups, two Opus for the judgement-heavy items) with orchestrator-level QA and one adversarial Opus review of the combined diff. The four honest-abstraction limits now sit on their pages, each verified first-hand against source rather than carried over from the audit — which mattered, because the audit's raw matrix had been cleared and two of its framings did not survive re-derivation (below). The adversarial review mattered too: it cut a dark-launched surface from the inventory (the basic flow's existing-conditions notice is behind a flag with no production entry), corrected the worked instance's account scope (four registrations fill that slot, not one), and pulled the Safe false-positive consequence back to the app layer, where it belongs — on-chain, the condition itself refuses a target that cannot answer `isOwner`.

  **The four limits.** [Plugin](./governance/plugin.md) states that a known plugin's integration depth varies and the floor is a single stand-alone page: Gauge and Capital Distributor register only the plugin-page slot, so no participation in proposals, no settings surface, no presence in the governance designer — named on the page as what the partially-supported delivery posture looks like in the interface. [Connecting a Safe](./accounts/connecting-a-safe.md) states the recognition rule: the backend contract-metadata name, lower-cased, tested against four fixed substrings, at exactly one call site (the designer's add-body any-address field) — fallible in both directions, with the concrete consequence of each miss (an unrecognized genuine Safe silently drops out of the proposal-creation eligibility list entirely; a name-matched non-Safe gets Safe branding and the `SafeOwnerCondition` toggle). [DAO slots](./design/dao-slots.md) carries the worked instance its second use lacked: Cryptex's allocation-file generator, a DAO-slot control injected immediately above the Capital Distributor campaign-creation form's upload field that produces the very file the next field asks for. [Reach out when the abstraction cannot stay honest](./design/reach-out-to-the-team.md) carries the entry-point inventory: exactly three surfaces render the handoff — the designer's team-built advanced-governance panel, the dashboard onboarding card during the admin flow, and the explore getting-started card — all sending to the same external assistance form.

  **Two audit framings corrected by re-derivation.** The per-account second tier is not "branding plus embedded controls": only Cryptex ships both; katana (twice) and a Sepolia demo account ship controls with no branding. And the embedded control's production reachability holds for a non-obvious reason — the control lives in the action layer and surfaces through the generic permission-driven action composer; its only tie to the dark-launched capital-flow module is a bare slot-id enum with no runtime gate, so the flag that keeps that module out of production does not touch it, and the module's open owner question stays untouched and unnamed.

  **The inventory's sharper counter-finding.** The app also names limits where it renders no route out: an advanced process shows its composition read-only with no edit control and no contact entry point, and the partially-supported client call to action has no in-app entry point at all — the affected governance-type option appears disabled with a **By request** tag, without the remedy [control availability](./design/control-availability.md) says a disabled control owes. Ordinary support (the footer Support link, the error-state Report issue action) is stated on the page as a different kind; the in-app support widget stays out entirely, being an open owner question.

  **Corrected in passing.** [Governance designer](./governance/governance-designer.md) claimed Safe recognition "checks via the Safe API"; no such call exists anywhere in the app, and the sentence now links the heuristic on Connecting a Safe. [DAO slots](./design/dao-slots.md)' former open question is answered from source and folded in: slot content is imported and hand-registered in the app's source, so a bespoke slot is Aragon-team work shipped with a release, with no configuration path — and the page's definition now admits that a slot can hold a resolver function as well as a component, which the Cryptex instance itself uses. The advanced-access gate discrepancy the handoff sweep independently re-found was settled the same day by the owner's availability answers (below), so nothing was re-filed; the inventory's advanced bullet and the reach-out framing follow that ruling — the boundary is a configuration the flow cannot represent faithfully, not advanced access itself.

  **Board delta:** task retired — document-recognition-and-handoff-limits (row removed, file deleted); page question answered and removed — DAO slots (how slot content ships), platform aggregate now 21 across 17 pages; no owner ask added or resolved (the capital-flow and support-widget rulings stay with their existing input row); no gap opened, closed, or narrowed; no draft added or removed.
- **2026-08-05** — Closed out document-treasury-composition-and-history at `app@122f1bd1`, run as four parallel verification passes (two Sonnet, two Opus) followed by an Opus writing pass and an adversarial Opus review. **The assets withdrawal claim is corrected, not cleared — the audit's counter-finding was right.** [Assets](./treasury/assets.md) claimed a withdrawal is "a selection against real holdings, not an address paste"; the code offers both — the selector's default list is the account's indexed holdings with balances, and a permanently available **Add address** route accepts any address that resolves as an ERC-20, never checked against holdings and entering at a balance of zero. The amount is not capped by the balance, deliberately: the treasury transfer form switches off the max rule the same input enforces for token locking and wrapping. The page now states the default-not-boundary rule, with the balance-error claim scoped to the transfer path because lock and wrap do surface one.

  **The audit's truncation claim inverted under verification.** The truncating coercion it pointed at (`bigIntUtils`) is real but unreachable from the amount path; the amount is parsed by viem's `parseUnits`, which **rounds half-up** on the last retained digit and writes the parsed value back into the field, silently. [Create transaction](./treasury/create-transaction.md) states the rounding rule with a worked example and names it a departure from both [refusal](./design/invariant-validation.md) and [meaning-preserving normalization](./design/input-normalization.md), whose table counts amount precision among what a conversion may change. That divergence is filed as the candidate [preserve or reject amounts the token cannot express](./treasury/preserve-or-reject-over-precise-amounts.md). One adjacent finding is recorded here without filing: the same field accepts scientific notation such as `1e-7`, which the unit parser rejects on an unguarded render path, reaching an app-level error boundary rather than a field message — established by source reading only, not reproduced in a browser, so a reproduction should precede any ticket.

  **Tabs are conditional, and sharper than the audit claimed.** The tab row on [Transactions](./treasury/transactions.md) renders only when at least two of executions, deposits, and withdrawals have history — zero or two-plus tabs, never one, with **All transactions** riding along rather than counting, and a failed probe counting as history; test-covered. The page states the rule and reconciles it with [datalist page](./design/datalist-page.md)'s page-defined framing: the set is the page's model, its appearance follows the account's history. **Decode degradation is immediate on mismatch, bounded on polling.** Opening an execution polls the backend decode status every two seconds up to ten responses; a decoded/raw count mismatch degrades every row to raw calldata at once — including cleanly decoded ones — with the action count staying accurate; the bound is the dialog's own and is not generalized to proposal surfaces. The adversarial review mattered: it found the degradation is not silent (each degraded row carries the action viewer's "Action had issues with decoding" warning when expanded, so the page now scopes silence to the polling bound alone), that window expiry is not final (a stale reopen refetches and can pick up a completed decode), and that a claimed review step does not exist in the one-step flow — all three corrected before landing. **Board delta:** task retired — document-treasury-composition-and-history (row removed, file deleted); opportunity added — preserve-or-reject-over-precise-amounts (`candidate`, treasury); no owner ask, page question, gap, or draft added or removed.

- **2026-08-05** — Completed document-token-holder-prompts-and-reward-routes at `app@122f1bd1`, closing both ends of the token holder's economic loop — run as three parallel writers, one adversarial reviewer, and, after the task's mid-pass refinement raised the bar, a combined dialog test in the app repo. **The simultaneity case is settled by evidence rather than code reading:** a real two-watcher render under one real dialog provider (six assertions, five identical runs; all probe files removed afterwards) confirms that when the delegation and lock-or-wrap prompts are eligible together, exactly one shows — dialogs replace rather than stack, the survivor is decided by mount order and query timing rather than any rule, a replacement can remove a dialog the holder is already reading, and the lost prompt does not return until a fresh deliberate connection. The test also narrowed the reachability precondition (the exit-queue case collides only when the holder also retains a positive underlying balance) and showed the shared profile-pause flag releases both watchers in the same commit, making simultaneity more likely. Per the task's routing rule the canonical pages stay silent on the collision and the defect is filed as [Coordinate colliding onboarding prompts](./governance/coordinate-colliding-onboarding-prompts.md). **Page changes:** [Token panel](./governance/token-panel.md) now states the separate Lock to Vote plugin's lock-on-connect trigger beside its adjacent nudges — deliberate connection, positive plugin-token balance, nothing locked with the body's lock manager, never leading into delegation — and the returning-holder consequence of the deliberate-versus-silent connection rule. [Choose a voting-power mechanism for token governance](./guides/choose-token-voting-power-mechanism.md) states the participation-denominator difference — Token Voting weighs a proposal against the supply recorded with its own settings at creation, Lock to Vote reads live total supply, so the same percentage is a fixed bar under one plugin and a moving target under the other — and returns to draft for that addition. [Gauge voting](./governance/gauge-voting.md) gains lending-market gauge registration: its own Gauge Registrar contract and `GAUGE_REGISTRAR_ROLE`, register/unregister identified by qiToken, incentive type, and reward controller, execution reverting during an active vote window, on the partially-supported footing — and the pass corrected a live claim, the gauge admin permission string being `GAUGE_ADMIN`, not the `GAUGE_ADMIN_ROLE` the page asserted. [Capital Distributor](./governance/capital-distributor.md) gains the lock-compounding payout mode: chosen by the campaign at creation, surfaced only for accounts wired with a voting-escrow resolver, warning rather than hiding when no escrow address resolves, the claimant's payout-address step unchanged. A second candidate came out of the unlock verification: [Surface unlock eligibility before the attempt](./governance/surface-unlock-eligibility-upfront.md) — the app simulates the Lock to Vote unlock on load, discards the verdict, and shows a fixed after-the-fact message that can misattribute the cause. Scope held: no exit-queue math, curves, checkpointing, or capital-flow material pulled in. **Board delta:** task retired — document-token-holder-prompts-and-reward-routes; opportunities added — coordinate-colliding-onboarding-prompts and surface-unlock-eligibility-upfront; draft added — choose-token-voting-power-mechanism (re-drafted for the denominator addition, 73 → 74); no owner ask, page question, or knowledge gap added or resolved.

- **2026-08-05** — Closed out document-interrupted-and-resumed-flows at `app@122f1bd1`, run as four parallel writer passes (one per page; Sonnet for the dialog census, Opus for the rest) with the lead session as integrator, followed by one adversarial Opus review whose surviving findings — seven blockers, five should-fixes, four nits — were applied in a single fix pass. The audit's raw evidence files were already cleared, so every claim was re-derived from source, and the task's own framing lost four points to that re-derivation. The pass landed concurrently with the day's ontology reshape and was merged onto it; the one placement it changed is that the in-flight conflict rule, which the task aimed at the proposal page, now lives where the reshape moved creation: [proposal creation](./governance/proposal-creation.md).

  **What the pages now state.** [Wizard](./design/wizard.md): one exit guard at the shared wizard root, inherited by both container families — the full-screen flows *and* the dialog wizards, so the task's "all five wizards" undercounted its own finding — challenging the navigation close control and in-app links, browser Back, and reload or tab close once the form is dirty, through the browser's own prompts rather than the product's [dialog taxonomy](./design/dialog-taxonomy.md); it releases when the submission dialog reaches its leave-from state, and the direct-creation dialog alone re-arms on a failed send. Outside-click refusal on dialog wizards is attributed to the app: at gov-ui-kit 2.9.0, the version the app builds against, the library's default is to dismiss — inverting the audit note that had that suppression as the library's own default. [Transaction submission stepper](./design/transaction-submission.md): the base's first chain-mismatch statement — detection against the transaction's own chain, an `info`-severity disclosure deliberately outside the warning/critical vocabulary, resolution inside **Approve/sign** before anything reaches the wallet, and the stale-chain refusal being the app's rather than the wallet's — plus a resumption section: submissions remembered against the action's identity, resumption automatic rather than offered (the task's "offered for resumption" is refuted for this page; the only genuine offer is proposal creation's), only a broadcast send surviving a reload, four suppression mechanisms retiring stale attempts, a reload-resumed send able to watch but not re-send, and new-versus-resumed as first-class states whose stuck-transaction clock runs from the original broadcast. [Dialog taxonomy](./design/dialog-taxonomy.md): 28 of 84 dialogs across 19 registries declare a wallet requirement (recounted exactly, matching the audit), enforced by skip-render and close-on-genuine-disconnect with a reconnection grace; the focus trap is a library default the app switches off statically for exactly two dialogs, and only the connect-wallet dialog also tracks the connector's lifecycle, releasing its modal block while the connector's UI is open — the task's "two dialogs release in step with the connection lifecycle" conflated the static flag with the dynamic tracking. [Proposal creation](./governance/proposal-creation.md): the in-flight conflict check at publish — scoped to the same account and process, keyed on a content identity that deliberately excludes schedule (a timing-only edit resumes the earlier attempt as it was), stopping a second publish at a warning whose forward action supersedes the pending creation and whose resume alternative is offered only while the dialog's inputs are still held in memory, degrading to a plain dismiss after a reload; the check is the wizard's own, single-tab, and not run by flows that create proposals under the hood.

  **Cross-page corrections made while integrating**, both pre-existing: the stepper's leave-early control is labeled **Continue in background** (two page mentions said "Proceed anyway"), and direct transaction creation does not run the four-phase stepper — its dialog is a two-step send with no resumption memory, now stated on [create transaction](./treasury/create-transaction.md) and [full-screen wizard](./design/full-screen-wizard.md), which both said it follows the shared stepper; it does share the stepper's chain-mismatch disclosure and pre-signing switch. A fifth full-screen wizard exists in source — capital flow's Create policy — and stays off the canonical pages per the publication boundary and [product scope exclusions](./product-scope-exclusions.md). One adversarial finding was declined: linking the wizard page to its opportunity entry would break the board's one-way discipline.

  **Recorded, not filed:** the day-long resumption TTL ends silently; the conflict warning's forward action clears every matching record while showing one, so a multi-conflict case collapses; under-the-hood proposal creations populate records the wizard's check can trip over without running the check themselves; the warning copy says "this proposal" where it is by construction an edited one; a stale code comment promises a post-reload transaction link that was never built; the support-chat context consumes pending-transaction state. **Board delta:** task retired — document-interrupted-and-resumed-flows (row removed, file deleted); owner ask added — rule on the wizard exit prompt's chrome and reach; opportunity added — [keep the wizard exit guard armed](./design/keep-the-wizard-exit-guard-armed.md) (candidate, design: a nested dialog wizard disarms its parent's in-app-exit guard, a never-removed history sentinel, both guard halves untested); page questions added — [alert severity](./design/alert-severity.md) (whether the chain-mismatch `info` disclosure admits a third severity) and [transaction submission stepper](./design/transaction-submission.md) (whether the single-tab pending-transaction memory is intended, flagged by two independent readings); no gap opened, closed, or narrowed; drafts unchanged at 73, seven rewritten in place.

- **2026-08-05** — Split the single 73-page owner-review ask into two dependency-aware passes after a filtered graph analysis and three independent reviews of the cohort and workflow. The pre-audit cohort is nine **semantic anchors**: Value proposition and Platform design principles as the product-intent roots, plus Account, Plugin, Governance process, Body, Action, Proposal, and Action builder as the shared semantic hubs. The graph count excludes log, backlog, task, index, planning-overlay, duplicate-link, and upstream noise; 44 of the other 64 current drafts directly depend on at least one anchor. The owner reviews this fixed set only after the six source-mining tasks and repository-wide terminology changes settle, so the purpose/topology and section-fit audits can consume approved meaning rather than infer it. The later owner pass is deliberately query-driven: it reviews every page still carrying `status: draft`, including pages created, split, or substantively reopened by the audits. Both reviews remain board-owned phases rather than task entries because draft status is the source of truth. **Board delta:** owner ask split and staged — semantic-anchor review before the structural audits, remaining-draft review after them; task state changed — audit-page-purpose-and-repository-topology from `ready` to `blocked` on the source, terminology, and anchor-review gates; audit-section-fit-and-location refined to preserve approved semantics and re-draft substantively changed pages; no draft, page question, opportunity, or knowledge gap changed.

- **2026-08-05** — Separated product-scope data from publication process after the owner's follow-up. New [Product scope exclusions](./product-scope-exclusions.md) is the source of truth for implemented surfaces that are not live; it currently records automated capital flows/policies and the in-app support channel, with documentation disposition, revisit trigger, and an optional product-opportunity link. [WORKFLOW.md](./WORKFLOW.md) now owns only how rows enter and leave that reference. The linked-account helper was removed from exclusions after source-history verification: `linkedAccountDisplayUtils` has had no production caller since its original SubDAO feature commit, while the live assets and transactions selector labels the primary and linked accounts through `useDaoFilterUrlParam`. That establishes dead duplicate code, not a confirmed user-facing bug. New candidate [Retire or wire the unused linked-account display helper](./accounts/retire-or-wire-linked-account-display-helper.md) routes the cleanup through the existing opportunity system and requires a concrete missing fallback before anyone wires it into the live product. **Board delta:** opportunity added — retire-or-wire-linked-account-display-helper (`candidate`); scope reference added; tasks refined — document-recognition-and-handoff-limits and document-token-holder-prompts-and-reward-routes; no documentation task, owner ask, gap, page question, or draft added or removed.

- **2026-08-05** — Turned the repository-wide `DAO` terminology audit into review-dao-terminology-violations, a 39-item linked owner-review ledger, then corrected the task-state model after the owner identified that actionable owner work had been mislabeled as blocked. The audit task resolves the prior report’s ambiguous conclusion: the read-only protocol submodule has no identified violations because its uses name the OSx DAO contract or explicit token-governance cases, while the platform value proposition, terminology policy, account presentation, account-scoped product copy, navigation, and maintenance references do contain violations. Exact identifiers and historical/source captures remain outside the rewrite set. [WORKFLOW.md](./WORKFLOW.md) now separates actability (`status`) from ownership (`next_actor`): ready owner work goes in *Ready for your input*, ready agent work goes in *Ready to run*, and blocked means neither actor can move and requires a real external trigger. All task entries now carry `next_actor`; the DAO review is `status: ready` / `next_actor: owner`, with a visible owner action rather than a trigger. **Board delta:** task added — review-dao-terminology-violations (ready; next actor: owner); workflow and task metadata migrated; owner ask added — disposition all 39 ledger items; no task retired and no draft, page question, or knowledge gap changed.

- **2026-08-05** — Set the publication boundary from the product owner's ruling on the codebase audit's four built-but-unreachable findings. Canonical product documentation now covers a feature only after the owner confirms it is live; partially implemented, in-development, local/preview-only, and unused source surfaces stay out until launch, while “partially supported” remains a valid documented state for a live team-assisted capability. [WORKFLOW.md](./WORKFLOW.md) carries the maintainer-only exclusions: automated capital flows/policies and the in-app support channel are not live, and the tested four-tier linked-account/plugin display-name helper has no production caller and therefore defines no current naming rule. Linked accounts themselves are fully live; the audit's local-only flag observation was not their availability boundary, so their present-tense pages remain correct. The generic [reach-out pattern](./design/reach-out-to-the-team.md) is unchanged and unrelated to the in-development support channel. The exclusion was applied to queued work by removing automated-capital-flow controls as a proposed [DAO slots](./design/dao-slots.md) example, narrowing the remaining capital-flow and support references to explicit scope boundaries, and removing both from canonical [Source repositories](./repositories.md) prose. **Board delta:** owner ask resolved — all four built-but-unreachable findings; tasks refined — document-recognition-and-handoff-limits and document-token-holder-prompts-and-reward-routes; no task, gap, opportunity, page question, or draft added or removed.

- **2026-08-05** — Reworked the access-control sequence from the product owner's section-by-section review. [Authorization and execution model](./access-control/authorization-and-execution.md) now defines access control, authorization, execution, effective authority, and administration in dependency order; treats target-local and shared policy as policy locations; and explains authority composition from a target-level role bundle through an account's aggregate downstream authority to the process that directs it. [OSx authorization paths](./access-control/osx-authorization-paths.md) now identifies each DAO as its organization's shared access-control authority and introduces guarded plugin checks before ROOT administration. [Scoped authority](./access-control/scoped-authority.md) now defines the execution-boundary scope directly, connects that scope to one account and treasury, states the selector condition's argument and value limits, and gives the OSx–OpenZeppelin comparison one home. The comparison records that both OSx and `AccessManager` provide shared access control while packaging the organizational account, treasury, execution route, and governance components differently. Eleven independent drafts were reduced through adversarial minimality, sequencing, and technical-truth reviews; [Gradual permission handover](./access-control/gradual-permission-handover.md) remained unchanged because its existing sequence already applies scoped authority over time. **Board delta:** no draft, task, opportunity, owner ask, or page-question change.

- **2026-08-05** — Folded the product owner's Aragon Profiles clarification into [Claiming an Aragon Name](./accounts/claiming-an-aragon-eth-name.md) and [Delegate profile record](./governance/delegate-profile-record.md). The deployed Member Registry is audited and authorized to create subnames under `aragon.eth`; its eviction and upgrade administration sit behind an internal Aragon-controlled address, so the page states the revocability and upgrade boundary without turning the exact internal holder into a product concern. ENS reviewed the delegate-record key and document shape and endorsed the approach, but the reference does not present that feedback as a general ENS standard or a headline product claim. Both page questions and their promoted backlog row were removed. **Board delta:** owner ask resolved — Aragon Profiles external-status facts; page questions resolved — Member Registry deployment and authority, and ENS review of the delegate-record shape; no task, gap, opportunity, or draft added or removed.

- **2026-08-05** — Processed the product owner's review of the app interaction-principles inventory into five draft design patterns: [normalize input without changing its meaning](./design/input-normalization.md), [show validation when it can help](./design/validation-timing.md), [preserve task context across dialogs](./design/dialog-continuity.md), [make user-supplied link destinations inspectable](./design/inspectable-link-destinations.md), and [use primary actions sparingly](./design/primary-action-hierarchy.md). The first two were rebuilt from first principles: normalize only proven meaning-preserving representations, and show errors at the earliest useful repair point. The owner's primary-action rule now owns both action scarcity and result-specific labels, replacing the narrower label-only candidate. Code verification corrected three audit claims: member-profile links already show their resolved destinations; proposal descriptions and delegate statements do allow arbitrary selected text to carry a URL through `TextAreaRichText`; and all live `SafeDocumentParser` callers pass strings while gov-ui-kit v2.9.0 accepts and sanitizes strings, so rich-content sanitization is a satisfied security invariant rather than an interaction principle or demonstrated vulnerability. Normal lock/wrap-to-delegation onboarding is explicitly sequenced and reported tested; only the internal note's exit-queue simultaneity case remains unverified, so document-token-holder-prompts-and-reward-routes now requires a combined reproduction or test instead of asserting that one prompt is lost. The reviewed inbox capture was fully mined and retired, and the two principles-page questions were resolved by filing interaction rules in the design pattern library rather than the platform-principles collection. **Board delta:** drafts added — input-normalization, validation-timing, dialog-continuity, inspectable-link-destinations, and primary-action-hierarchy; owner ask resolved — interaction-principle candidates; page questions resolved — candidate disposition and collection placement; task refined — document-token-holder-prompts-and-reward-routes; no gap or opportunity added.

- **2026-08-05** — Folded the product owner's token-governance, availability, and permission-management review into [Choose a voting-power mechanism for token governance](./guides/choose-token-voting-power-mechanism.md) and [Governance designer](./governance/governance-designer.md). The guide now starts from the economic claim whose reuse must be prevented, separates checkpoint-based Token Voting from custody-based Lock to Vote, makes the wrapper's pre-snapshot requirement and post-snapshot exposure explicit, and adds DeFi-position and veLocker routes without treating the underlying asset as the voting unit. Lock to Vote is available to everyone. Granular permission management is fully self-serve in Governance Designer, although complicated configurations may still warrant contacting the Aragon team. The application does not offer the token-voting approval threshold and installs it as zero. The advanced-governance guide and multisig-gate pattern no longer imply that a development environment or production handoff is required. Both reviewed pages left draft status; their answered questions and promoted input rows were removed, and affected tasks were pruned to remaining work. The adversarial Sol review then added the all-or-nothing and proposal-creator unlock constraints, removed a queued Lock-to-Vote/veLocker conflation, and reconciled the question inventory. **Board delta:** owner asks resolved — token-governance mechanism, Lock-to-Vote availability, and advanced-governance cutoff; tasks refined — document-governance-derivation-and-availability, document-token-holder-prompts-and-reward-routes, and rethink-the-guide-layer; drafts retired — choose-token-voting-power-mechanism and governance-designer.

- **2026-08-05** — Completed `inventory-the-interaction-principles` against `app@122f1bd1` after the product owner narrowed its repository scope to the app alone. The cleaned capture at `inbox/2026-08-05-app-interaction-principles-inventory.md` presents six candidates for keep/revise/reject review: normalize human-authored prose while preserving protocol-shaped input; validate after interaction or commitment and keep repair local; preserve parent state for nested decisions and sequence unrelated interruptions; reveal user-authored link destinations; sanitize rich content at one render boundary; and name primary actions by their immediate outcome. Each candidate carries at least two named app instances and the observed divergence or, where none was found, the unencoded contract. The pass explicitly excludes behavior already owned by the control-availability, wizard, transaction-submission, datalist, address, metadata, and drill-down patterns or by queued flow tasks. `gov-ui-kit` was not inspected and the capture makes that boundary explicit. **Board delta:** task retired — inventory-the-interaction-principles; owner ask changed from go-ahead and repository scope to candidate confirmation, absorbing the existing competing-prompt ask; the principles collection question now follows that confirmation; no canonical principle, draft, opportunity, or knowledge gap added.

- **2026-08-05** — Rebuilt the access-control folder from first principles after the product owner's page-purpose and naming review. Two graph-safe moves established the final names: [OSx authorization paths](./access-control/osx-authorization-paths.md) and [Scoped authority](./access-control/scoped-authority.md). Four dedicated Terra passes then separated the pages by question: [Authorization and execution model](./access-control/authorization-and-execution.md) owns the generic caller/target/authorization model and execute-collapse problem; OSx authorization paths owns the concrete permission tuple, condition, ROOT, `DaoAuthorizable`, and SPP/GlobalExecutor traces; Scoped authority owns the product rule for limiting processes at the account execution boundary; and [Gradual permission handover](./access-control/gradual-permission-handover.md), retyped from concept to pattern, owns the optional application of those scopes over time. The area index now presents that reading order, and current backlinks use the new titles. A highly adversarial Sol review found no blocker or high-severity defect, verified the concrete OSx and OpenZeppelin claims against the pinned sources, and drove eight bounded corrections: guarded-target qualification, native call value as an authorization input, the exact selector/native-transfer cases, removal of generic-page OSx leakage, removal of a compatibility-only stable-address section, direct pattern definition, one SPP tracing deduplication, and the primary-account → linked-account → inner-target caller sequence. The owner-reviewed generic and handover pages left draft status, reducing the authoritative draft inventory from 72 to 70. **Board delta:** drafts retired — authorization-and-execution and gradual-permission-handover; no task, gap, page question, or owner ask added or resolved.

- **2026-08-05** — Folded the product owner's App CMS answer into [App CMS](./app-cms.md): anyone may propose a change to the public repository, only Aragon contributors approve and merge changes, and Aragon makes hiding decisions case by case with the affected client. A hidden item is absent from the applicable UI; there is no general requirement to preserve another app route to it, while its underlying on-chain state remains unchanged. Removed the resolved page question and its promoted backlog row. **Board delta:** owner ask resolved — App CMS operating boundary; page question resolved — App CMS curation policy; no task, gap, opportunity, or draft added or removed.

- **2026-08-05** — Reclassified the two comparison pages as choice guides and moved them into `guides/` with graph-safe link rewrites: [Choose between Token Voting and Lock to Vote](./guides/choose-token-voting-power-mechanism.md) now opens with the outcome, canonical plugin prerequisites, availability, ordered branches, and a done state; [Choose between a Safe and an Aragon multisig](./guides/safe-vs-aragon-multisig.md) now routes the account-versus-plugin choice into the appropriate governance pattern. Extracted the reusable Safe definition into the new accounts concept [Safe](./accounts/safe.md), and redirected plain Safe references there while preserving comparison links where the choice itself matters. The existing Token Voting/Lock to Vote owner questions remain the only unresolved inputs; the deferred guide-layer task now audits all three current guides, so no new task or question was added. **Board delta:** draft added — Safe; two drafts retyped and refiled under Guides; no task, gap, page question, or owner ask added or resolved.

- **2026-08-05** — Renamed and reframed [Action simulation](./governance/action-simulation.md) after the product owner's first-principles review. The capability now belongs to a prepared action array and its intended execution context, not to a proposal: proposal creation and proposal details expose it because they already bundle the actions and identify the process, while direct transaction creation exposes the same simulation with the connected actor as caller and no proposal or plugin involved. The page now separates that generic execution test from proposal-specific access rules such as cached results, the ten-minute rerun limit, and non-terminal-state availability; it also states explicitly that proposal metadata, status, voting, approval, and lifecycle never enter the simulation. The Action builder, Proposal, direct transaction, staged-permission comparison, guide, and governance map now use the generic name and boundary. The reviewed page left draft status and the draft inventory. **Board delta:** draft retired — action-simulation; no task, gap, page question, or owner ask added.

- **2026-08-05** — Renamed [Proposal actions](./governance/action-builder.md) to **Action builder** with a graph-safe move, rewriting all 42 inbound links from `governance/proposal-actions.md` to `governance/action-builder.md`. The capability is no longer framed as proposal-owned: it composes and renders ordered action batches for both [proposal creation](./governance/proposal-creation.md) and direct [`DAO.execute` transactions](./treasury/create-transaction.md), while its process-scoped allowed-actions filter remains explicitly a proposal-creation behavior. [Action](./governance/action.md) now makes both execution routes first-class and defines proposal versus direct execution as an authorization-route difference rather than an action-shape difference. Current indexes, draft inventory, capability references, guide inputs, and design examples use the Action builder name; historical log wording remains historical. **Board delta:** none.

- **2026-08-05** — Rebuilt and renamed [Governed account authority](./access-control/scoped-authority.md) after the product owner's first-principles review, informed by three parallel passes over narrative purpose, permission semantics, and product-surface boundaries. The page now leads with the DAO as the stable holder of external authority and the process-specific scopes that divide it. The central technical problem is precise: a bare Execute grant admits every action batch the DAO can complete, while an `ExecuteSelectorCondition` restores target-and-selector scope at the `DAO.execute` boundary and leaves argument-level rules to more specific conditions. Separation of powers now follows from that mechanism; the OpenZeppelin comparison credits `AccessManager` with analogous cross-contract centralization and confines fragmentation to target-local `Ownable` or `AccessControl` assignments. ROOT's self-call path for governed grant/revoke actions is explicit, and live allowlist changes are identified as configuration changes on the existing condition rather than upgrades. Detailed action-builder behavior, the condition catalogue, proposal-creation conditions, permission signaling, and cross-account caller authorization remain with their own pages; current backlinks and area/root navigation use the new concept name. The reviewed page left draft status and the draft inventory. **Board delta:** draft retired — governed-account-authority; no task, gap, page question, or owner ask added.

- **2026-08-05** — Corrected the proposal ontology split after the product owner's structural review. [Proposal](./governance/proposal.md) is now the canonical object page beside process, body, action, and plugin: it defines the cross-plugin object, its process ownership, metadata and optional ordered actions, identifiers, list partition, signaling form, lifecycle state, and shared product surfaces. [Proposal creation](./governance/proposal-creation.md) now owns the capability end to end: entering the flow, selecting the proposal type/process, preflighting and configuring creation eligibility, composing metadata/actions/plugin settings, and submitting the transaction. The existing eligibility material remains intact under its own section; the wizard material moved out of the object page; creation-specific anchors and the governance area map now point at the capability page. **Board delta:** none.

- **2026-08-05** — Rebuilt [OSx authorization mapping](./access-control/osx-authorization-paths.md) from the common path outward after the owner's second review. The stable DAO address and its value now lead; one focused plugin → DAO → target section owns the main call flow and its two independent authorization relationships; ROOT now owns every grant/revoke statement and is described affirmatively as the permission-table administrator with total authority. The Safe, direct-address execution, unguarded-target, generic alternate-executor, and permission-signal detours are removed. `DaoAuthorizable` now sits late as a contract-integration pattern beside OpenZeppelin `AccessManaged`/`AccessManager`, and the final advanced-governance section explains the specific SPP constraint that earns the `DelegateCall` route: a plugin body uses the GlobalExecutor loop in its own context so `reportProposalResult` reaches SPP from the body's address. The account, target, and access-control index pointers now match those boundaries. The owner's rejection of the permissionless-GlobalExecutor digression resolves the upstream-placement ask without an upstream change. The reviewed page left draft status and the draft inventory. **Board delta:** owner ask resolved — GlobalExecutor consequence disposition; draft retired — osx-authorization-mapping; no task, gap, or page question added.

- **2026-08-05** — Commissioned a two-pass repository-ontology review. audit-page-purpose-and-repository-topology is queued after the six current source-mining tasks (`ready`): it accounts for every page's purpose, compares the aggregate topology and taxonomy, applies supported split/merge/retype/refile decisions, and clarifies retained page openings before owner review. audit-section-fit-and-location follows it (`blocked` on the first pass): it tests every substantive section against the settled page purpose and relocates or consolidates macro-level content before that same review. A duplicate check kept two neighboring tasks distinct: run-the-first-post-review-consistency-sweep remains a later correction-driven terminology, provenance-style, and convention pass after the owner's review, while rethink-the-guide-layer owns the user-outcome portfolio rather than the role or internal structure of every page. Either new audit runs agent-led until a material disposition genuinely needs product intent; it then parks in progress and moves its single row to **Ready for your input** with bounded questions. **Board delta:** tasks added — audit-page-purpose-and-repository-topology (`ready`) and audit-section-fit-and-location (`blocked` on the first task); draft-review ask deferred behind both passes and its former settle-first subset removed; no owner ask added yet; no gap opened, closed, or narrowed.

- **2026-08-05** — Advanced the pinned [protocol-doc](./protocol-doc/index.md) snapshot from [`2d800ee`](https://github.com/aragon/protocol-doc/commit/2d800ee849be6f02c8f9676c9470090127586751) to [`3e4cd0d`](https://github.com/aragon/protocol-doc/commit/3e4cd0dd56727ba6e28964f73908ebb3cb90ebeb), the latest `main` commit verified on 2026-08-05. The sync incorporated 13 upstream commits across 42 existing Markdown files, with no added or deleted entries; the changes clarify prose and heading hierarchy. Upstream `wiki.toml` did not change, so the parent composite vocabulary and ignore configuration required no reconciliation. Updated the root index's exact SHA and snapshot date. **Board delta:** none.

- **2026-08-05** — Reworked [OSx authorization mapping](./access-control/osx-authorization-paths.md) against a product-owner review of the page, run as five parallel analysis passes (framing, upstream-redundancy disposition, the `DaoAuthorizable` section, the Safe passage, the two closing value claims) followed by one adversarial review that caught a self-contradiction the first rewrite introduced. **The overarching correction the owner asked to be extracted: the page was describing OSx, which protocol-doc already does, instead of applying the model to it — so every passage now has to make one identifiable point in the model's vocabulary with a stated relevance to a platform reader, rather than chain protocol facts by association.** The page is reframed as the [authorization and execution model](./access-control/authorization-and-execution.md) with Aragon's contracts substituted into its roles, opening on a role table (governor, account, intermediate guarded target, action target, shared configuration) whose third column is what each substitution obliges an organization to configure. The duplicated ASCII flow diagram is gone; the model page owns it.

  **What the review's specific objections resolved to.** *Guarded functions* are now framed as any contract deferring to a DAO, not as plugin functions: a `DaoAuthorizable` contract necessarily carries a DAO reference because the DAO is the permission manager, the *where* is the guarded contract rather than the DAO, and the two consequences are that naming a DAO grants nothing and that one table governs many contracts' functions. The `PermissionManager`-inheritance contrast, the `hasPermission` code block, and the whole nested-execution paragraph are cut as upstream mechanics. The Safe passage was three unrelated claims filed together; its one live point survives where it belongs — execute on the DAO is a grant on an **address**, so a Safe or EOA reaches `DAO.execute` through the identical check ([create transaction](./treasury/create-transaction.md) is the surface) — and the section it was squatting in is now about routes alone. The route bullets keep the caller explicit per route as [target](./governance/target.md) promises, name `DelegateCall` as the route the designer gives every plugin body rather than as an exception, and record that no `DAO.execute` check stands in that trace. The stable-address claim leads with when and why it matters — leaving an admin bootstrap, handing an action scope to a new process, while external contracts and treasury positions already point at the account — and this page now owns that elaboration, with [account](./accounts/account.md) reduced to a pointer. The signals section names the linked-account acknowledgement grants concretely and says plainly that nothing ever asks the DAO about them.

  **Board delta:** owner ask added — rule on whether the permissionless-`GlobalExecutor` integrator consequence belongs upstream in protocol-doc; [access-control index](./access-control/index.md) reworded off the rejected "guarded plugin functions" framing; the page remains `status: draft`; no task, gap, or page question added or removed.

- **2026-08-05** — Completed capture-the-action-builder-feature-set from the product owner's full action-builder briefing, with the implementation details the owner left uncertain verified against `app@122f1bd1` and the reusable view behavior checked against the local `gov-ui-kit`. [Proposal actions](./governance/action-builder.md) now starts from the empty builder and its three routes; makes action optionality and ordered execution explicit; states exactly what **Only show allowed actions** filters and why it hides WalletConnect and Upload; explains the curated registered-action list, contract-and-selector grouping, proxy and implementation ABI lookup, verified write-function expansion, and unverified raw fallback; gives Transfer its asset-first exception with the ERC-20 versus native-value distinction; and replaces the task's stale Basic / Advanced / Byte wording with the live **Basic / Decoded / Raw** composer model, including editability, NatSpec, Solidity-type validation, tuple and array fields, payable value, and calldata re-encoding. The WalletConnect section now distinguishes the pasted connection URI from the QR that exposes it, documents queued `eth_sendTransaction` requests and raw fallback, and states the sequential-dApp limitation without claiming every external flow fails. JSON reuse now owns the exact `{to, value, data}` array, validation, batch decode, loading state, and raw preservation path.

  [Proposal](./governance/proposal.md) now names the zero-action result as a signaling proposal: voting remains onchain, but no action batch exists, so a successful proposal ends ACCEPTED rather than becoming EXECUTABLE. The briefing's product idea became the candidate [reveal all write functions for known contracts](./governance/reveal-all-write-functions-for-known-contracts.md), scoped so the curated list and allowed-action filter remain intact. Its tentative guide idea was routed into rethink-the-guide-layer as a **compose proposal actions through an external dApp** candidate, where the guide rubric can decide whether the WalletConnect sequencing caveat warrants a walkthrough. Two queued tasks that treated the action-builder capture as future work now point to the canonical page instead. Per the owner's instruction, this pass does not count as draft review: the two canonical pages remain `status: draft` for the later drafts-review pass. **Board delta:** task retired — capture-the-action-builder-feature-set; owner ask resolved — deliver the action-builder brain dump; opportunity added — reveal-all-write-functions-for-known-contracts (`candidate`, governance); existing blocked task rethink-the-guide-layer refined without a state or ordering change; no page question, knowledge gap, or canonical draft added or removed.
- **2026-08-05** — Closed out audit-the-codebase-for-undocumented-business-logic at `app@122f1bd1`, the code-first counterpart to the verify pass: four coverage oracles over all 87 platform pages, two seam indexes over the code, eight classification slices, and a lead-session verification layer that overrode three agent verdicts. **The headline is that the task's own flagship candidate inverted.** Capital flow is indeed the largest undocumented cluster in the codebase — the base cannot name its central nouns, "router" and "dispatch", anywhere outside the repository table — but both its entry points sit behind a flag enabled in `local` only, and its addresses are configured on Sepolia alone. Under this pass's own boundary that makes it an owner question, not documentation work, and it collapsed ten of one slice's twelve claimed absences. Six tasks were filed, ordered corrections-first.

  **Corrected enumerations.** The workspace's own architecture doc is wrong on both counts it states: **9** modules (adds `capitalFlow`, `ens`) and **7** plugins (adds `capitalDistributor`, `lockToVote`, `gaugeVoter`), exactly as the task predicted. Beyond that: **5** boot registries, not 4 — `initActionViewRegistry()` is wired from `providers.tsx` and `layoutRoot.tsx` rather than `initPluginRegistry.ts`, the sharpest instance of the task's "not every registry is wired in the obvious place". **84** dialogs across **19** real registries (24 files match the naming pattern; 4 are form-shape types, 1 an aggregator). **34** slot ids over 6 files, **5** wizard-step registrations, **9** per-account registrations across **7** directories (xmaquina and katana each ship two), **12** feature flags, **55** shared entries.

  **The classification.** *Modules (9):* `governance`, `finance`, `createDao`, `explore`, `dashboard`, `settings`, `application`, `ens` — all covered or yielding narrow absences now filed; `explore` and `ens` fully covered with zero absences. `capitalFlow` — the owner question. *Plugins (7):* `multisig`, `token`, `spp`, `lockToVote` covered by the 28-page governance area, with residual absences in the filed tasks; `admin` ruled out cleanly with none; `capitalDistributor` and `gaugeVoter` covered as capabilities but yielding the integration-depth finding below. *Workspace apps (2):* `apps/app` is the subject; `apps/assistant` plus `assistant-chat` are the support-chat owner question, and `assistant-contracts` is out of scope as shared schema. *Boot registries (5):* plugin slots pre-closed to [plugin slots](./design/plugin-slots.md); the policy registry inside the capital-flow owner question; per-account packs yielding the DAO-slots worked instance; the condition registry ruled out as display machinery, not the catalogue three decisions rejected; the action-view registry covered by [action](./governance/action.md).

  **A category that spans modules and one that comes from a seam**, both required and both present. Capital flow spans a module, two plugins, three action packs, and the per-account packs across treasury and governance. The onboarding-prompt policy is the seam category: its triggers live in `components/` and `hooks/` rather than `utils/`, and outside the dialog definitions entirely, so two slices each assumed the other held it — it was found only by naming it explicitly.

  **Ships-or-gated, with one qualifier that governs all of it.** 8 of 12 flags omit `production`. The override cookie is honored in production with **no authentication anywhere in its parse path**, so "does not ship" means "off by default", never "unreachable" — every gate note is phrased that way. Two flags are `local`-only: `capitalFlowAutomation`, and `linkedAccount`, which gates a cluster **six pages** describe in the present tense. `permissionsPage` is defined but **never read** — the subpage renders unconditionally everywhere, so [permission model](./access-control/scoped-authority.md)'s claim holds in production by accident, the gate its own description promises having never been wired. `gateAdvancedGovernance` inverts the usual sense: production-only, and it turns *on* an account-creation cutoff of `2026-04-02` that passed four months ago, set identically in all five env configs and failing closed.

  **Three agent verdicts overridden by first-hand reading.** A claim that code hard-blocks removing an account's last full-execute process, contradicting the owner-validated [removing the last governance process warns, never blocks](./accounts/last-process-removal.md), was **refuted** — the dialog is an alert with no refusal branch. But the truth is better than either version: uninstall is always routed as a proposal through *another* full-execute process, so removing the last one has no route, not because the product refuses but because the mechanism needs a carrier that would no longer exist. An emergent dead end, not an eligibility guard, and it bears on that page's own open question about recovery paths. Separately, an earlier session note claiming the permissions view was gated out of production was wrong for the reason given above, and the capital-flow slice's absence count was over-collection resolved by checking the gate.

  **Out of scope, argued rather than assumed.** The 70-entry permission-name taxonomy is precisely the catalogue three prior decisions rejected. Observability noise classification is architecture, on the same reasoning that already excluded the backend event catalogue — as is the frontend analytics taxonomy, adjudicated explicitly rather than skipped, and useful only as a cross-check, which it passed. Exit-queue math, multiplier curves, and fee curves stay upstream per [veLocker](./governance/velocker.md)'s own boundary. Toasts are not a gap at all: the product has no toast mechanism, verified by repo-wide search, and wallet-connection state belongs to a third-party connector.

  **The locale cross-check corroborated the flagship from an unrelated direction.** All 12 `app.*` namespaces have a home in the derived set, so nothing looks invented; exactly one large subtree has no task — `capitalFlow`, at 9.2 KB, larger than `settings` and comparable to `governance` — and it has none because it is dark-launched, not because it was missed.

  **Routing, named not mined:** action-builder composer depth stays with capture-the-action-builder-feature-set; input-sanitization posture across 91 files goes to inventory-the-interaction-principles. The task text reserved wizard per-screen narration for capture-wizard-screen-walkthroughs, which **completed and retired on 2026-08-04** — so that material is covered, not reserved, and the routing note was stale. The four surfaces the verify pass settled were fast-pathed rather than re-mined.

  Three absences were found independently by two slices each — the wizard exit guard, transaction resumption, and a recognized plugin integrating as one stand-alone page with zero governance-slot participation — which is the strongest evidence available that they are real rather than artifacts of one reading. **Board delta:** tasks added — document-governance-derivation-and-availability, document-token-holder-prompts-and-reward-routes, document-interrupted-and-resumed-flows, document-page-and-list-state, document-recognition-and-handoff-limits, document-treasury-composition-and-history (all `ready`); task retired — audit-the-codebase-for-undocumented-business-logic; owner asks added — two rows, one ruling on four built-but-unreachable surfaces and one confirming two behaviors that look unintended; owner asks narrowed — App CMS operating boundary (six fail-open surfaces plus the unauthenticated flag cookie) and token-governance setup (the availability sub-question answered: two gates exist); page questions — one deleted from [reach out when the abstraction cannot stay honest](./design/reach-out-to-the-team.md), taking the platform aggregate to 29 across 22 pages; opportunity added — wire-or-retire-the-permissions-page-gate, the flag that described a restriction it did not impose; no gap opened, closed, or narrowed; one draft-equivalent added as an opportunity entry, and no canonical page changed beyond that one checkbox deletion.

- **2026-08-04** — Closed out verify-documentation-against-source-code, run as four parallel Sonnet passes (one per deliverable) each followed by an adversarial Opus review at `app@122f1bd1`. The reviews mattered: they refuted the pass's flagship invariant finding, overturned a structural claim about slot machinery, and found that a state two pages describe is currently unreachable by its intended route. Everything below is what survived that second pass.

  **Plugin slots — all four halves of the contract, none left implicit.** [Plugin slots](./design/plugin-slots.md) now states what a plugin declares (identity, install version, and the optional setup copy that is what makes it *selectable* in the add-body flow — which is why the admin plugin and the staged proposal processor are installed but never offered as a choice, network availability being a second gate); that skipped slots fall back to a generic default *except* on the transaction path, where a missing registration fails outright, so "required slot" is the exception and the exceptions are the installation ones; that validation is the plugin's own business riding the form it appears in, including cross-field rules like the multisig threshold capped at member count, with no plugin-supplied validation step for the generic flow to call; that transaction composition asks the plugin for the encoded installation call and never inspects it, with update following the same split and uninstall the reverse one (the plugin supplies helper addresses, the generic code encodes around them); and that the definitions live in one registration table each plugin fills from its own folder at startup.

  Two first-pass claims were refuted and are not on the page. Slot fields do **not** all live in one wizard form — the add-body dialog is its own form, which is why a body returns to the designer only once its plugin's fields pass. And DAO slots are **not** separate machinery: a bespoke account registers into the same table keyed by the account instead of the plugin, so [DAO slots](./design/dao-slots.md) now says the two kinds are one mechanism separated by convention, correcting its "isolated in a dedicated area of the codebase" framing to a folder discipline the team upholds rather than a boundary the code enforces. Both citations already on the plugin-slots page were re-verified and are exact.

  **Repository entry points.** [Source repositories](./repositories.md) gains an *Inspect first* column plus a section saying what each entry point owns and what layout fact would otherwise waste a reader's time. Two picks changed under review: `app` resolves to the plugin aggregator rather than the eleven-line bootstrap it feeds, and `app-backend` to the v2 API router rather than the indexer config, which is named as the second stop instead — the API surface is what the product's own areas map onto. Three corrections came with it: the `app` row now notes the monorepo also ships a separately deployed support-chat service; the `app-cms` row's feed description was too narrow and now covers compliance lists and feature flags as well; and a new `aragon-domain` row closes an inconsistency where the base documented that dependency's behavior across four pages without listing it as a source. The page's condition-library question is resolved by the checkout itself and removed. A caveat records that the local `gov-ui-kit` lags the version `app` builds against.

  **The permission boundary holds, with two qualifications.** No grant/revoke dialog, page, or registered action view exists anywhere in the frontend, and the designer's allowlist composer is literally the ordinary action composer with raw calldata excluded — so [permission model](./access-control/scoped-authority.md)'s "generic account action, not a product feature" framing is correct, and the page now says outright that a composed grant is indistinguishable from any other contract call. What the page under-claimed: permissions *do* have a product surface, a read-only settings view in list and graph form with each condition broken out, which is the derivable-from-chain-state rule in practice rather than an exception to it; and the designer is the *primary* guided flow, not the only one, since admin membership is composed as grants and revocations presented to the user as membership. The empty-allowlist paragraph lost its claim that the app can create that state: the state is real and displayed, but its direct route is broken (filed as an opportunity), and the page now states that no control edits a live allowlist, so such a change is a governed action against the condition contract needing that contract's ABI to render readably. All six specific claims checked held. The allowed-actions list being backend-computed was assessed against the chain-state principle and judged a non-issue — the backend indexes chain events, and the page's independent-verifiability promise is untouched.

  **What the app refuses, and what it batches.** The pass's headline refusal finding did not survive: the support threshold's cap just below 100% is *not* the app being stricter than the protocol. The protocol reference cited to prove it says the opposite — the protocol caps that threshold below 100% itself — so the app is mirroring the protocol's strict-versus-loose comparison, not closing a gap. [Refuse unsatisfiable configuration](./design/invariant-validation.md) therefore presents that cap, the stage threshold bound, and the reused multisig rule as the app *surfacing* protocol bounds, and names exactly one refusal of the app's own: a process must have at least one live way for a proposal to be created. That one is a whole-design check rather than a field bound, it blocks rather than warns, it is test-covered in the app, and it has no protocol counterpart — the contracts would deploy a process nobody can propose into. Three borderline cases were deliberately not published as invariants (a token amount above zero, duplicate permission selectors, and a one-member floor that is emergent from three unrelated UI mechanisms with no rule stating it); the page's checkbox is narrowed to the peripheral setup flows this pass did not open. [Action](./governance/action.md) now names what arrives as one wallet request — any multi-action proposal, direct execution, the designer's two-signature install whose proposal can carry a dozen-plus granular actions, and uninstall or update — plus the case where the publishing process is the bootstrap admin plugin and creation *is* execution, so one signature performs the whole bundle atomically. Its checkbox is narrowed to the wallet half, which is now provably beyond the source's reach: the app hands the wallet only a target, a value, and calldata, with no typed data, batching capability, or human-readable hint, so the confirmation dialog is entirely the wallet's own decoding.

  **Filed rather than absorbed.** Two product opportunities: publish-a-process-with-no-selected-actions (the designer's terminal step fails silently when the author never opened the actions list, affecting both radio choices — established by library-level reproduction rather than a browser run, and flagged as such in the entry) and pre-empt-protocol-rejected-configuration (three advanced-flow values the app accepts and the chain refuses: a zero stage expiration that breaks two strict inequalities, a duplicate body address in one stage where the error plumbing exists but is never populated, and no upper bound on a voting duration). One page question added to [Governance designer](./governance/governance-designer.md): whether the absolute floor on approving voting power should stay unexposed, since both token-based paths install it as zero with an explicit code comment saying so. Two findings were recorded without filing: the product workspace's hand-written structure and slot docs have drifted from the code, now a caution on the repositories page and in the audit task's *Where to look*; and the backend's vendored contract bindings demonstrably do not track the published artifacts packages, since it declares no dependency on them — no product-semantic consequence, so it stays here. The audit task's three forward references to this pass were updated from pending to settled. **Board delta:** task retired — verify-documentation-against-source-code (row removed, file deleted); opportunities added — publish-a-process-with-no-selected-actions, pre-empt-protocol-rejected-configuration (both `candidate`, governance); owner ask extended — the token-governance row gains a fourth call; page question added — Governance designer (approving-power floor); page question resolved and removed — Source repositories (condition library public); no knowledge gap opened, closed, or narrowed; drafts unchanged at 74; open questions 30 across 23 platform pages.

- **2026-08-04** — Completed capture-wizard-screen-walkthroughs from the product owner's walkthrough of all four full-screen transaction-input flows, with the plugin-dependent Create Proposal step verified against `app@122f1bd1`. [Account creation](./accounts/account-creation.md) now states the two-step Create DAO sequence: select the chain first, defaulting to Sepolia with the first-launch test-DAO recommendation, then provide the DAO name, logo, description, and resources; the following transaction dialog submits one factory deployment. [Proposal](./governance/proposal.md) now owns the direct Create Proposal sequence from the top of the Proposals page: select a proposal type only when multiple processes are available; collect title, quick-understanding summary, rich-text rationale/body, and resources; add actions; show plugin settings only when required; then submit one create-proposal transaction. The source audit found five proposal-building integrations: Admin has no settings screen; Multisig has start and end with a five-day recommendation; Token Voting has start and end constrained by its configured minimum; Lock to Vote has start only because duration is configured on the plugin; and SPP has start only because its stages determine later timing. The shared action-builder's detailed composing behavior remains with capture-the-action-builder-feature-set.

  [Create transaction](./treasury/create-transaction.md) now states its one-step action-builder wizard, execute-permission-gated entry point on Transactions, and terminal single `DAO.execute` transaction dialog. [Governance designer](./governance/governance-designer.md) now carries its four parent screens — process metadata, governance, proposal-creation rules, and permissions — plus the nested basic-body dialog: plugin choice; Multisig membership and threshold; Token Voting create/import token and governance settings; and Lock-to-Vote configuration. It also records the advanced-flow metadata distinction and the Specific-actions replacement of the default unrestricted execute grant with an execute-selector-conditioned grant. [Wizard](./design/wizard.md) makes dialog-inside-full-screen the only nesting direction; [Full-screen wizard](./design/full-screen-wizard.md) summarizes every flow; [Metadata input](./design/metadata-input.md) and DAO presentation use the UI's **logo** label while retaining the serialized `avatar` field distinction.

  The requested post-walkthrough gap review found no structural advanced-governance gap: stages, bodies, targets, creation conditions, scoped execution, and prepare/apply installation already give the hidden advanced UI's capabilities a complete conceptual model. Its development-only screen order is therefore not promoted into a new owner ask. No page question, product opportunity, or replacement task was warranted. **Board delta:** task retired — capture-wizard-screen-walkthroughs (all four flows delivered); owner ask resolved — wizard walkthrough; no gap opened, closed, or narrowed; no draft added or removed.

- **2026-08-04** — Mined the product owner's app-metadata-input briefing and closed the backlog's top knowledge gap. New draft [Metadata input](./design/metadata-input.md) gives the repeated product abstraction one home: the app asks for a name or title, description, and resources modeled as URL-plus-label fields, pins the resulting JSON to IPFS, and passes the pointer into the transaction without asking the user to understand or paste an IPFS hash. The page keeps that app-owned schema distinct from the protocol's free-form plugin metadata bytes.

  The same installed-plugin metadata follows the plugin across the product's role interpretations: a plugin that is both a process and a body may present one name, description, and resource set in both the process-partitioned proposals experience and the body-partitioned members experience. Process metadata adds the process key; Capital Distributor campaigns apply the core as title, description, and resources while keeping asset, payout, allocation, and schedule as operational configuration; gauges add an optional avatar while keeping the gauge address outside the metadata payload. Plugin, Body, Gauge voting, Capital Distributor, and Proposal identifiers now link to the shared definition instead of leaving the pattern implicit.

  App verification at `122f1bd1` confirmed the process and body forms, plugin update action, campaign and gauge metadata builders, shared resource shape, and wizard step order. It also supplied the practical qualification to “metadata comes first”: prerequisite selection may precede it when that choice determines the form — network before Create DAO metadata, plugin before Add Body metadata — but the definition still precedes governance, membership, actions, permissions, and other behavioral configuration. [Wizard](./design/wizard.md) now states the one-idea-per-step rule and links the pattern. The broader capture-wizard-screen-walkthroughs task did not fire; this was a cross-wizard pattern briefing, not a per-screen walkthrough of one of its four flows. The draft inventory rises from 73 to 74. **Board delta:** knowledge gap closed and owner ask removed — app metadata pattern; draft added — Metadata input; no task or opportunity added, fired, or retired.

- **2026-08-04** — Closed out derive-the-executor-acl-auth-model's remaining agent-owned portion, run as three parallel Sonnet passes (OSx-route validation, one-pager disposition, rejected-terminology sweep) followed by one adversarial Opus review, which caught a fabricated execution route the first pass had introduced. **Routes and callers.** [OSx authorization mapping](./access-control/osx-authorization-paths.md) now leads its alternate-route section with the rule that the plugin's route decides which address action targets see, then names three: the default `Call` on the plugin's own DAO; `DelegateCall` to the shared GlobalExecutor, where the loop runs in the plugin's context so targets see the plugin; and `Call` to another executor, which must expose the same `execute` entry point, makes the action calls itself, and is therefore the caller its targets see. That third route carries the consequence the page was missing: another DAO applies its own `EXECUTE_PERMISSION_ID` check, while the standalone executor applies none, so a target authorizing the shared instance has authorized anyone who can reach it. Nested execution and the reentrancy rule are linked upstream rather than restated. The ROOT paragraph stopped re-enumerating dao.md's end states and states the product consequence instead: ROOT is not a runtime shortcut past a guard, but a ROOT holder can grant itself execute and act as the account, so ROOT is total control over the organization's authority.

  **A wrong route was found and removed, on two pages.** The first validation pass proposed, and the mapping page briefly asserted, that a staged proposal processor can be a `Call` route target holding `EXECUTE_PERMISSION_ID` on its DAO. That is impossible: the processor's `execute` takes a proposal ID, not the executor interface's `(callId, actions, allowFailureMap)` — confirmed in `app-backend`'s `stagedProposalProcessor` artifact against `osx` `Plugin.sol`'s `IExecutor(_target).execute(...)` — so the selectors cannot match. The claim was inherited from [target](./governance/target.md) and had propagated: target, [governance designer](./governance/governance-designer.md), [process](./governance/process.md), [body](./governance/body.md), and the governance index all now say that the processor is the *action's* target rather than the plugin's route, and that what a plugin's approved actions accomplish — not its `TargetConfig.target` — is what makes it a process or a body. The app gives each plugin body it installs a `DelegateCall` route through the shared GlobalExecutor (`app` `pluginTransactionUtils.getPluginTargetConfig`), so the body's actions run in its own context and its `reportProposalResult` call arrives from the body's own address, which is the only kind of report the processor counts; a body that is not a plugin has no route to configure. **Verification scope:** `osx` `Plugin.sol`, `Executor.sol`, `DAO.sol`, `PermissionManager.sol`, `DaoAuthorizable.sol`/`auth.sol`, the pinned dao/permissions/execution/auth/plugins pages, and `app`/`app-backend` for the target-config and processor facts. The staged processor's own Solidity is not checked out locally, so any processor-side claim rests on protocol-doc plus those app artifacts. Everything else held as written: the `dao.hasPermission(plugin, caller, permissionId, calldata)` argument order, the DAO's own-storage `auth` resolution, the `EXECUTE_PERMISSION_ID` gate with calldata reaching the condition, the ROOT claims, the governance-replacement claims, and the grant-event and silent-no-op signal claims.

  **One-pager disposition.** `inbox/acl-one-pager.md` is mined and removed. Most of its positioning material was already covered, in several places more carefully than the source stated it. Three claims did not survive validation and are carried nowhere: "targets trust only the executor" (exclusivity is the target's own configuration), "a single contained permission update" (the mapping page already says not necessarily one), and "introduced or retired by simply granting/revoking permissions" (adding a body is a plugin installation, not only a permission change). Three claims lacked homes and got them, all in the product's register rather than the source's: [permission model](./access-control/scoped-authority.md) now carries the source's central argument — authority written into each target contract means a governance change has to be repeated, sequenced, and re-reviewed on every one of them, growing with the contract count and leaving one group's authority fragmented rather than expressed once — plus the integration starting points (transfer `Ownable` ownership, grant the roles and revoke legacy holders, or name the account at deployment) and the correction that sole authorization is the target's configuration, not something the account asserts. [Value proposition](./value-proposition.md)'s evolving-governance bullet gained the customer-facing form: governance can be replaced without reconfiguring the contracts the organization already governs. The source's Path A/Path B framing, "governor-executor monolith", "blast radius", "boundary", and "governance legos" are deliberately not carried; the mechanisms behind Paths A and B already live on safe-vs-aragon-multisig, stages-over-direct-permissions, and create-transaction. The "new app / `onlyOwner` / RBAC" checklist was not promoted to a guide — the enabling capability is documented and the app has no flow behind it — and guide-portfolio decisions belong to rethink-the-guide-layer.

  **Terminology sweep: clean.** This was a scan for the rejected vocabulary, not a correctness audit of every page. Every canonical page in the root, `accounts/`, `governance/`, `treasury/`, `access-control/`, `design/`, and `guides/` was read, plus both model pages' backlinks and the concurrent optimistic-governance page. No page uses runtime-principal, execution-engine, authority-path, policy-as-authorizer, boundary-as-enforcement-point, direct-authorization, effective-control, disembodied-governor, or loose-ACL framing in the rejected sense; every keyword hit was ordinary English. The route error above was a factual defect the vocabulary sweep was not looking for, which is why the adversarial review caught it and the sweep did not. The two access-control pages stay `status: draft` for the separate drafts review. **Board delta:** task retired — derive-the-executor-acl-auth-model (row removed, file deleted); verify-documentation-against-source-code is now the first *Ready to run* row; no owner ask added or resolved; no gap opened, closed, or narrowed; no draft added or removed.

- **2026-08-04** — Mined the product owner's optimistic-governance briefing and closed the backlog's top knowledge gap. New draft [Optimistic governance](./governance/optimistic-governance.md) gives the concept one product-level home: it is an accept-unless-challenged decision pattern rather than one implementation; it reduces routine affirmative-voting, quorum, and transaction overhead by letting designated proposers author the concrete proposal while veto holders still monitor for proposals that need an objection; and its calendar duration still includes a protected objection window. The page distinguishes the general pattern from the platform's precise staged expression — vetoing bodies, no approval requirement, a positive veto threshold, and an explicit advance call after the window — and makes the institutional ordering configurable. The owner-described recurring core-team-or-multisig → token-holder-veto pattern and the community-approval → security-council-veto variant are examples, not prescribed bodies or stage order.

  The same page records the central failure mode: default passage is only protective when veto holders discover, understand, and can act on a proposal before the window closes. [Multisig gates](./governance/multisig-gates.md), [Stage](./governance/stage.md), [Staged proposals](./governance/staged-proposals.md), [Gradual permission handover](./access-control/gradual-permission-handover.md), [Value proposition](./value-proposition.md), the [multisig guide](./guides/multisigs-in-advanced-governance.md), and the governance index now link to that definition instead of partially re-defining it. The multisig pages also separate post-token approval from post-token veto rather than assigning security councils one universal role. The owner characterized Aragon's old Govern product, paired with Aragon Court as its escalation mechanism, as a pioneering implementation championed by Jorge Izquierdo; that remains attributed historical provenance rather than present-tense product behavior. Arbitration, optimistic-oracle, and optimistic-rollup analogies likewise remain context rather than importing those mechanisms into the canonical governance model.

  The guardian-DAO guide candidate on rethink-the-guide-layer now preserves the concrete use-case sketch: a vault curator or operator proposes changes while liquidity providers or another token-holder constituency can veto. It also preserves the owner's warning that this is only a sketch and the guide needs its own deeper treatment; no premature guide or Guardian DAO concept was created. The draft inventory rises from 72 to 73; the open-question, task, and opportunity inventories are otherwise unchanged. **Board delta:** knowledge gap closed and owner ask removed — optimistic governance; draft added — Optimistic governance; existing blocked task rethink-the-guide-layer refined without a state or ordering change; no task or opportunity added or retired.

- **2026-08-04** — Rebuilt the authorization and execution model from first principles after the owner's line-by-line review rejected the draft's generic authority stack. Two Sol producer audits split the work into governor/social-choice semantics and concrete authorization/call execution; an independent Terra review then attacked the integrated rewrite. Primary-source checks against OpenZeppelin Governor, Ownable, AccessControl, and AccessManager and the pinned OSx DAO, permissions, execution, auth, and plugin pages supported the correction. The prior draft had confused a governor with its voting method, denied governors could be addressable entities, treated timelocks/oracle thresholds/keepers as possible governors, invented a fixed two-boundary runtime-principal → policy → engine path, and used ACL as a synonym for access control.

  [Authorization and execution model](./access-control/authorization-and-execution.md) now starts from how actors' preferences and desires become onchain state transitions: a governor entity resolves proposed actions using a decision method; each EVM call has a concrete caller; and each guarded function accepts or rejects that caller. It gives explicit caller-preserving flows for OpenZeppelin Governor with and without a timelock, Safe as a governor–account combination with multisignature governance baked into the account contract, and the normal Aragon plugin → DAO → target path. The page removes action plans, runtime principals, resources/capabilities, enforcement points, authorizer/policy, policy administrators, execution engines, direct authorization, effective control, and the claim of exactly two authorization boundaries. ACL now means a list-like permission store: OSx `PermissionManager` and OpenZeppelin `AccessManager` are ACL-style; scattered `Ownable` fields are access control but not one ACL. All five former page questions are answered and removed.

  [OSx authorization mapping](./access-control/osx-authorization-paths.md) was rebuilt as concrete call tables and flows, including guarded plugin functions, ROOT-gated permission administration, Safe calls, and `DelegateCall` through GlobalExecutor where action targets see the plugin rather than the DAO. The Terra review caught and corrected residual absolutes in [Account](./accounts/account.md), [Safe vs Aragon multisig](./guides/safe-vs-aragon-multisig.md), [Stages over direct permission grants](./governance/stages-over-direct-permissions.md), and [Target](./governance/target.md); it also caught the Body/Safe contradiction, so [Body](./governance/body.md) now describes who supplies preferences in a given process rather than asserting that a body can never create or act, and its stale confirmation question is removed. [Process](./governance/process.md), [Plugin](./governance/plugin.md), [Action](./governance/action.md), [Permission model](./access-control/scoped-authority.md), and the linked-account pages were reconciled to the same caller-grounded model; permission-signal wording now respects idempotent grants that emit no event. Historical log entries remain unchanged; the active task Progress explicitly marks the rejected interpretation superseded. The one-pager capture remains because its positioning/integration claims still require a separate source-disposition subtask.

  A second top-to-bottom owner review tightened the core page without adding another ontology. Two Terra passes checked the OpenZeppelin account/timelock facts and rebuilt the caller argument around a transaction's call trace; two Sol passes then attacked the integrated text for factual and editorial regressions. The result removes universal governor recordkeeping, scopes the Safe example to its basic owner-threshold path, identifies the TimelockController as the account in `GovernorTimelockControl`, distinguishes guards from their local or shared configuration, and explains intermediate guards through the consequential state changes they can enable. The standalone authorized-intermediary, permission-administration, ACL-correction, and stable-account sections were removed; ACL is now defined where shared configuration is introduced, and the stable-DAO consequence remains only in the OSx mapping.

  A third owner pass reordered the page around its reader prerequisites. Consequential state changes now establish why the model matters before the page defines actions, targets, callers, and `msg.sender`; only then does it distinguish governors from accounts and follow execution through a nested call trace. The detailed TimelockController behavior moved beside its trace, and the authorization section now states the central distinction directly: every guard remains on a target function, while the configuration it consults may be local to that target or shared through an authority or DAO permission store. The call-trace close and authorization close both return to the consequential effects introduced at the top.

  A fourth owner pass added the transaction origin and security motivation that connects those concepts. The page now establishes that ordinary Ethereum transactions begin with an EOA, contract accounts call only after execution enters them, and a multi-transaction governance lifecycle therefore consists of several EOA-rooted traces. It contrasts a plain EOA's signing-key control with the richer thresholds, recovery, delays, and scoped rules a smart contract account can enforce, then introduces the governor as the component deciding which actions that account should take. The execution diagrams now show their EOA roots. The authorization close also makes its two classifications orthogonal: local/shared describes where authorization configuration or logic lives, while ACL describes one representation of permissions; OSx `PermissionManager` is both shared and a direct ACL, whereas OpenZeppelin `AccessManager` is shared and role-based. **Board delta:** owner ask resolved and removed — authority model; task derive-the-executor-acl-auth-model remains `in-progress` and moved to the top of *Ready to run* for its bounded OSx-route validation and one-pager disposition; six page questions resolved — the five authorization-model questions plus Body's absolute-prohibition confirmation; no task, opportunity, or knowledge gap added or retired.

  A fifth owner pass tightened the account-to-governance transition. The account section no longer introduces the defined term governance process early or detours into relaying, account abstraction, recovery, modules, and other mechanisms. It now moves directly from EOA-rooted transactions, through the plain EOA's signing-key decision rule, to governed smart-contract execution and the governor → account → guarded target flow. **Board delta:** none.

  A sixth owner pass rebuilt the authorization close around concrete consequences. The intermediate-function rationale now sits beside the guard definition and explains why an unguarded `DAO.execute` would let any caller direct the DAO's assets and permissions, while an unguarded delegated plugin entry point would expose the delegated action to anyone. The ACL discussion is reduced to one distinction—shared describes a common authorization source, while ACL describes explicit permission entries—and the custom-authority and permission-model detours are gone. **Board delta:** none.

  A seventh owner pass clarified task ownership rather than changing the semantic model. Derive-the-executor-acl-auth-model and its board row now state at the top that the owner's material is complete and that every remaining item is agent-owned; final approval of the draft page remains in the separate drafts inventory. **Board delta:** wording clarified on the existing Ready-to-run row; no state or ordering change.

- **2026-08-04** — Commissioned the missing code-first direction. The owner judged verify-documentation-against-source-code sound but one-directional: it corrects four named pages against the code and, because all three of its load-bearing sections name those same four page-sets, discovery is bounded by pages that already exist. Its "record any additional finding" bullet is a disposal rule, not a search instruction — a session that opens only the files the four traces require and files nothing is fully compliant. That task keeps its scope (widening it would restore the ambiguity its 2026-08-04 reframing removed) and gains one scope-boundary bullet routing discovery elsewhere. New ready task **audit-the-codebase-for-undocumented-business-logic** carries the other direction: carve the app into business-logic categories, classify each against the existing area map, and convert absences into at most six finite tasks. The justification recorded in the entry is that every completeness instrument this base owns is self-referential — `unresolved` sees only promises already made, `orphans` only pages already written, and a knowledge gap only a hole someone already noticed — so nothing asks the code what the product does that no page mentions.

  Three design corrections are baked into the entry rather than left to the running session. The deliverable is a coverage classification against the five existing areas, not a second taxonomy competing with them; it is pinned to `app` `122f1bd1` so exhaustion is decidable and a later audit is a fresh task at a fresh commit; and the artifact deliberately does not become durable — the classification lives in the close-out log and the spawned tasks, because a canonical coverage page would fail the present-tense product-truth contract, a backlog inventory row would be the hand-maintained second copy that section bans, and an `inbox/` capture is reserved for non-regenerable owner material. Four checkable constraints guard the dominant failure mode, which is transcribing the sixteen module and plugin directory names as categories: a name test (no path, module, component, hook, or route identifier in any category name), a one-sentence shape test, a hard no-canonical-page-edit rule, and a production-reachability rule so gated or dark-launched code becomes an owner question rather than a documentation task. The task-count cap cites the thirty-to-twenty-two consolidation, and the entry states outright that a low task count is success.

  Scope was cut against four siblings rather than merged: no page intersection with verify-documentation-against-source-code (the audit writes no canonical page), cross-surface recurring rules stay with the owner-blocked inventory-the-interaction-principles, and wizard and action-builder depth stay reserved for owner material. Two stale forward references were repaired — capture-wizard-screen-walkthroughs and rethink-the-guide-layer both deferred work to the retired ground-the-base-in-the-source-repos slug, and now name verify-documentation-against-source-code and the new audit respectively. Three verified leads seed the pass: `apps/assistant` is a support-chat intake service in the same monorepo that no page in this base mentions and whose product scope is an owner call; [reach out when the abstraction cannot stay honest](./design/reach-out-to-the-team.md)'s handoff-inventory checkbox is code-answerable and therefore a task masquerading as an owner question; and the capital-flow policy, router, and dispatch surface has no page at all.

  Four agent passes ran — a code-first survey of `app`, a documentation coverage map, an adversarial task-design critique, and an adversarial verification of the written entry. The verification caught the pass's own overreach and it was corrected: the draft had claimed the four-step transaction lifecycle, its resume and duplicate-submission guard, control gating, and alert severity as undocumented, when [transaction submission](./design/transaction-submission.md), [control availability](./design/control-availability.md), and [alert severity](./design/alert-severity.md) already own all four — the entry now names them as covered so the running session cannot repeat the error, and the design characterization was narrowed from "one arc" to "its strongest cluster." It also found three of the four guard rules had no Done-when test; the criteria now check the name and sentence tests by inspection, require reachability marks, require that the category set not be the module tree, and require a zero-absence result to be argued rather than silent, replacing an `raw/`-empty criterion that an untouched scratch directory satisfied trivially. [Reach out when the abstraction cannot stay honest](./design/reach-out-to-the-team.md) left the settle-first draft batch, since a queued row now touches it. A third stale slug was repaired in capture-wizard-screen-walkthroughs, which still routed dialog-wizard shape to the retired grounding task rather than to the settled [dialog taxonomy](./design/dialog-taxonomy.md). No code was changed and no canonical page was edited.

  Both entries were then pulled back to nudge altitude on the owner's direction: the product owner sets the intent and the guard rails, not the procedure. The audit lost its glob catalogue, its per-file path enumerations, and its step-by-step method, keeping *Where to look* as the seams worth following — registries and slot definitions, dialog definitions, the reachability gates, the translation file as an independent partition — plus the two traps and the split-repo warning. Verify lost its per-repository depth instructions and its field-by-field slot prescription in favor of naming the four outcomes. Every load-bearing decision survived in both: the commit pin, the six-task cap, the low-count-is-success framing, the implementation-independent naming rule, the product-behavior test, no canonical page edits, and the routing to the four sibling tasks. **Board delta:** task added — audit-the-codebase-for-undocumented-business-logic (`ready`, second in *Ready to run*); verify-documentation-against-source-code gained a scope boundary and stays `ready` and first; two retired-slug references repaired; no owner ask, opportunity, or knowledge gap added or closed.

- **2026-08-04** — Resolved the first Ready-for-input item from the owner's scope answer. Permission grants and revocations are generic smart-contract actions, not individual product features or a human-role catalogue; the Action Builder carries the general path and the Governance Designer composes the guided happy path. The Permission model, Proposal actions, and Governance designer pages now state that boundary, the obsolete Roles promise was removed, and verify-documentation-against-source-code was narrowed from an exhaustive condition inventory to representative verification of the two paths. The backend's indexed-event catalogue, indexer/service topology, credentials, and Tenderly API wiring were classified as implementation details outside this product-semantic base; Platform design principles now preserves only the product-level fact that the backend indexes relevant chain events and the frontend reads the derived state through its API. The veLocker page now points directly to the public `aragon/ve-governance` repository as the authoritative mechanism source and requires no further owner briefing or platform-layer task; its live-deployment question was retired in favor of the page's explicit non-claim because the available sources establish code support but not a production deployment. **Board delta:** owner ask resolved — the three optional source investigations; page question resolved — veLocker production deployment; task narrowed — verify-documentation-against-source-code remains ready; unwritten Roles promise retired; chain-state and ve-governance mechanism gaps closed as out of platform-doc scope; no task or opportunity added or retired.

- **2026-08-04** — Reframed the ambiguous ground-the-base-in-the-source-repos task as verify-documentation-against-source-code. The core work now names the four documentation outputs, the source repositories used for each, the facts the code can and cannot establish, and the exact split between AI execution and product-owner review. The task no longer waits on a broad go-ahead: its repository scope is fixed from its deliverables and its status is `ready`. The permission-management UI, chain-state architecture, and ve-governance protocol work remain optional, separately commissionable investigations rather than hidden extensions of the core task. No source-code analysis was run in this editorial pass. **Board delta:** task renamed and moved from blocked to ready — verify-documentation-against-source-code; owner ask narrowed — three independent yes/no decisions on optional follow-up investigations; no task, opportunity, or knowledge gap added or retired.

- **2026-08-04** — Completed mine-platform-infrastructure-and-general-ui by reconciling the owner's final two ordered release-note categories against the current app (`122f1bd1`), app-backend (`107103b`), and pinned protocol documentation (`2d800ee`). [Assets](./treasury/assets.md) retains the verified CoinGecko dependency and now replaces its stale transfer-event/`balanceOf`-only explanation with the current backend reconciliation of native and token balances, the shared per-network historical crawl and polling loop, and network-specific provider selection. It records the configured Etherscan, Routescan, zkSync, and Blockscout explorer paths and links their ABI consequence to [Proposal actions](./governance/action-builder.md) without claiming that these facts close the broader live-read, indexing, consistency, and freshness architecture gap. The page also adds the address-scoped [Octav](https://pro.octav.fi/) analytics drill-down, keeps Aragon's own asset coverage deliberately bounded, and distinguishes imperfect automated spam-token heuristics from the reported-address manual override already owned by [App CMS](./app-cms.md), with team handoff for suspected misclassification.

  New draft [Contract upgrades](./accounts/contract-upgrades.md) gives the settings capability one product-level home: when enabled for an eligible account and an update is available, the app can package OSx DAO and compatible in-place plugin upgrades into a proposal through a selected governance process. It links the pinned DAO and plugin mechanisms rather than restating them, and preserves the owner's deliberate opt-in posture: executable-code changes carry risk, many releases only add features an account does not need, and the app does not push every update as mandatory maintenance. [Account creation](./accounts/account-creation.md) resolves its supported-networks question by naming the live Create DAO picker and `app` configuration as authoritative instead of freezing a list. It also records the general wallet-connection Terms/Privacy acceptance gate while explicitly separating that gate from Action Builder WalletConnect.

  [Transaction submission](./design/transaction-submission.md) now records that the browser session remembers a broadcast transaction, confirmation exposes a chain-specific explorer link, and a long-unconfirmed retry is guarded by an explicit duplicate-risk confirmation. Current source corrected the older release-note wording: the alert itself does not instruct the user to open the explorer, so the page describes the explorer affordance and retry gate separately; duplicate proposals are one concrete consequence, not the only affected flow. [Alert severity](./design/alert-severity.md) carries the required unresolved page-failure/missing-data question without manufacturing a product-wide error policy from the app's partial error components.

  Every remaining source item received a deliberate non-addition: the old blue top loading bar and Vercel rationale were dropped as minor release-era implementation detail with no current source use or pattern home; release 1.20 contributed no durable claim; Octav's relationship-building rationale and release-number chronology stayed historical rather than canonical product meaning; the garbled static network examples were replaced by the live-source rule; and the speculative “code is law” commentary did not become a new absolute principle because [Platform design principles](./principles.md) and App CMS already hold its bounded disposition. Three Terra source/graph tracks and three post-edit adversarial reviews found and corrected the stale asset pipeline, native-asset omission, indexer/provider overstatement, upgrade eligibility boundary, and protocol provenance. Focused app verification passed 7 suites / 97 tests; 177 directly selected app-backend pricing, spam-heuristic, provider, and explorer tests passed, while its broad Windows unit wrapper discovered zero files and is not counted. Both source worktrees and the protocol submodule were preserved, including the app's pre-existing `.gitignore` edit. No new task, owner ask, product opportunity, or structural knowledge gap was warranted; the existing chain-state architecture gap remains open. Final inventory after retirement: 161 indexed entries; 72 drafts; 7 task entries (0 ready, 1 in progress, 6 blocked); 45 questions across 29 platform pages (62 checkboxes including 17 upstream steps); only the intentional Roles promise unresolved; no orphans. **Board delta:** task retired — mine-platform-infrastructure-and-general-ui; draft added — Contract upgrades; account-creation network question resolved; alert-severity page-failure/missing-data question added; no task, owner ask, opportunity, or knowledge gap added or closed.

- **2026-08-04** — Completed ground-dialog-taxonomy-in-gov-ui-kit against gov-ui-kit `9c106d8` and app `122f1bd1`. [Dialog taxonomy](./design/dialog-taxonomy.md) now maps every public `dialogs/` export: the `Dialog` compound family is the general-dialog container; the same family with `Dialog.Footer variant="wizard"` supplies the dialog-wizard control row; and `DialogAlert` is the alert-dialog family. The public barrel exposes only those two families, so all three product kinds are covered and no component family remains outside the taxonomy. Full-screen wizards, transaction submission, internal accessibility/context/animation helpers, and the separate non-dialog `alerts/` family remain outside this container inventory.

  [Wizard](./design/wizard.md) now records that the wizard footer only places Back/Close on the left and Next/Submit on the right. The consuming app still owns the step graph, form state and validation, navigation, exit behavior, analytics, and dialog transitions; using the footer variant alone does not make a procedural dialog a wizard. [Alert severity](./design/alert-severity.md) now records that `DialogAlert.Root`'s warning and critical variants drive the matching header icon/title and consequential-action treatment, with Cancel placed before the action. The toolkit's additional info/success variants are explicitly not new product severity categories, and the existing question about non-dialog severity surfaces remains open.

  Three high-reasoning Terra tracks covered the exhaustive UI-kit inventory, app orchestration boundary, and adversarial graph/requirements audit, followed by three page-level adversarial reviews. Focused verification passed 11 gov-ui-kit suites / 24 tests and 2 app suites / 8 tests; both source worktrees were preserved, including the app's pre-existing `.gitignore` edit. No new task, owner ask, product opportunity, structural knowledge gap, index change, repository entry, or draft was warranted. **Board delta:** task retired — ground-dialog-taxonomy-in-gov-ui-kit; the three grounded pages remain drafts for owner review; no other queue or inventory state changed.

- **2026-08-04** — Completed ground-the-voting-terminal-spec by grounding [Voting Terminal](./design/voting-terminal.md) in gov-ui-kit `9c106d8` and app `122f1bd1`. The pattern now covers the direct proposal card and staged accordion, single- and multi-body navigation, the Breakdown/Votes/Settings state rules, and every `SimpleGovernance`, `SingleStage`, `MultiStage`, and `MultiBody` reference-story shape. It separates overall proposal status from stage/sub-proposal status, records the min/max advance-window presentation and disabled-until-minimum action, and distinguishes the terminal from the best-effort advancement requested by registered-body reports.

  Approval and veto are documented per body, including independent requirements and the mixed Token Holder approval plus Security Council veto acceptance case. The app-specific bodyless timelock card now has its pending, active, and ended states without implying a vote or automatic advance. Safe rendering distinguishes sole-body from multi-body navigation, design-system-optional Votes hiding from the current staged app's external-body behavior, and Safe-as-one-object from indexed Aragon Multisig participation. The implementation scope records SPP's staged replacement, the shared non-staged fallback used by Token Voting, Multisig, and Lock to Vote, Admin's automatic-execution empty state, and Gauge Voter's exclusion as an epoch-allocation control. The page links the canonical Figma component while keeping prop-level API detail out of the pattern.

  Three high-reasoning Terra tracks covered UI-kit inventory, app assembly, and graph/semantic boundaries, followed by three adversarial page reviews. Corrections landed for action ownership, Safe story differences, status ownership, advance-display semantics, registered-body best-effort advancement, final-stage controls, exact role labels, and the UI-kit/app timelock boundary. Focused verification passed 17 gov-ui-kit suites / 75 tests and 3 app suites / 87 tests; both source worktrees were preserved, including the app's pre-existing `.gitignore` edit. No new task, owner ask, product opportunity, structural knowledge gap, repository entry, or canonical-page ripple was warranted. **Board delta:** task retired — ground-the-voting-terminal-spec; Voting Terminal remains a draft for owner review; no other queue or inventory state changed.

- **2026-08-04** — Completed mine-admin-and-dao-presentation by reconciling the owner's ordered **DAO creation, admin experience & DAO-level presentation** release-note category against the current app (`122f1bd1`) and the pinned protocol documentation. [Admin flow](./accounts/admin-flow.md) now carries the requested two-sided catalogue. Admin is a known, registered plugin with dedicated slots, but it has no setup-picker definition: the app factory installs it as the sole initial plugin with the connected wallet as first admin, and the live-account self-service picker cannot reinstall it. Admin-member proposal creation remains permission-gated, while the submitted `createProposal` executes atomically and the app's Admin status function always returns `EXECUTED`. The dedicated surface is now precise: a per-account onboarding dashboard, a settings-route banner with admin/Add-governance and fallback/View-admins branches, an installed-plugin settings panel, a guarded member-management dialog that grants or revokes the execute-proposal permission, and an Admin-specific removal entry point.

  The catalogue separates Admin's own factory-time installation from installing replacement governance through Admin: the latter uses the normal permissionless `prepareInstallation` followed by the temporary-ROOT/apply/revoke proposal actions, which Admin executes immediately. Admin's own removal likewise uses the shared two-step prepare/apply-uninstallation machinery, but only after another full-execute process exists; the generic dialog then selects the process that publishes the removal proposal, so the existence check is not itself a guarantee that the other process is used. The app adds no Admin-specific cleanup or warning for the former admins' leftover `EXECUTE_PROPOSAL_PERMISSION_ID` grants, leaving the inert dangling state documented upstream. [Admin management](./accounts/admin-management.md), [Install admin plugin by default](./accounts/admin-plugin-by-default.md), and [Plugin](./governance/plugin.md) now agree on the settings-only banner, the stricter Admin removal gate, the ordinary-process warning boundary, and the network-dependent three setup families.

  New draft DAO presentation gives metadata-backed display and the DAO's ENS identity one atomic home. The app creation payload pins name, description, avatar, and links; surfaces using the shared display-name helper fall back from an unusable metadata name to the truncated DAO address. On Ethereum mainnet the creation wizard can pass an optional label to factory registration for a `<label>.dao.eth` address record; the app prefers that name in DAO URLs, and it is a human-readable transfer destination for the DAO address. This DAO-address subname is explicitly separate from a participant wallet's [`name.aragon.eth`](./accounts/claiming-an-aragon-eth-name.md). [Plugin slots](./design/plugin-slots.md) now has the requested Token-Voting-as-a-body worked example while the full slot contract remains with the blocked grounding pass; [DAO slots](./design/dao-slots.md)'s existing custom-header example was verified and left unduplicated. The exact creation-metadata checkbox is resolved; the broader metadata-pattern owner ask remains intact. No new task, owner ask, product opportunity, or structural knowledge gap was warranted.

  Three high-reasoning Terra tracks covered the authorized source, app catalogue, and graph/requirements boundary, followed by three adversarial review rounds; corrections landed for ENS registrar ownership, picker availability by network, Admin-removal proposal selection, source-link ranges, and inventory drift. Focused app verification passed six suites / 89 tests, and the app worktree remained unchanged apart from its pre-existing `.gitignore` edit. Final inventory after retirement: 163 indexed entries; 71 drafts; 10 task entries (3 ready, 1 in progress, 6 blocked); 45 questions across 29 platform pages (62 checkboxes including 17 upstream steps); no orphans. **Board delta:** task retired — mine-admin-and-dao-presentation; draft added — DAO presentation; account-creation and Admin-settings questions resolved; no task, owner ask, opportunity, or knowledge gap added.

- **2026-08-04** — Completed mine-capital-distributor-release-notes from the owner's ordered **Capital distributor & Merkle-based reward distribution** release-note category and the public `aragon/osx-capital-distributor` contracts at `development@8577c133`. New draft [Capital Distributor](./governance/capital-distributor.md) defines the installed-plugin capability: DAO-held tokens become claimable through campaigns; the current Aragon flow prepares Merkle trees, roots, and per-claim proofs off-chain; the Merkle strategy verifies the claimant's address and amount onchain; and the claimant can redirect their own payout without transferring eligibility. The page covers the eligible-rewards claim wizard, signed claim transaction, Aragon UI's OFAC-list gate and optional country geofencing, the public Merkle-generation API for external claiming UIs, and the Basic action views for campaign creation, pause, resume, and end. It keeps two boundaries explicit: the source does not define the compliance policy or make those UI controls a guarantee, and an observed client-specific Action Builder path is not a general self-service campaign-authoring promise.

  Gauge-informed distribution is bounded to a separately arranged handoff from [Gauge voting](./governance/gauge-voting.md) outcomes into off-chain campaign preparation; Gauge Voter does not create, fund, or pay a campaign automatically. Boundless, Katana, and CryptEx remain named usage context, while project-specific calculation rules stay out of the canonical model. The delivery posture links to [Partially supported plugins](./partially-supported-plugins.md) rather than restating that model. [Proposal actions](./governance/action-builder.md), the governance map, draft and unwritten inventories, and [Source repositories](./repositories.md) were reconciled; the official `aragon/osx-capital-distributor` repository and inspected commit are now recorded. Three high-reasoning Terra research tracks and two adversarial re-reviews corrected generic campaign-authoring scope, Merkle-root ownership, semantic linking, and draft-count drift. No new task, opportunity, owner ask, or structural knowledge gap was warranted. **Board delta:** task retired — mine-capital-distributor-release-notes; draft added — Capital Distributor; repository added — `aragon/osx-capital-distributor`; no owner ask, gap, opportunity, or follow-up task added.

- **2026-08-04** — Completed mine-gauge-voting-release-notes by reconciling releases 1.15, 1.16, the editorially normalized 1.21, 1.25, and 1.27 in the ordered **Gauge voting** category against the current app (`122f1bd1`), app-backend (`107103b`), and public `aragon/ve-governance` contracts (`develop@408ad144`). New draft [Gauge voting](./governance/gauge-voting.md) defines the Gauge Voter as a non-proposal, generally signaling governance capability: eligible holders allocate voting power by relative weight across active gauge addresses during an epoch voting window, while the plugin itself neither creates `IProposal` objects nor executes DAO actions. The UI normalizes nonzero weights into two-decimal percentages totaling 100%, and the contract replaces the voter's prior allocation in its current write state. Epoch behavior is configuration-dependent rather than the release note's unresolved “roll over or not” shorthand: update-hook deployments use persistent epoch-zero allocation and can recalculate downward as power falls, while non-hook deployments use the current epoch and an epoch-start voting-power snapshot.

  Current code corrected the release-note administration vocabulary. The literal `GAUGE_ADMIN_ROLE` exists, and the app supplies purpose-built Action Builder create/detail views for create, deactivate, activate, and update-metadata; there is no current removal function, so a created gauge stays enumerable and deactivation is the current retirement path. The page also owns the dedicated `/gauges` navbar page, first-instance-only limitation, gauge-details dialog, Gauge-specific epoch-allocation terminal, App CMS redirect, and its participation aside. That aside uses a Gauge-specific Lock form and shared Delegate form. A third-party escrow has no native Lock or connection-time Lock/Wrap route in the current app; the remaining ungated Delegate behavior is preserved as one owner question because code proves the exception but not its intended policy. Reward integration is bounded to the demonstrated pro-rata calculation and configured Capital Distributor campaign-input export, not automatic campaign creation or payment.

  [Proposal](./governance/proposal.md) now correctly limits `IProposal` to proposal-producing governance plugins. [Proposal actions](./governance/action-builder.md), [Token panel](./governance/token-panel.md), [veLocker](./governance/velocker.md), Governance index, [Source repositories](./repositories.md), the Capital Distributor task, and the Voting Terminal task now link or inherit the durable boundary; Gauge's sticky terminal is explicitly excluded from the proposal-page pattern. Run as three x-high Terra source, implementation, and graph tracks followed by three adversarial reviews; substantive corrections were applied for current-vs-release management actions, persistent-hook increases, largest-remainder scope, Gauge-specific Lock reuse, third-party support wording, and the demo-only Capital handoff. Focused verification passed three frontend suites / 21 tests and ten backend reward-distribution tests; no Forge run was available, so contract behavior was verified directly against the canonical source and specification. Final inventory after retirement: 163 indexed entries; 69 drafts; 12 task entries (5 ready, 1 in progress, 6 blocked); 47 questions across 30 platform pages (64 checkboxes including 17 upstream steps); two product opportunities; six intentionally unresolved edges covering Roles and Capital Distributor; no orphans. **Board delta:** task retired — mine-gauge-voting-release-notes; draft added — Gauge voting; owner ask added — third-party Gauge Delegate gating; task dependencies refined — mine-capital-distributor-release-notes and ground-the-voting-terminal-spec; no task, opportunity, structural knowledge gap, repository row, or tag added.

- **2026-08-04** — Completed mine-velocker by reconciling the owner's veLocker marketing brief with releases 1.7.0, 1.11, 1.14, and the dictated 1.20.22 (likely 1.22) in the ordered **veLockers (vote escrow lockers)** release-note category. New draft [veLocker](./governance/velocker.md) owns the app-supported vote-escrow participation semantics without treating veLocker as a separately installed app plugin: a recognized escrow adapter behind Token Voting gives the Members-page [Token panel](./governance/token-panel.md) its Lock path, while `delegate(address)` capability gates Delegate and proposal voting remains Token Voting's surface. The page covers amount entry and approval, the time-varying power preview and flat-slope hiding rule, independently displayed locks with live per-lock power, configured minimum-lock and cooldown boundaries, unlock/withdraw states, the 1.14 early-exit fee posture, and timestamp-clock compatibility. It also gives dynamic delegation its own bounded definition, distinguishes escrow-backed Token Voting from [Lock to Vote](./guides/choose-token-voting-power-mechanism.md), and keeps gauges separate.

  Current app (`122f1bd1`) and app-backend (`107103b`) source verified the recognition, indexing, panel gating, lock/list/unlock/withdraw, fee-display, and delegation paths. The code proves current product support but not a live production deployment, so the page carries that one remaining deployment-status question. The public [`aragon/ve-governance`](https://github.com/aragon/ve-governance) repository was confirmed as the canonical contract source at `develop@408ad144`; its adapter specification resolves the source's dynamic-delegation behavior and timestamp clock, while its README confirms the public contracts and deployment factory. The owner's OSx v1.4-or-later statement remains explicitly source-attributed rather than presented as a code-derived compatibility check. The repository is now in [Source repositories](./repositories.md), and the absent upstream mechanism layer—multiplier curve, adapter/delegation, exit/cooldown, and fee curve—is a third opt-in extension of the board's source-repo grounding pass. If green-lit, it spins out as protocol-layer work rather than expanding the platform page or the existing task. No separate ready task or product opportunity is warranted now.

  Marketing-only launch timing, audit timing, customer and partner name-drops, personas, use cases, incentive/anti-dump positioning, superlatives, CTA, and author commentary were dropped. The mined machine-local brief was retired in full because it contained no other source material; the ordered release-note capture remains for sibling categories. Governance index, Body, draft inventory, repository inventory, knowledge-gap routing, and unwritten inventory were reconciled. Run as three x-high Terra source, implementation, and graph tracks followed by three adversarial re-reviews and a final signoff pass; every substantive finding was applied. Focused frontend verification passed four suites / 49 tests; the backend's broad Windows unit runner discovered zero tests and is not counted as passing evidence. Final inventory after retirement: 163 indexed entries; 68 drafts; 13 task entries (6 ready, 1 in progress, 6 blocked); 46 questions across 29 platform pages (63 checkboxes including 17 upstream steps); two product opportunities; nine intentionally unresolved edges covering Roles, Gauge voting, and Capital Distributor; no orphans. The composite wiki gate passed, and the protocol-doc submodule remained clean at `2d800ee`. **Board delta:** task retired — mine-velocker; draft added — veLocker; knowledge gap added — ve-governance mechanisms as the third opt-in source-grounding extension; repository added — `aragon/ve-governance`; owner question added — live production deployments; no separate task or opportunity added.

- **2026-08-04** — Completed mine-premium-services-model-release-notes by reconciling the owner's **Premium / partially-supported services model** release-note category (1.7.0, 1.8.0, and 1.15) against the current app (`122f1bd1`) and app-backend (`107103b`). New draft [Partially supported plugins](./partially-supported-plugins.md) gives the repeated client-delivery posture one cross-cutting home: the app supports the installed feature's user experience, but there is no self-service deployment experience; Aragon can deploy it while working directly with a client, and the stated call to action is to reach out to the team. The source names supported, partially supported, and unsupported categories but defines only the middle one, so the page preserves both the program-name question (“partially supported plugins,” “premium features,” or “premium Aragon services”) and the undefined outer-tier criteria instead of inventing a full taxonomy. Commercial claims remain instance-specific: veLocker is generally charged for, Capital Distributor is not available for free for anyone to deploy, and Gauge carries no fee claim.

  [Plugin](./governance/plugin.md) now owns the technical axis and corrects its former recognition/deployment conflation. The backend resolves proxy implementations and classifies bytecode from required function selectors; the frontend routes the resulting interface type into registered modules and slots. Compatible alternate implementations can therefore reuse a surface without a bespoke frontend path, subject to expected behavior and data, single-instance limitations, and repository-subdomain-specific updates. This is separate from delivery posture, and the backend's `isSupported` field is explicitly recorded as a technical lifecycle flag rather than a commercial-support category. Current code also narrows the deployment evidence precisely: Gauge and Capital Distributor have installed-instance surfaces but no setup data or non-zero current-network repository address for the governance-designer picker; that proves absence from the picker, not every possible technical deployment route. veLocker is recorded as a related vote-escrow component rather than a standalone frontend plugin interface.

  The veLocker, Gauge voting, and Capital Distributor tasks now link the durable concept and are constrained to state only their instance-specific posture; the Capital Distributor task's unsupported “revenue-generating” label was removed. The root index and draft inventory expose the new page, while the unwritten inventory now includes Capital Distributor beside the already promised veLocker and Gauge pages. Run as three x-high Terra source, implementation, and graph tracks followed by adversarial re-reviews; every finding was applied, and focused app verification passed two suites / 30 tests. No new task, product opportunity, structural knowledge gap, page beyond the requested concept, or tag vocabulary was warranted. Final inventory after retirement: 163 indexed entries; 67 drafts; 14 task entries (7 ready, 1 in progress, 6 blocked); 45 questions across 28 platform pages (62 checkboxes including 17 upstream steps); two product opportunities; 12 intentionally unresolved graph edges covering Roles, veLocker, Gauge voting, and Capital Distributor; no orphans. **Board delta:** task retired — mine-premium-services-model-release-notes; owner asks added — approved product name and the supported/unsupported criteria; tasks refined — mine-velocker, mine-gauge-voting-release-notes, and mine-capital-distributor-release-notes; no task, opportunity, or knowledge gap added.

- **2026-08-04** — Completed mine-explore-page-release-notes by reconciling the ordered **Explore page & DAO discovery** release-note category (1.0.0, 1.2.1, and 1.24) against the current app (`122f1bd1`) and app-backend (`107103b`). [Explore page](./accounts/explore-page.md) now describes the live surface as hero, App-CMS-fed Featured carousel, three Getting-started routes, and one account list with **All DAOs** and **Member** modes. **All DAOs** is the active, non-hidden, mainnet-only view in descending total-assets-USD order; its visible search promise is name/address/ENS while the backend also searches implementation and creator addresses, description, subdomain, and transaction hash. The page explicitly records that there is no interactive sort or further filter. **Member** is connected-wallet-only, spans all configured supported networks, accepts any one qualifying installed and supported membership signal across token voting, delegated ve-lock, lock-to-vote, multisig, or admin, sorts newest first, and returns to All on disconnect. The featured list remains curated through [App CMS](./app-cms.md), with external destinations supported, and the current Getting-started cards route to team assistance, self-serve account creation, and the developer portal.

  Current code corrected the source category's most consequential placement error: release 1.24's **onboarding dashboard** is not an Explore redesign. It is an individual account's [Admin flow](./accounts/admin-flow.md#onboarding-dashboard) state when the admin plugin exists and no other visible process plugin does. The admin variant—also gated by the effective `governanceDesigner` feature flag—offers **Get in touch**, a **Set up governance yourself** card, **Add governance**, and **View docs**; non-admin and disconnected visitors see the being-set-up state and **View admins**. This resolves the Admin-flow page's standing transition-guidance question as well as both Explore questions. The later admin-and-DAO-presentation task was narrowed to preserve this settled boundary while it completes the broader admin catalogue and remaining presentation facts.

  Run as three x-high Terra source, implementation, and graph tracks followed by three adversarial re-reviews. Every finding was applied: All-only testnet suppression, full server search scope, exact CTA labels, effective-feature-flag semantics, source attribution for human curation, the Explore/Admin ownership split, and removal of the retiring task slug from surviving work. Focused verification passed five frontend suites / 27 tests. No new owner question, task, opportunity, structural knowledge gap, page, or tag vocabulary was warranted. Final inventory after retirement: 163 indexed entries; 66 drafts; 15 task entries (8 ready, 1 in progress, 6 blocked); 43 questions across 27 platform pages (60 checkboxes including 17 upstream steps); two product opportunities; eight intentionally unresolved graph edges covering Roles, Gauge voting, and veLocker; no orphans. **Board delta:** task retired — mine-explore-page-release-notes; owner asks resolved — Explore search/sort/filter, featured-list operation, and Admin-flow transition guidance; task refined — mine-admin-and-dao-presentation; no task, opportunity, or knowledge gap added.

- **2026-08-04** — Completed mine-token-panel-onboarding-release-notes as a claim-by-claim reconciliation of the ordered **Token panel, delegation, address input & onboarding nudges** release-note category against the current app (`122f1bd1`), app-backend (`107103b`), installed gov-ui-kit (`2.9.0`), and the pinned protocol documentation (`2d800ee`). New draft [Token panel](./governance/token-panel.md) owns the actionable token-participation surface in the selected body's Members-page aside. It records the indexed-setting-driven Wrap, voting-escrow Lock, and Delegate variants; the allowance-dependent approval step; panel-only unwrapping; the selector-detected `hasDelegate` boundary; and the absence of the panel when no control applies. It links [Lock to Vote vs Token Voting](./guides/choose-token-voting-power-mechanism.md) and the upstream governance-token/voting-power pages rather than redefining wrapper, snapshot, or delegation mechanics, and intentionally forward-links the future veLocker page for voting-escrow position semantics.

  Current code resolved the source's uncertain placements and regressions. The Members page mounts the panel through the token plugin's member-panel slot; onboarding dialogs reuse its Wrap and Delegate forms; the Gauge page separately assembles the shared Lock and Delegate forms rather than mounting the panel component. The Gauge aside's third-party-escrow and ungated-Delegate exceptions stay bounded here and were carried into the Gauge task for full treatment. Across the ordinary member surfaces, the backend's `delegate(address)` capability flag consistently gates the Delegate tab, connection watcher, member-list card and delegate pin, and member-profile delegation section, resolving the source's untested profile concern without a new owner question. Connection nudges fire only on manual wallet connection: positive governance balance plus a zero/no-result delegate can open delegation; zero votes plus positive underlying balance can open supported escrow-lock or wrapper onboarding; after a successful wrap or supported lock, delegation follows only on a successful zero-address read. The Members list reuses those checks with Delegate → Lock → Wrap card priority, while its API-default voting-power-descending order is preceded by a positive-power connected wallet and then a distinct nonzero delegate.

  New draft [Address input](./design/address-input.md) owns the reusable validation and identifier controls. Checksum enforcement defaults on: lowercase, uppercase, and valid mixed-case addresses normalize to EIP-55; invalid mixed case is critical and unaccepted. Mainnet ENS resolution and reverse lookup remain distinct from the consumer-supplied chain's explorer link. The page also records the exact loose-address edge: invalid mixed-case text may still expose explorer/copy controls even though the form rejects it, and Copy preserves the current field text (so an ENS value must be switched to address view when the address itself is wanted). The release note's broad “copy throughout the UI” shorthand was narrowed to the shared clipboard affordance on verified address and identifier displays.

  [Aragon Profiles](./accounts/aragon-profiles.md) now states its participant-summary boundary, while [Body](./governance/body.md) and the governance/design maps expose the new capability and pattern. The veLocker task now treats the panel as its shared interaction dependency; the Gauge task links the durable page, owns the separate assembly and its three verified caveats, and no longer links directly to other tasks. Run as three x-high Terra extraction, implementation, and graph tracks followed by three adversarial re-reviews; focused app verification passed four suites / 26 tests. Review corrections distinguished manual connection from auto-reconnect, panel Wrap from onboarding Wrap, Lock's escrow-address prerequisite, the Lock information step, delegate-gated versus independent pinning, Gauge and post-action exceptions, fixed versus pinned member ordering, and accepted input from loose explorer/copy eligibility. No new owner question, task, opportunity, structural knowledge gap, or tag was warranted. Final inventory: 66 drafts; 46 questions across 29 platform pages (63 checkboxes including 17 upstream steps); two product opportunities; eight intentionally unresolved graph edges covering Roles, Gauge voting, and the new veLocker promise; no orphans. **Board delta:** task retired — mine-token-panel-onboarding-release-notes; drafts added — Token panel and Address input; tasks refined — mine-velocker and mine-gauge-voting-release-notes; unwritten promise added — veLocker; no owner ask, task, opportunity, or knowledge gap added.

- **2026-08-04** — Completed mine-aragon-profiles-brief as a claim-by-claim reconciliation of the owner's pre-launch Profiles marketing brief and the ordered **Member identity: Aragon Names, ENS profiles & delegate statements** release-note category against the current app (`122f1bd1`), app-backend (`107103b`), and pinned protocol documentation (`2d800ee`). The narrow [Aragon Names](./accounts/aragon-names.md) seed is now the overview of four separately owned drafts: [ENS as the profile layer](./accounts/ens-as-the-profile-layer.md) records the decision to keep ENS authoritative rather than create a proprietary profile database; [Claiming an Aragon Name](./accounts/claiming-an-aragon-eth-name.md) owns eligibility, mainnet transactions, constraints, rename, and release; [Aragon Profiles](./accounts/aragon-profiles.md) owns resolution, display, editing, and member-profile context; and [Delegate profile record](./governance/delegate-profile-record.md) owns the network-and-token ENS key, IPFS pointer, and current editor document shape. The decision page explicitly corrects the release note's “everything on blockchain” shorthand: ENS stores the records and pointer on mainnet, while statement JSON and app-pinned avatar files live on IPFS; backend retrieval, caching, indexing, and pinning are delivery infrastructure rather than a competing profile authority.

  **Every Done/ETA claim received a current-source disposition.** Mainnet ENS name, avatar, and text-record reads are centralized in the app's ENS module and reused across identity surfaces. The member page displays the primary name, avatar, description, and links, and can additionally show delegation stats, a token-specific statement, and an optional EFP follower/following card when their respective conditions are met. The feature-flagged profile dialogs edit the supported ENS fields with signed mainnet resolver writes and pin a newly uploaded avatar first. Profile onboarding while viewing a DAO and the user menu offer creation to a wallet without a primary ENS; the claim flow reads `isRegistered` and `memberSubdomain`, calls the fixed-address Member Registry's `register`, then calls the mainnet reverse registrar to set the new primary name. Both requests send zero native value, while the wallet still pays gas. The current frontend is therefore wired to a mainnet registry deployment and a self-service claim call, but local source does not establish that deployment's audit status, current `aragon.eth` claim authority, or eviction/upgrade permission holders; that uncertainty is preserved on the claiming page and promoted on the board instead of being asserted.

  **The brief's stale rename/release status was resolved in favor of current code, not either prose source.** Rename is a live two-transaction flow: the app enumerates indexed text records, reads the resolver's live address and contenthash, supplies that supported record tuple to `MemberRegistry.move`, and then sets the new name as primary. The page deliberately limits the persistence promise to those record types rather than “every ENS record.” Release is also live for a primary `.aragon.eth` name: the confirmation flow calls `MemberRegistry.release` on mainnet, and the upstream clean-handover mechanism clears the resolver state before the label is reusable. Because both app flows exist, no rename or release opportunity was created; the task's later unconditional opportunity wording was treated as superseded by its own evidence-first conflict rule.

  Delegate statements are also implemented end to end. The app builds `<EIP-3770-shortname>.<lowercase-token-address>.delegate` (Sepolia uses `test`), writes an `ipfs://` CID to the primary ENS resolver on mainnet, and its editor pins version-1 Markdown JSON with `version`, `type`, `format`, and `content`. The backend reader accepts nonempty string content and normalizes it rather than enforcing the entire writer schema; the member page renders that content and offers create/edit controls to the connected member address. Whether ENS endorses the convention remains the second promoted owner question. Release-only facts also landed: the delegation stats card's voting power, token balance, and received-delegation count; conditional EFP API context; and CMS-selected featured addresses replacing the onboarded dashboard's ordinary member section and becoming the first/default Members tab when a Token Voting body matches. [App CMS](./app-cms.md) remains the owner of curation, which does not grant voting power.

  The accounts index now distinguishes governed accounts from participant identity; the governance and root indexes expose the new cluster; later token-panel and DAO-presentation tasks now link the durable profile/claiming pages instead of this retired task. The mined first section was removed from the machine-local `raw/marketing_briefs.md` without touching the following veLocker brief. Run as three x-high Terra source, implementation, and graph tracks followed by three adversarial re-reviews. Review corrections narrowed the avatar fallback to the interface verified locally, distinguished the editor's write schema from the backend's looser read validation, qualified one-name and zero-value behavior to what the inspected UI and ABI prove, preserved the token-panel interaction boundary, and added the precise featured-delegate conditions. No new task, product opportunity, or structural gap was warranted. Final inventory: 64 drafts; 46 questions across 29 platform pages (63 checkboxes including 17 upstream steps); two product opportunities; six intentionally unresolved links; no orphans. **Board delta:** task retired — mine-aragon-profiles-brief; drafts added — ENS as the profile layer, Claiming an Aragon Name, Aragon Profiles, and Delegate profile record; owner asks added — registry deployment/authority confirmation and ENS endorsement; tasks refined — mine-token-panel-onboarding-release-notes and mine-admin-and-dao-presentation; no task, opportunity, or gap added.

- **2026-08-04** — Completed mine-token-governance-setup as one reconciliation of the owner's LockToVote marketing brief and the ordered release-note categories for Lock-to-Vote and existing-token import/wrapping against the current app (`122f1bd1`), pinned protocol documentation (`2d800ee`), and the developer portal's [Importing Existing Tokens](https://docs.aragon.org/token-voting/1.x/importing-existent-tokens.html) guide. New draft decision [Lock to Vote vs Token Voting](./guides/choose-token-voting-power-mechanism.md) owns the three setup branches: use an existing `IVotes` / `ERC20Votes` token directly with Token Voting; let token-voting setup deploy a governance wrapper for a plain ERC-20 when that holder experience is acceptable; or choose Lock to Vote when the token is normally deployed elsewhere and holders participate reactively. The page links the upstream snapshot, wrapper, and locked-power trade-offs instead of duplicating their mechanics, qualifies Lock-to-Vote's broad ERC-20 acceptance by the upstream caveats, and states the holder-specific snapshot constraint: wrapping must precede the particular proposal whose voting power should include it.

  [Governance designer](./governance/governance-designer.md) now carries both configuration surfaces. The Token Voting import flow checks the ERC-20 interface and the current/historical voting-power functions `getVotes`, `getPastVotes`, and `getPastTotalSupply`; a compatible token is used directly and the setup takes the wrapper branch on failure. Lock to Vote asks for the token address, support threshold, and minimum participation, plus proposal duration for a standalone process; a staged body takes its voting period from its stage. App verification resolved and retired one stale task question: the setup already exposes Vote Replacement under the **Vote change** label, defaulting to Standard when disabled. Publish continues to point to the existing prepare-then-apply installation sequence rather than being described as a single deployment transaction.

  This pass did not promote the source's provisional rollout language; the current availability rule is the product-owner answer recorded on 2026-08-05. The source's launch timing, Morpho partnership and announcement coordination, personas, categorical liquidity claims, and protocol-level lock/unlock narrative were not promoted. The mined LockToVote section was retired from the machine-local `raw/marketing_briefs.md` without touching the following veLocker section; the ordered release-note capture remains for sibling tasks.

  The governance index and draft inventory expose the decision. The Token Panel and veLocker tasks now link the durable decision instead of the retiring task slug; the guardian-DAO guide remains owned by rethink-the-guide-layer. Run as three Terra extraction, implementation, and graph tracks followed by three adversarial re-reviews. Review corrections removed incidental new-token/warning/delegation claims from the setup description, named the app's actual three-function capability check, and preserved the code-resolved **Vote change** fact with explicit provenance. No new task, product opportunity, or structural gap was warranted. Draft inventory is 60; page questions reconcile to 47 across 28 platform pages (64 including 17 upstream checklist items); unresolved promises remain Roles and the intentionally forward-linked Gauge voting page. **Board delta:** task retired — mine-token-governance-setup; draft added — lock-to-vote-vs-token-voting; temporary availability questions were later resolved by the 2026-08-05 owner review; tasks refined — mine-token-panel-onboarding-release-notes and mine-velocker; no task, opportunity, or gap added or resolved.

- **2026-08-04** — Completed mine-permissions-and-access-control as one reconciliation of the owner's Granular Access Control marketing brief and release-notes walkthrough against the current app (`122f1bd1`), app-backend (`107103b`), and pinned protocol documentation (`2d800ee`). [Permission model](./access-control/scoped-authority.md) now describes per-organization separation of powers, independently verifiable on-chain authorization, and the boundary-safe form of externalizing governance assignments from protocol contracts. It also records that an execute-selector-conditioned grant may begin with no allowed selectors and later change its allowlist without replacing the grant. New draft [Gradual permission handover](./access-control/gradual-permission-handover.md) owns the action-by-action adoption pattern: broad core-team multisig authority, then selected actions behind multisig plus token-holder veto, then selected actions proposed and executed directly by token holders, with scope adjusted to each action's risk. The access-control index and permission model link it.

  [Governance designer](./governance/governance-designer.md) now owns the Permissions step and reconciles the two wallet submissions with prepare/apply: preparation deploys or configures the plugin and condition without changing DAO permissions; the second submission creates the proposal whose later execution applies the installation and its grants. App verification corrected two marketing-brief details rather than promoting stale UI: the current choices are **Any action** and **Specific actions** (the brief called the latter “Selected actions”), and a custom permission is composed by selecting a write function on a verified contract so the app derives the target and 4-byte selector, not by typing a raw selector. Specific-actions mode can start empty, but the conditioned grant is attached only when the apply proposal executes. The page also carries the one owner question this material leaves open: whether granular-permission setup should be fully self-serve or begin with Aragon guidance.

  [Governance process](./governance/process.md) now describes the process-details **Authorized actions** section as an indexed target-selector presentation that decodes contract and function names where available, without claiming unknown selectors become plain language or that the view is an exhaustive live-chain read. [Proposal actions](./governance/action-builder.md) explains why its default authorized-only filter reduces likely execution failures while preserving the reveal toggle. [Proposal](./governance/proposal.md) distinguishes proposal executability from caller eligibility: on supported guarded plugin versions the app checks the connected wallet's `EXECUTE_PROPOSAL_PERMISSION` and blocks an unauthorized attempt; older open-execute versions skip that check. The condition-factory/execute-selector mapping matched protocol-doc; the garbled release-note version was not turned into a claim.

  The mined source section was retired from `raw/marketing_briefs.md`; sibling source material remains. Run as three Terra extraction, implementation, and graph tracks followed by three adversarial re-reviews. The reviews caught and removed an unsupported Governance Designer “edit governance settings” example and narrowed process details from a complete live-chain view to the indexed surface the app actually serves. No new task, product opportunity, or structural gap was warranted. The standing account-wide permission-surface owner ask is narrower but remains open, and Roles remains unwritten. Draft inventory is 59; page questions reconcile to 44 across 27 platform pages (61 including 17 upstream checklist items); unresolved promises remain Roles and the intentionally forward-linked Gauge voting page. **Board delta:** task retired — mine-permissions-and-access-control; draft added — gradual-permission-handover; owner ask added — designer self-serve vs assisted setup; existing permission viewing/editing ask narrowed; no task, opportunity, or gap added or resolved.

- **2026-08-04** — Completed mine-app-cms-release-notes as a source-and-code reconciliation, creating the cross-cutting draft reference [App CMS](./app-cms.md). The page documents the five uses established by the owner's release-note category: Aragon-curated featured accounts on Explore; presentation-only plugin hiding in datalists, including the duplicate-looking-list rationale when separate body plugins represent the same census with different settings; per-account navigation-link hiding, with gauges as the worked example; a DAO/network/token-voting-plugin-scoped list of featured-delegate addresses; and manual filtering of specifically reported spam tokens. Featured-delegate selection stays separate from the profile surface and delegation behavior owned by later tasks, and the list is explicitly not presented as an exhaustive App CMS schema inventory.

  Verification against `app@122f1bd1` and `app-backend@107103b` corrected two pieces of release-note shorthand. The frontend consumes the featured-account, featured-delegate, and DAO-override files from the `app-cms` repository's current `main` branch with cached background revalidation, while the spam list is fetched by `app-backend`, synchronized as a CMS-origin token classification, and filtered from default asset and transaction results. `pluginsToHide` is a case-insensitive address filter for opted-in visible lists and does not uninstall a plugin or break canonical proposal/detail lookup. `navLinksToHide` hides navigation links rather than providing a general-purpose override; only the Gauge page also redirects a direct visit when hidden. There is still no local `app-cms` checkout, so the already-blocked source-repository grounding task retains that code-entry gap.

  The chain-source-of-truth tension now has a bounded disposition rather than a new absolute “code is law” principle: [Platform design principles](./principles.md) states that App CMS can curate selected content and suppress or filter how chain-derived items surface without changing the underlying on-chain fact. [Linked-account signaling](./accounts/linked-account-signaling.md) now points to the CMS reference for the explicit non-use: feature visibility may be configured off-chain, but the linked relationship itself is a mutual on-chain acknowledgement. [Body](./governance/body.md) links the plugin-hiding rationale; the root index and [Source repositories](./repositories.md) expose the new reference; and the Explore, Aragon Profiles, platform-infrastructure, and source-grounding tasks now cite its exact home. The former structural App CMS gap is therefore closed into one page-owned owner question, still promoted in Ready for your input: who authors and approves CMS changes, what programmatic or chain-derived surfaces may be suppressed, and what must always surface.

  Run as three Terra extraction/code/graph tracks followed by three adversarial re-reviews. The reviewers found no source or taxonomy defect and drove the frontend/backend and navigation-link wording corrections above. No new task, product opportunity, structural gap, or additional owner ask was warranted. Draft inventory is 58; page questions reconcile to 43 across 26 platform pages (60 including 17 upstream checklist items); the only unwritten promises remain Roles and the intentionally forward-linked Gauge voting page. **Board delta:** task retired — mine-app-cms-release-notes; tasks refined — mine-explore-page-release-notes, mine-aragon-profiles-brief, mine-platform-infrastructure-and-general-ui, and ground-the-base-in-the-source-repos; draft added — app-cms; App CMS structural gap closed into a narrower page-owned operating-policy question; no task, opportunity, or owner ask added.

- **2026-08-04** — Completed mine-proposal-lifecycle-and-actions as a claim-by-claim reconciliation of the release notes against the current app (`122f1bd1`) and pinned protocol documentation (`2d800ee`). New draft [Transaction submission stepper](./design/transaction-submission.md) is the named home for the reusable post-input lifecycle: prepare the transaction and any IPFS metadata, approve/sign, wait for chain confirmation, then poll backend indexing; the success action leaves into fresh app state, while **Proceed anyway** can leave before indexing finishes without changing the confirmed transaction. [Wizard](./design/wizard.md), [full-screen wizard](./design/full-screen-wizard.md), [dialog taxonomy](./design/dialog-taxonomy.md), [account creation](./accounts/account-creation.md), [proposal](./governance/proposal.md), [governance designer](./governance/governance-designer.md), and [create transaction](./treasury/create-transaction.md) now link that lifecycle rather than each defining it. The later platform-infrastructure task points directly to this page for wallet retry/duplicate-transaction safety.

  [Governance designer — Preparing and applying an installation](./governance/governance-designer.md#preparing-and-applying-an-installation) is the named home for the other shared mechanism: permissionless preparation deploys/configures the plugin and records the exact requested changes without mutating DAO permissions; governed application changes the live DAO, so an authorized process carries it as a proposal. Admin is the bootstrap route whose proposal passes and executes immediately, not an exception to the proposal requirement. The later permissions task now cites this section and the submission pattern directly instead of being told to rediscover their homes.

  The lifecycle facts landed on their existing owners. [Proposal simulation](./governance/action-simulation.md) now names both Tenderly entry points, the cached latest proposal-page result and ten-minute rerun limit, and the non-terminal-state boundary that excludes executed proposals. [Proposal creation](./governance/proposal-creation.md) now describes the process selector accurately as a fail-open simulation preflight: successful checks sort first, a concrete revert disables and sorts the row last, inconclusive checks do not claim ineligibility, and **View requirements** reaches process details. [Proposal](./governance/proposal.md) now records the leading/default All-proposals aggregate when multiple processes exist, the process-key-bearing slug as its process indicator, and per-process tabs; app filtering by process plugins resolved and removed the stale body-plugin tab question, while [Proposal identifiers](./governance/proposal-identifiers.md) remains the one home for key/slug semantics.

  [Proposal actions](./governance/action-builder.md) gained reorder/remove controls, specialized basic-view examples for nested `DAO.execute`, proposal-creating-proposal, and gauge add/remove, and the portable JSON workflow. App source resolved the release note's ambiguous three export locations to: the proposal action-composition flow, an existing proposal's details page, and an execution-detail dialog on [Transactions](./treasury/transactions.md); imports decode while loading. [Create transaction](./treasury/create-transaction.md) matched the release note's permission-gated direct `DAO.execute` flow and now includes its Tenderly simulate/skip path, so there was no conflicting implementation to choose between. The gauge example intentionally promises the Gauge voting page, owned by the already-ready gauge task.

  Deliberate non-additions: real-time proposal-page decoding is already expressed by the action-view capability; the generic URL tab-deep-link detail was not promoted because the source itself marks it as possibly immaterial; vague action-input “improvements” and the garbled JSON-shape phrase were not turned into facts. Composer field/validation/template depth remains with capture-the-action-builder-feature-set, per-screen flow depth remains with capture-wizard-screen-walkthroughs, and wallet retry safety remains with mine-platform-infrastructure-and-general-ui. Run as three Terra research/audit tracks, implementation, and two focused adversarial re-reviews; no new task, owner ask, or structural gap was warranted. Draft inventory is 57; page questions reconcile to 42 across 25 platform pages (59 including 17 upstream checklist items); the only unwritten promises are Roles and the intentionally forward-linked Gauge voting page. **Board delta:** task retired — mine-proposal-lifecycle-and-actions; tasks refined — capture-the-action-builder-feature-set, mine-permissions-and-access-control, and mine-platform-infrastructure-and-general-ui; draft added — transaction-submission; no task, owner ask, or gap added or resolved.

- **2026-08-04** — Completed mine-governance-model-release-notes as a reconciliation against the corrected SPP/app model rather than a fresh rewrite. The source-confirmed semantics now read consistently across [Staged proposals](./governance/staged-proposals.md), [Multisig gates](./governance/multisig-gates.md), the [value proposition](./value-proposition.md), the advanced-governance [guide](./guides/multisigs-in-advanced-governance.md), and the [Voting Terminal](./design/voting-terminal.md): approval and veto are per-body roles, a veto-only shape requires one or more vetoing bodies with no approval requirement and a positive veto threshold, bodyless stages are timelocks, qualifying stages become advanceable rather than advancing automatically, and the affirmative action for a vetoing body is labeled **Yes to veto**. The already-correct [Stage](./governance/stage.md) field disposition remained the authority; the release notes added UI copy but did not overturn any source-verified field.

  Four missing fact clusters landed in their existing homes. [Body](./governance/body.md) now distinguishes reusing one installed plugin across processes — one shared governance configuration, with minimum-duration risk — from separate body instances representing the same census with different settings. [Governance designer](./governance/governance-designer.md) now states that an existing process whose permissions admit the installation actions can install another process, and makes the uninstall-specific prepare-then-apply-via-proposal sequence explicit; [removing the last process](./accounts/last-process-removal.md) identifies the sharpest case as removing the only unconditional/unrestricted Execute route, which can leave the account semi-immutable without claiming every such account is bricked. [Safe as a body](./governance/safe-as-a-body.md) now records Safe branding/logo and the process-details visibility of Safe-owner proposal eligibility. The ambiguous release-note taxonomy that treated standalone, basic, and advanced as three peer process varieties was not promoted over the clearer current model.

  Scope stayed with existing owners: mine-proposal-lifecycle-and-actions retains the reusable submission pattern and its rationale; mine-app-cms-release-notes retains duplicate-looking membership and plugin-hiding presentation; ground-the-base-in-the-source-repos and capture-wizard-screen-walkthroughs retain plugin-slot implementation and screen depth. No new task or structural gap was warranted. The optimistic-governance owner ask remains, narrowed to the model's broader purpose, history, and current posture now that its core mechanics and rejected synonyms are settled. The shared release-notes inbox capture remains because sibling mining tasks still depend on its other categories. **Board delta:** task retired — mine-governance-model-release-notes; optimistic-governance gap narrowed; no task, owner ask, or gap added.

- **2026-08-04** — Folded the owner's authority-vocabulary answer into the base, advancing derive-the-executor-acl-auth-model without closing it. **Two nouns are settled.** A **governor** is a decision *process*, not an entity: the rule that aggregates the distinct preferences of multiple actors into one choice (the owner's own framing, by analogy to social choice theory). It has no address and is not a party to any call, so "which contract is the governor?" is a category error; in this product it appears as a [body](./governance/body.md) of actors plus the rule aggregating them, composed and carried to execution by a [governance process](./governance/process.md). An **account** is an address that can act — defined by exposing a generic execution entry point that takes a target, value, and calldata and makes that call as itself — with externally-owned accounts (the keyholder can sign anything) and smart contract accounts (an Aragon account's `execute`, a Safe's equivalent) as its two kinds. "Executor" is therefore not a second role: an account *is* an executor, and upstream's own name for that hat ("the executor" on [DAO](./protocol-doc/core/dao.md)) is recorded as the protocol layer's wording rather than a competing product term. The owner explicitly **rejected** the protocol-wide reading in which every address is an account — an ERC-20 address is not one — so [account](./accounts/account.md) states the narrower capability-based sense and notes the broader Ethereum-docs usage only so protocol material stays readable; the Ethereum account model is cited, not restated.

  Reconciled every page that carried the older vocabulary as load-bearing prose, rewriting to the new present rather than narrating the change: [account](./accounts/account.md) gained the definition and an explicit *an account is not a governor* section; [safe vs Aragon multisig](./guides/safe-vs-aragon-multisig.md) now describes a coupled **governor–account monolith** whose authorization rule ships in the same contract as the execution entry point it authorizes (rigid in that changing who decides means changing the thing that acts — not that Safe cannot be extended); [authorization and execution model](./access-control/authorization-and-execution.md) renamed its *decision mechanism* and *execution identity* roles to governor and account and now links the account definition instead of duplicating it; [OSx authorization mapping](./access-control/osx-authorization-paths.md) renamed the same two rows and its topology section; and [stages over direct permission grants](./governance/stages-over-direct-permissions.md), [target](./governance/target.md), [vault](./treasury/vault.md), the [treasury index](./treasury/index.md), and [transactions](./treasury/transactions.md) lost "arbitrary executor" as a competing noun. All the pages that were waiting now link the model — [target](./governance/target.md) on the two authorization boundaries, [linked-account signaling](./accounts/linked-account-signaling.md) on grants that sit outside the authority path entirely, [linking does not imply control](./accounts/linking-does-not-imply-control.md) on direct authorization versus effective control — so `wiki backlinks` shows the dependency rather than prose restating it.

  Two of the model page's six open questions are answered and deleted; a new residual replaced them (can a decision rule with no actor preferences to aggregate — a time lock, an oracle threshold, a keeper — be a governor?), and the four untouched ones stand: policy administration, target-side authorization as a formal second boundary, effective control as a first-class concept, and what **ACL** names. mine-permissions-and-access-control's vocabulary constraint was updated accordingly — governor and account are now *required* usage, only "ACL" stays hands-off — which removes the ordering hazard that had it writing around three unsettled labels. Composite gate run. **Board delta:** owner ask resolved in part — the authority-vocabulary input row is replaced by a narrower row 3 covering only the remaining boundary and naming questions, and the optimistic-governance briefing moves to input row 1 as the ask that now blocks most (it gates the first row of *Ready to run*); task refined — derive-the-executor-acl-auth-model (still in progress, Work pruned to the open questions, Progress recorded, a new Done-when criterion added: no page presents "executor" as an entity distinct from an account) and mine-permissions-and-access-control; no gap opened or closed; drafts inventory unchanged.

- **2026-08-03** — Completed redesign-the-documentation-backlog and, on the owner's instruction, audited and consolidated the entire task queue in the same pass. **The board is now an action router organized by who moves next** — Ready for your input, Ready to run, Not yet ready, Inventories — replacing the five record-kind sections (drafts / highlights / tasks / knowledge gaps / unwritten pages). Every row states why it is there, what it produces, and the surfaces it affects; blocked rows name their blocker; and position is the only priority signal, so the "band the queue Now/Next/Later" rule and the per-task `Impact:` ratings both retired — the ratings reworded to "Where the value is likely to be" rather than deleted, so their substance survived. The one highlight and all five knowledge gaps became input rows, so everything waiting on the owner now reads from one section, while the drafts list, the `wiki checkboxes` aggregate, and `unresolved` moved to Inventories. Two findings landed there: the drafts list was reconciled against `wiki list --where status=draft` (56 entries, exhaustive now that the private `research/` workspace is off the board), and a **settle-first batch** was named — the nine drafts no queued row will touch — because rows 1–13 rewrite most of the governance, accounts, and access-control drafts, so reviewing those now would mean reviewing them twice.

  **Queue consolidation: 30 open tasks → 22, plus this one retired.** Six merges, each because the tasks had been split by *source* where the work is one *outcome*, and each carrying over every claim, pointer, and scope boundary from its inputs: **mine-permissions-and-access-control** (← mine-granular-access-control-brief + mine-permissions-release-notes), **mine-token-governance-setup** (← mine-lock-to-vote-brief + mine-lock-to-vote-release-notes + mine-token-voting-erc20-release-notes), **mine-velocker** (← mine-velocker-brief + mine-velocker-release-notes), **mine-proposal-lifecycle-and-actions** (← mine-proposal-lifecycle-release-notes + mine-action-builder-release-notes), **mine-admin-and-dao-presentation** (← mine-dao-creation-admin-release-notes + catalogue-admin-plugin-differences), and **mine-platform-infrastructure-and-general-ui** (← mine-platform-infrastructure-release-notes + mine-general-ui-release-notes). The token-governance merge changes an outcome rather than merely saving a pass: the LockToVote brief's selection rule ("a vanilla ERC-20 takes lock to vote") is incomplete without the release notes' automatic-wrapper branch, so a decision page written from the brief alone would have been wrong. Three tasks were deliberately *not* merged despite sharing a page — capture-the-action-builder-feature-set (blocked on owner material, and it owns a different section of proposal-actions than the release notes' action-list controls; the boundary is now stated on both), plus the page-specific Voting Terminal and dialog-taxonomy groundings, which stay independent of the owner-blocked broad codebase pass. The retired lock-to-vote release-notes task's one non-duplicate item, the guardian-DAO guide need, moved into rethink-the-guide-layer as a named candidate so it survived its file's deletion.

  **Ordering rewritten on rework-avoided rather than on value**, and the rule recorded in WORKFLOW: vocabulary, shared mechanisms, and area framing before the work that would re-invent or contradict them; product semantics before the presentation layer that renders them; cheap passes several later rows depend on early; scattered small-fact passes last, once their homes exist. The concrete calls: governance-model reconciliation leads because it carries a terminology ruling (the owner rejects "multisig gating"/"safe gating" for optimistic governance) against the most-depended-on pages in the base — governance-designer alone has 65 backlinks; the transaction-dialog stepper and the two-step deploy-then-propose pattern move to row 2 so three later rows cite one home instead of each re-explaining it; app-cms moves to row 3 as the hub three later rows link; aragon-profiles precedes the token panel and the DAO's own ENS name because it re-scopes `accounts/` to admit participant identity; premium/partially-supported services precedes its three plugin instances so none invents the phrasing; the Voting Terminal grounding follows both the corrected per-body role model and the gauge decision it must reflect. An ordering defect was also fixed: the two access-control mining tasks were scheduled *ahead* of the authority-vocabulary answer they depend on — the merged task now states its claims without leaning on the contested "ACL"/"governor"/"executor" labels, so it neither idles behind the owner nor bakes in prose a different ruling would force rewritten. Shared facts got single owners where several passes each claimed one: featured delegates (CMS mechanism vs delegate surface vs delegation), the DAO's `dao.eth` name vs the participant's `aragon.eth`, the profile's summary cards vs the token panel, and the wallet retry-safety warning vs the submission stepper.

  **WORKFLOW.md** now carries the model: the four sections plus the one-item-one-row-one-section and position-is-the-priority rules; the merge rule (one task per surface-and-source-set, with "check whether the sibling task already ran" named as the smell of a merge that should have happened); the row contract and how a section mirrors an entry's `status`; the ordering rule; and a reconciliation step rewritten for sections rather than state labels. "Queue row" was normalized to "board row" across every entry. Composite gate run. **Board delta:** tasks retired — redesign-the-documentation-backlog (completed) and thirteen superseded by merge: mine-granular-access-control-brief, mine-permissions-release-notes, mine-lock-to-vote-brief, mine-lock-to-vote-release-notes, mine-token-voting-erc20-release-notes, mine-velocker-brief, mine-velocker-release-notes, mine-proposal-lifecycle-release-notes, mine-action-builder-release-notes, mine-dao-creation-admin-release-notes, catalogue-admin-plugin-differences, mine-platform-infrastructure-release-notes, mine-general-ui-release-notes; tasks added — mine-permissions-and-access-control, mine-token-governance-setup, mine-velocker, mine-proposal-lifecycle-and-actions, mine-admin-and-dao-presentation, mine-platform-infrastructure-and-general-ui (all ready); tasks refined — rethink-the-guide-layer (guardian-DAO candidate), capture-the-action-builder-feature-set (list-controls boundary), mine-governance-model-release-notes (cite the submission pattern), mine-app-cms-release-notes (featured-delegates ownership), mine-token-panel-onboarding-release-notes and mine-gauge-voting-release-notes (merged-slug references and ownership boundaries); owner asks — the authority-vocabulary highlight and all five knowledge gaps (optimistic governance, permission surface, chain state, app metadata, app-cms) re-expressed as input rows 1–8 with nothing dropped, plus the draft review as row 9; no gap opened or closed.

- **2026-08-03** — Separated the owner's private research from the portable knowledge graph: `research/` is now explicitly Git-ignored and wiki-ignored, its private review material left the documentation backlog, and a local research index owns that workspace's navigation. Refiled [Honest abstraction](./principles/honest-abstraction.md) as the first expanded part of the load-bearing [Platform design principles](./principles.md) collection; the overview remains at the root while atomic principle pages now live under `principles/`. Updated the operating policy and wiki skill to enforce both boundaries. Board delta: private research removed from drafts; tasks, highlights, and gaps unchanged.

- **2026-08-03** — Closed out capture-the-product-updates-braindump: the owner confirmed `raw/release_notes.md` was the complete product-updates walkthrough, no further batches coming. Both of the task's Trigger deliveries (the 2026-08-03 marketing-briefs drop and the 2026-08-03 release-notes dictation) are captured, and every feature across both has a disposition — an existing page, a new draft, or one of the 20 queued mining tasks (16 release-notes tasks plus the 4 marketing-brief tasks) — with the two genuine deep-dive captures (wizard screen walkthroughs, the action builder's composer depth) correctly staying their own separate, still-blocked tasks rather than folded in here. Queue row removed, file deleted. While renumbering, also corrected four stray by-row-position citations introduced across the new tasks' backlog rows (a queue-position reference to the Aragon-names ship-status contradiction, and three "alongside/superseded by/routed to row N" phrasings) into proper task-entry links, per this board's own by-entry-not-position citation rule. Board delta: task retired — capture-the-product-updates-braindump; queue renumbered to 31 rows.

- **2026-08-03** — Commissioned mining of the topically-reordered release notes (`inbox/2026-08-03-release-notes-ordered.md`) as 16 new per-category tasks, queued rows 7–22: mine-explore-page-release-notes, mine-dao-creation-admin-release-notes, mine-governance-model-release-notes, mine-permissions-release-notes, mine-proposal-lifecycle-release-notes, mine-action-builder-release-notes, mine-token-voting-erc20-release-notes, mine-token-panel-onboarding-release-notes, mine-velocker-release-notes, mine-gauge-voting-release-notes, mine-lock-to-vote-release-notes, mine-capital-distributor-release-notes, mine-premium-services-model-release-notes, mine-app-cms-release-notes, mine-platform-infrastructure-release-notes, mine-general-ui-release-notes. On the owner's explicit instruction, these were kept as separate, standalone tasks rather than merged into the four already-queued marketing-brief tasks covering the same features (permissions, veLockers, lock-to-vote, member identity) — the owner will audit and consolidate the task list. Two findings surfaced worth flagging here: (1) mine-aragon-profiles-brief was updated with a direct contradiction — that task's own ship-status call treats Aragon-name rename as unbuilt, but the release notes' 1.33 entry states in the owner's voice that it shipped, with existing ENS records moving to the new name; unresolved until whoever runs that task checks the app or asks the owner. (2) The Linked accounts category needed no task at all — its content (the bi-directional permission-signal mechanism, seeing governance processes together) is already fully covered, with more technical precision, by the existing [linked-account](./accounts/linked-account.md) and [linked-account-signaling](./accounts/linked-account-signaling.md) pages. Also updated: the parent capture-the-product-updates-braindump task's Trigger, annotated with this second 2026-08-03 partial fire (the task stays blocked — several release-notes entries themselves promise more braindump material later); the backlog's `app-cms` knowledge gap, narrowed to reference the new app-cms task as its partial closure route; and a guardian-DAO guide candidate routed to rethink-the-guide-layer from the lock-to-vote release notes. Board delta: 16 tasks added (rows 7–22, all ready), queue renumbered to 32 rows; mine-aragon-profiles-brief and capture-the-product-updates-braindump updated in place; app-cms gap narrowed. Composite gate run. (Note: capture-the-product-updates-braindump was closed out later the same day — see the entry above.)

- **2026-08-03** — Reorganized the release-notes cleaned capture (`inbox/2026-08-03-release-notes-cleaned.md`) into a topical view, `inbox/2026-08-03-release-notes-ordered.md`: 18 major categories (Explore page & DAO discovery; DAO creation, admin experience & DAO-level presentation; Governance model; Permissions & granular access control; Proposal lifecycle; Action builder & complex/nested actions; Token voting: ERC-20 import & wrapping; Token panel, delegation & onboarding nudges; veLockers; Gauge voting; Lock-to-vote; Capital distributor & Merkle distribution; Premium/partially-supported services model; Member identity — Aragon Names/ENS/delegate statements; App CMS content configuration; Linked accounts; Platform infrastructure; General UI infrastructure & wallet safety) replacing the 34 per-release entries, every item retagged with its originating release version for provenance. Produced by 18 parallel Sonnet extraction passes plus a completeness sweep against the release-ordered source; the sweep's audit agent hit a hard session-usage limit on both attempts, so the sweep was run by hand instead — one genuine gap found and fixed (the 1.0.0 body-adding/plugin-slot mechanism, folded into Governance model), no fabrications found. Composite gate run.

- **2026-08-03** — Completed compare-stage-against-spp against the pinned SPP stages/lifecycle documentation and the clean local `app` checkout at `122f1bd1`. [Stage](./governance/stage.md) now carries a complete 12-field disposition for both SPP structs and corrects the abstractions the comparison refuted: approval and veto are per-body roles that can mix, their thresholds are independent body counts and vetoes are decisive, early advance is approval-only, veto stages preserve the full duration, bodyless stages are timelocks, and expiration is a fixed deadline derived from duration plus the optional grace period rather than a window starting when thresholds are met. The app encoder settled the would-be owner questions: cancellation and action editing are fixed off, external bodies derive manual reporting, and report-and-advance is fixed on as a best-effort request; no human input or follow-up question remains. Updated the governance map, designer summary, and staged-proposal traversal wording that restated the old model; retargeted ground-the-voting-terminal-spec to per-body roles and the timelock shape; and added the canonical `aragon/staged-proposal-processor-plugin` repository with protocol-doc snapshot `676617a9` to [Source repositories](./repositories.md). Board delta: task retired — compare-stage-against-spp; task refined — ground-the-voting-terminal-spec; highlights and gaps unchanged.

- **2026-08-03** — Completed mine-linked-accounts-brief as a claim-by-claim reconciliation rather than a rewrite. [Linked account](./accounts/linked-account.md) now explains earmarking as intent encoded by where funds live, distinguishes aggregate views from the existing process/body and account partitions, identifies each linked account's name and description as its own account metadata, and points linked-account proposal creation to the normal authorized-actions filter. App and app-backend checks resolved the brief's two apparent ambiguities: proposals and members remain flat in their process/body partitions rather than gaining a separate account partition, and a linked-account process resolves its account as the action target before using the existing selector-based filter. [Linked-account signaling](./accounts/linked-account-signaling.md) now records the anti-spam reason for mutual acknowledgement and the proposal-based, contact-Aragon setup posture while preserving the exact permission IDs and both-grants-active rule from the earlier follow-up. Confirmation-only control, execution, and asset pages stayed unchanged; timeline, personas, and narrative examples remained marketing-only, and the mined Linked Accounts section was removed from `raw/marketing_briefs.md`. Board delta: task retired — mine-linked-accounts-brief; no highlights, owner questions, or knowledge gaps added or resolved.

- **2026-08-03** — Completed rework-the-design-principles from the owner's 2026-07-29 review: [Platform design principles](./principles.md) now carries the ten-principle set; [Honest abstraction](./principles/honest-abstraction.md) is the first promoted `principle`; and [abstract then drill down](./design/abstract-then-drill-down.md), [invariant validation](./design/invariant-validation.md), [the team-handoff pattern](./design/reach-out-to-the-team.md), and [Aragon Names](./accounts/aragon-names.md) carry the reusable consequences. Relocated the protocol-mechanism → product-capability → interaction-pattern rule into WORKFLOW.md and the root index, repaired every live stale principle citation, and landed the governance, transaction, autonomy, modularity, curation, and stewardship ripples across their owning pages. Protocol and app-source checks grounded action batching in `DAO.execute(Action[])` rather than a generic multicall and grounded Aragon Names in the Member Registry; the broader verified identity surface remains with mine-aragon-profiles-brief, while ground-the-base-in-the-source-repos now owns the app-enforced invariant and batched-wallet-request inventory. The non-regenerable `raw/principles_notes.md` transcript was retired after its durable substance and provenance were folded. Board delta: drafts added — honest-abstraction, abstract-then-drill-down, invariant-validation, reach-out-to-the-team, aragon-names; task retired — rework-the-design-principles; tasks refined — mine-aragon-profiles-brief, ground-the-base-in-the-source-repos, and inventory-the-interaction-principles; rethink-the-guide-layer queue label reconciled to its existing blocked state; highlights and gaps unchanged.

- **2026-08-03** — Partially mined `inbox/acl-one-pager.md` into two access-control drafts after checking the pinned protocol pages and their `osx` sources: [Authorization and execution model](./access-control/authorization-and-execution.md) defines implementation-neutral roles (decision mechanism, runtime principal, enforcement, authorizer/policy, policy administrator, execution identity and engine, final target) and the two independent authorization boundaries; [OSx authorization mapping](./access-control/osx-authorization-paths.md) maps DAO, plugins, `auth`, permission tuples, conditions, ROOT, and execution routes onto those roles without feeding OSx primitives back into the generic model. The pass treats the one-pager's fixed governor→executor→target line, exclusive target trust, and single-permission governance swap as conditional topology claims rather than protocol invariants. Among existing product pages, only Permission model links the drafts; the access-control map exposes them, while the five other waiting pages stay untouched until the owner settles terminology and scope. The source remains in the inbox because its positioning and integration claims are not fully dispositioned. The derive-the-executor-acl-auth-model task moved Ready → In progress and now records the remaining validation and backlink work. Board delta: drafts added — authorization-and-execution and osx-authorization-mapping; highlight added (authority vocabulary, topology, and promise boundaries); executor/account/ACL/`auth` knowledge gap closed into page-level questions; task derive-the-executor-acl-auth-model moved ready → in-progress; other gaps unchanged.

- **2026-08-03** — Folded the owner's linked-account permission-ID follow-up into [Linked-account signaling](./accounts/linked-account-signaling.md): recorded the exact parent-to-sub-DAO and sub-DAO-to-parent acknowledgement constants, then verified in the app-backend linking path that both latest permission states must be granted before the relationship is written and that revoking either removes it. Removed both answered page checkboxes and updated mine-linked-accounts-brief so its later marketing-brief reconciliation preserves the resolved mapping and two-grant rule. Board delta: highlight resolved (linked-account acknowledgement permission IDs); tasks and gaps unchanged.

- **2026-08-03** — Commissioned rethink-the-guide-layer from the owner's request to think through the full guide/use-case surface and reconsider what the layer should own. The ready task preserves the newly settled audience boundary (repository orientation stays in the root index; guides serve people doing things in Aragon) while treating the rest as open design work: inventory every current area and capability, derive the real user journeys, define the guide-selection and availability rubric, disposition the current multisig guide, and turn the chosen portfolio into durable WORKFLOW/index policy plus finite ready or blocked guide tasks. It deliberately does not write every guide in the strategy pass. Board delta: task added — rethink-the-guide-layer (ready, queued at row 8 after the five feature-brief mining tasks); highlights and gaps unchanged.

- **2026-08-03** — Resolved the guides-layer purpose from the owner's review: guides now serve people trying to accomplish a concrete task in the Aragon platform, not people navigating this repository. Folded the useful reading order and pre-build guidance from the former Orientation guide into the root index, retired the separate orientation entry, and rewrote the guides index and multisig guide around a user goal, actions, availability, and a done state. Updated the queued principles-rework task so its future platform/protocol/interaction boundary lands in the root index rather than the retired page. Board delta: highlight resolved (guides-layer purpose); Orientation draft retired; tasks and gaps unchanged.

- **2026-08-03** — Captured the owner's release-notes brain dump (`raw/release_notes.md`) as `inbox/2026-08-03-release-notes-cleaned.md`: 34 per-release entries (1.0.0 through 1.35 as dictated, plus a third-party-dependencies aside), wording cleaned with every claim, hedge, open question, and follow-up preserved, and dictation garbles flagged in per-entry Transcription notes rather than silently corrected (the duplicate "1.10" that likely means 1.1.0; "app 21, 121" rendered 1.21; "1.20.22" possibly 1.22; 1.18/1.29/1.31/1.32 absent from the dictation). Produced by a per-chunk clean + adversarial fidelity-audit agent fleet over a byte-verified 34-way split of the raw file; the interrupted fleet's residual audit findings were applied by hand. Raw file left in place; the capture is unmined (it bears on capture-the-product-updates-braindump's trigger, which stays with the owner). Composite gate run.

- **2026-08-03** — Commissioned the documentation-backlog redesign after a first-principles review of the board as an action surface. The ready task redesign-the-documentation-backlog asks for a clear separation between owner input, runnable agent work, and work still waiting; meaningful ordering without a second priority taxonomy; and a concise explanation of each item's placement, blocker where relevant, and affected surface. The task deliberately leaves the migration mechanics to the agent running it. Board delta: task added — redesign-the-documentation-backlog (ready, queued immediately after capture-the-product-updates-braindump at row 2); highlights and gaps otherwise untouched.

- **2026-08-03** — Commissioned the mining of the owner's five-feature marketing-briefs drop (`raw/marketing_briefs.md`, git-ignored and machine-local; the owner routed it to `raw/` as brain-dump source and asked for one mining task per feature). Run as a five-agent analysis pass — one agent per brief, each surveying existing platform and protocol-doc coverage before drafting its task — producing five Ready task entries, queued at rows 2–6, each shaped to its brief's nature: mine-linked-accounts-brief (pure reconciliation — the four `accounts/` drafts already cover the mechanics; net-new is the earmarking rationale, the naming surface, the anti-spam rationale, and the contact-Aragon onboarding posture; one drilldown contradiction to resolve; the permission-ID highlight stands unanswered), mine-granular-access-control-brief (enrichment of the designer / permission-model / process / proposal-actions pages plus one gradual-rollout concept page; narrows the permission-surface gap without closing it and deliberately does not deliver roles.md), mine-lock-to-vote-brief (mechanics fully upstream in protocol-doc → drop-with-pointer dispositions; one new token-governance mechanism page; provisional rollout language in the brief conflicted with the designer's three-plugin claim and was left for owner review; the capability is treated as current product per the 2026-07-28 installable-set confirmation, with the September date read as announcement timing), mine-aragon-profiles-brief (greenfield identity cluster — ENS-as-profile-layer decision, subname-claiming and profile capabilities, delegate-record reference in `governance/`; the brief is pre-launch material, so a per-claim ship-status disposition gates every present-tense claim; the unbuilt rename/release flows become opportunities), and mine-velocker-brief (double-greenfield — one `governance/velocker.md` capability page owning the Members-page surface and dynamic delegation, an explicit lock-to-vote disambiguation, and the missing ve-governance mechanism layer recorded as a knowledge gap rather than restated from a marketing brief). The drop partially fires capture-the-product-updates-braindump's trigger (annotated ✅ on the entry; the task stays blocked on the rest of the walkthrough). Composite gate run. Board delta: tasks added — mine-linked-accounts-brief, mine-granular-access-control-brief, mine-lock-to-vote-brief, mine-aragon-profiles-brief, mine-velocker-brief (all ready, queued 2–6); capture-the-product-updates-braindump trigger annotated as partially fired 2026-08-03, still blocked; highlights and gaps untouched.

- **2026-07-29** — Planned the principles rework from the owner's voice-note review of all 11 principles (landed in `raw/principles_notes.md`; it answers the former highlight 1 — none of principles 5–10 survives as written). Run as a five-agent analysis pass: a per-principle disposition analysis, three area ripple sweeps (governance; accounts/treasury/access-control; design/guides/root), and a graph blast-radius audit over `wiki backlinks`/term sweeps/upstream coverage. The settled plan lives in the new rework-the-design-principles task (queued Ready at 2): a ten-principle set — the chain/semantics pair reframed ("the app serves the chain"; "product semantics are their own model"), a new honest-abstraction principle carrying the owner's four examples (designer gating, process/body as app-built abstractions, reach-out entry points, multicall signing opacity) and slated for promotion to its own root page, autonomy kept with the invalid-input carve-out, the modularity cluster rewritten (semantic layer bounds generalization; shared-framework argument; isolate-the-bespoke/linear additions), old 7 split into autonomy plus adopt-industry-standards (Aragon Names/ENS — currently documented nowhere), and curation grounded concretely — plus old 3 relocating to WORKFLOW.md as the three-layer meta-principle, four new page candidates (abstract-then-drill-down, invariant-validation, reach-out-to-the-team, aragon-names), and the citation-repair map for the twelve pages linking principles.md. The owner's deferred interaction/semantic split stays deferred; its preparatory analysis is queued as inventory-the-interaction-principles (blocked on the owner's go-ahead). Composite gate run. Board delta: highlight resolved (principles 5–10 confirmation — answered by the review); tasks added: rework-the-design-principles (ready), inventory-the-interaction-principles (blocked on the owner's go-ahead); gaps untouched.

- **2026-07-29** — Mined the owner's UX-principle briefing into two linked design patterns. [Every element makes a claim](./design/every-element-makes-a-claim.md) defines semantic minimalism: every visible element asserts that something exists, matters, or is actionable; user expectation and product meaning — never architecture alone — determine whether it earns attention. [Control availability](./design/control-availability.md) turns that rule into the hide vs disabled vs eligibility-guard vs warning decision model, with existing product instances linked in both directions. The root [principles](./principles.md) now carries the cross-cutting rule; [proposal creation](./governance/proposal-creation.md) makes the concrete advanced-governance boundary explicit: the basic flow does not advertise existing conditions or their absence. Both patterns added to the design map and draft-review board; the now-answered “are there principles beyond ten?” question retired. Composite gate run.

- **2026-07-29** — Task-clarity pass (owner feedback: tasks argued their motivation but buried what to do and how to verify done). The contract in [WORKFLOW.md](./WORKFLOW.md) now requires it structurally: `## Work` bullets are imperative actions (verb first, artifact named, rationale demoted to a trailing clause), and the success criteria live in a `## Done when` section — one bullet per criterion, each answerable yes/no by a cold session against a named artifact, the completion sequence always last (the inline label remains only for genuinely single-condition tasks). All ten task files rewritten to that shape; the why stays in each lead and What-this-unblocks, the action sections read as instructions. Also folded in: ground-the-base-in-the-source-repos's checkout list now counts `conditions` (cloned yesterday) and leaves only `app-cms` open. Composite gate run.

- **2026-07-28** — Completed the clone-the-conditions-repo task: `aragon/conditions` checked out as the sibling `../conditions` (default branch, at `33918b0`), and the [repositories](./repositories.md) row's local-path cell filled to `../conditions`, matching the other sibling rows' relative notation. The row's existing confirm-or-correct checkbox (public condition library vs the old "condition-factory, access not shared" claim) stays open — it is an owner question, not part of the errand. Queue row removed, task file deleted. Composite gate run.

- **2026-07-28** — Hardcore backlog refinement over the task queue (owner-requested), run as a subagent fleet: a structure-only survey of the sibling base's nine open tasks (process archetypes only, no content carried over) → parent-level import mapping → ten per-task effectiveness reviewers → one cross-task coherence and ordering reviewer. **Imports:** of nine sibling archetypes, seven were honestly skipped (need absent here, already owned by the inbox/loop/board conventions, or premature) and one adapted in: run-the-first-post-review-consistency-sweep (one finite consolidation pass once the first draft-review cycle completes; its recurring halves were graduated instead — loop step 2 now follows `wiki backlinks` on corrections like step 3, and Grooming gained the tag-vocabulary reconciliation duty). One further task was minted from the board's own gap analysis: derive-the-executor-acl-auth-model, the executor/account/ACL/auth gap's stated guided-pass closure route (protocol-doc submodule + `osx`), unblocking six pages across three areas; the permission-surface and chain-state gaps' executable routes became named candidate extensions on ground-the-base-in-the-source-repos for the owner's go-ahead to rule on, and the three gaps now cross-reference their queued routes by plain-text slug. **Per-task effectiveness edits landed on all ten files**, the substantive ones source-verified by the reviewers in the sibling checkouts: voting-terminal now names gov-ui-kit's real component (`ProposalVoting`, its Storybook state variants, the four app-side assemblies including the undocumented gauge-voter one, and the `isTimelockStage` third-mode question); dialog-taxonomy now records that the dialog surface is exactly two exported families and the dialog-wizard container is `Dialog.Footer variant="wizard"`, and its alert-severity claim was descoped honestly (non-dialog severities live in the separate `alerts/` family); compare-stage-against-spp gained three verified candidate leads (`cancelable`/`editable` unmapped; approval/veto bodies mix per stage; veto-stage `voteDuration` overrides `minAdvance`) and its repo-row bullet became a copy-across from protocol-doc's own repositories page; catalogue-admin-plugin-differences fixed a carried-over misquote ("in many ways"), gained the admin-is-neither-known-nor-unknown framing bullet, the dangling `EXECUTE_PROPOSAL_PERMISSION_ID` lead, and two missing unblocks (proposal-creation, proposal-status); capture-wizard-screen-walkthroughs' inverted trigger now names the owner's session as the unblocking event and anticipates per-wizard partial fires; scope-boundary sentences settled every real overlap the coherence matrix found (broad pass ↔ executor model on permission-model; walkthroughs ↔ action-builder/dialog-taxonomy/plugin-slots); repo pointers normalized to bare sibling names (`../app` from `tasks/` resolves wrong); gendered pronouns normalized to "the owner". **Queue reordered** per the adapted rules (foundations and blockers first, cheap unblockers early, human-blocked surfaced not idled): the blocked broad pass stays surfaced at 1, the clone errand jumped to 2, the executor model leads the ready foundations at 3, the owner-dependent captures rank by ask-actionability at 8–9, the sweep closes at 10; the coherence reviewer confirmed zero missing tasks (the app-answerable page checkboxes ride the broad pass's catch-all rather than a new task). Board preamble now says a blocked top row stalls nothing. Post-refinement, the owner queued an eleventh task at the top of the board; its prose row was converted to a proper entry per the queue contract (capture-the-product-updates-braindump — the owner walking through all product updates so nothing the base should cover is missed). A structure/types normalization task was evaluated and deliberately not minted: its one-time calls live in the sweep task, its recurring half in Grooming, and the rest was already done by the submodule migration — re-evaluate only if the first review cycle's sweep surfaces structural drift. Composite gate run.

- **2026-07-28** — Mounted `aragon/protocol-doc` as the read-only upstream `protocol-doc/` git submodule, pinned to `2d800ee849be6f02c8f9676c9470090127586751` (latest `main`, 2026-07-23). Platform-doc now explicitly extends that snapshot: protocol entry references were changed from branch-floating GitHub URLs to local relative links, making their files and anchors part of the parent-root graph; `wiki.toml` now enforces the union vocabulary and mirrors the nested bundle's operating-file/orphan exclusions; [WORKFLOW.md](./WORKFLOW.md) codifies upstream-first redundancy checks, platform → protocol-only dependency direction, read-only submodule handling, and the composite `wiki --root . check` gate. The upstream-only check is clean; the combined check passes with its documented nested-root `okf_version` advisory and the base's pre-existing unresolved `roles.md` promises.

- **2026-07-28** — Backlog process rework: tasks are now first-class entries (owner-requested; modeled on a sibling base's per-task-file system — process architecture only, no content carried over). New `tasks/` folder holds one `type: task` entry per task (frontmatter `status` ready | in-progress | blocked, plus `source`; body: lead → What this unblocks → Trigger → Work → Where to look → `**Done when:**` → Progress, with only lead and Done-when required), and the board's Tasks section became a **queue** of plain links that owns only the ordering. Contract codified in [WORKFLOW.md](./WORKFLOW.md): **only the board links into `tasks/`** — pages, indexes, and this log cite a task by plain-text slug, which keeps `wiki orphans` (board-omission detector) and `wiki unresolved` (to-write list) honest; **completion is one sequence, log first** (close-out here naming the slug → queue row removed → file deleted → `wiki check`), the close-out being the commit point for interrupted-completion recovery; loop step 4 now also fires blocked tasks' triggers when material lands; loop step 5 reconciles file↔row presence, label↔status, link-only rows, and close-out↔disk; task files never carry checkboxes or `## Open questions` (`wiki checkboxes` stays the owner-questions aggregate). The eight board tasks migrated to entries enriched strictly from the base (ground-the-base-in-the-source-repos — slug deliberately codebase-neutral, the owner never specified which repo; compare-stage-against-spp — now also records that the SPP source repo is missing from repositories.md; ground-the-voting-terminal-spec; ground-dialog-taxonomy-in-gov-ui-kit; capture-the-action-builder-feature-set; capture-wizard-screen-walkthroughs; catalogue-admin-plugin-differences — seeded with the four admin-difference instances the base already documents; clone-the-conditions-repo). The board's stray 2026-07-13 semantic-clarifications section dissolved into confirm-or-correct checkboxes on the owning pages ([proposal](./governance/proposal.md), [body](./governance/body.md), [repositories](./repositories.md)). Run adversarially: a structure-only survey of the sibling system → two independent design critics (conventions purist with live `wiki` experiments on a disposable copy; operational red-team simulating nine flows) → implementation → two adversarial implementation reviewers (migration fidelity: lossless, fabrication-free; contract consistency: no blockers). Their fixes, applied after the protocol-doc submodule landed mid-pass: the owner-questions aggregate reads `wiki checkboxes` **minus `protocol-doc/`** (17 upstream deploy-checklist steps are not questions — WORKFLOW and board intro qualified); the guides-layer-purpose highlight gained its page home as a checkbox on [guides/index.md](./guides/index.md) (board-only state is for gaps alone); the completion gate spelled as the composite `wiki --root . check`; a single-bullet section folded inline in catalogue-admin-plugin-differences; degraded mode's stray "commit it" aligned with the no-unrequested-commits convention; structure enumerations now include [value-proposition.md](./value-proposition.md); `risk` rephrased as declared-not-in-use; AGENTS.md's stale `tasks`-command sentence and checkbox-example trap corrected. Composite gate run.

- **2026-07-28** — Mined the owner's third answers briefing from `inbox/` (ingest → adversarial ingest review → 7 writers → 4 adversarial reviewers incl. a base-wide question-vs-task sweep → integration). **Convention correction codified:** an `## Open questions` checkbox is only ever a genuine question; executable agent work is a **task** on the backlog's new Tasks section — [WORKFLOW.md](./WORKFLOW.md) updated, five owner-named task-shaped checkboxes moved off pages (stage SPP comparison, Voting Terminal spec, plugin-slot analysis, action-builder feature set, dialog-taxonomy grounding) plus four more found by the sweep (repositories ×2, admin-flow catalogue, permission-model condition inventory — the last folded instead: the condition library's three audited conditions are now named). Three new drafts: [design/dao-slots.md](./design/dao-slots.md) (the second slot kind: DAO-scoped components for branding and one-place functionality; explore page + creation flow as the unscoped exceptions), [treasury/create-transaction.md](./treasury/create-transaction.md) (execute directly on the DAO bypassing its processes; permission-gated "+ Transaction" button, hidden not guarded; full-screen wizard straight into the action builder; distinguished from process-level execute grants), [design/alert-severity.md](./design/alert-severity.md) (warning vs critical, beyond dialogs). Answers folded: governance-designer (dev-environment access + reach out; **any known plugin uninstallable via the process page's two-step uninstall**; plugin-slots question reclassified), plugin (recognition = per-plugin front-end modules populating the plugin slots), proposal-actions (basic views deliberately uncatalogued; SCC retired; upload button placement confirming the existing order; the **authorized-actions toggle** over the execute selector condition), proposal-status (FAILED removed — nothing produces it; stage-tree paragraph confirmed), staged-proposals (advancing open to anyone, protocol default; contracts' auto-advance-on-report not surfaced; final-stage advance *is* execution, corrected against protocol-doc), full-screen-wizard (the four wizards: Create DAO, Create governance process, Create proposal, Create transaction; all other multi-step flows are dialog wizards), dialog-taxonomy (three kinds settled; form inputs ⇒ never an alert dialog; Radix-based alert component), account-creation (Create DAO confirmed full-screen — stale question closed). Backlog: Tasks 7–8 added (admin-plugin catalogue; clone `aragon/conditions`), Task 1 extended; three new drafts listed. Source-provenance collapsed base-wide to `briefings (2026-07-28, see log.md)`; the convention recorded in WORKFLOW. Inbox emptied; `wiki check` run.

- **2026-07-28** — Mined the owner's second answers briefing plus the Universal Proposal Status flowchart (ingested to `raw/`, adversarially reviewed against the app repo's `proposalStatusUtils` before mining — the pass's new ingest-review gate). New draft [governance/proposal-status.md](./governance/proposal-status.md): the two-layer model (per-process-type input derivation feeding one shared, ordered decision tree), the 12-status vocabulary (9 tree-produced; DRAFT pre-indexing fallback, UNREACHED per-stage, FAILED producer unknown), the admin-proposals-always-EXECUTED exception, per-type consequences (VETOED staged-only; multisig executable only within its window; token voting and lock to vote never EXPIRED; signaling proposals end at ACCEPTED; a failed stage makes later stages unreachable and settles the proposal REJECTED), and the separate stage-level tree. Answers folded (checkboxes deleted): proposal-actions + action (the per-action basic/decoded/raw viewer, always present and disabled when unsupported; ABIs from the block explorer, unverified → raw only; native = app-aware contract, not account-native; reading viewer vs form composer disambiguated), governance-designer (editing model corrected: creating a process is itself a proposal; basic processes and bodies — including inside staged processes — editable by proposal; advanced processes not editable in the app, hands-on via the Aragon team; installable set exactly multisig/token voting/lock to vote — naming fixed from "locked-to-vote"; advanced flow gated to the development environment; execute-permission "unrestricted execute" + process details page), plugin (Aragon Governance Standards / Open Design System claims retired — owner does not recognize them, links removed, clearing two unresolved promises; known set = installable set, recognition mechanical via ABIs and events; unknown plugins listed in settings, unknown behavior expected), multisig-gates + guide (customer ask confirmed as create-a-proposal; governance-is-decision-making framing folded; guide notes the advanced flow is not yet walkable in production), proposal-identifiers (slug indexes only collide via indexer bugs). value-proposition's evolving-governance promise qualified; proposal.md's status question resolved into a link. Backlog: status-vocabulary highlight resolved; **app-repo grounding pass** recorded (owner-suggested); unresolved promises down to roles.md. Run as ingest → adversarial ingest review → 5 writers → 3 adversarial reviewers → integration. Inbox and raw note emptied; `wiki check` run.

- **2026-07-28** — Mined the owner's open-question answers briefing (follow-up to the multisig-gates pass) from `inbox/`. New draft [governance/proposal-identifiers.md](./governance/proposal-identifiers.md) (on-chain proposal ID vs the app's slug; process key chosen at design time, stored in the process's IPFS metadata; slug index derived off-chain, not authoritative; URLs accept both). Answers folded (checkboxes deleted): proposal-creation.md (rule condition open by default but configured per process via body on/off toggles whose **membership conditions** compose in boolean logic; the **existing conditions** paste-an-address field, e.g. Token Voting's `VotingPowerCondition`; no bespoke-condition authoring and no bare-address creation grant in the designer — "create permissions" confirmed as a slip for "create proposals"; `ConditionFactory` routing confirmed, closing the earlier open question; live-process eligibility editing is manual rule-condition surgery, shown on the process details page), safe-as-a-body.md (the connected-Safe **Approve** button composes the SPP-targeted report transaction; registration via the designer's add-body **any address** option into the stage config at SPP install; Safe absent from the members list; the on-chain ID is what signers compare), governance-designer.md (new "Adding a body" block: any-address bodies, Safe detection via the Safe API → background `SafeOwnerCondition` as the body's only creation-condition option; process key at creation), proposal-simulation.md (Tenderly; likely-to-succeed/fail verdict linking to Tenderly results; supported chains only), voting-terminal.md (Safe rendered as a single object — signatures collected off-chain in Safe infrastructure, unindexed). Two questions rephrased rather than answered, per owner request: multisig-gates.md (customer's create-a-proposal vs create-a-process intent, split in two) and proposal.md (proposal status vocabulary — now backlog highlight 1). Consistency fixes on body.md (members list does not show a Safe body) and permission-model.md (condition catalogue updated). Backlog: advanced-designer-boundary highlight resolved and removed; **app metadata pattern** gap opened (owner flagged for later discussion); chain-state gap's simulation clause updated. Run as a multi-agent pass (4 writers → 3 adversarial reviewers → integration). Inbox emptied; `wiki check` run.

- **2026-07-28** — Separated product-improvement intake from documentation review. New [Product opportunities](./product-opportunities.md) board and area-filed `opportunity` type keep candidate improvements out of canonical capability and pattern questions while preserving a path to product-backlog tickets. Added candidates for [rebasing-token balance freshness](./treasury/rebasing-token-balance-freshness.md) and the settled [account-terminology rollout](./design/account-terminology-rollout.md). [Assets](./treasury/assets.md) now states only that CoinGecko prices refresh frequently; its cadence question was answered and the rebasing-token idea was promoted. The account-terminology implementation item was likewise promoted from an open question. Renamed the board heading to **Documentation backlog** without moving its path.

- **2026-07-28** — Mined the product-owner answer to the Transactions open question. [Transactions](./treasury/transactions.md) now documents its four tabs (all, executions, deposits, withdrawals), the execution-detail dialog and shared action views, deposit/withdrawal block-explorer behavior, and JSON export; the answered checkbox was removed. New draft [design/datalist-page.md](./design/datalist-page.md) captures the shared list-left/contextual-aside-right page structure and the rule that each datalist owns its relevant tabs; linked from Assets and Proposals. The earlier linked-account wording on Transactions was narrowed so it no longer incorrectly claims the list has no other partitioning dimension.

- **2026-07-28** — Mined the product-owner briefing on multisig-gated advancement into Token Voting from `inbox/` (both parts: the customer-request reframe and the broader multisigs-throughout-advanced-governance material). Nine new drafts: [governance/safe-vs-aragon-multisig.md](./guides/safe-vs-aragon-multisig.md) (Safe = account, governor-executor monolith; Aragon multisig = plugin, the account executes), [governance/multisig-gates.md](./governance/multisig-gates.md) (a multisig governing a stage before/after Token Voting — creation-side and security-council patterns, worked recommendations per multisig kind), [governance/safe-as-a-body.md](./governance/safe-as-a-body.md) (Safe as a stage's governing body; owner-gated creation; approval flow; manual-body protocol link), [governance/proposal-creation.md](./governance/proposal-creation.md) (per-process eligibility; `SPPRuleCondition` rules composing body conditions; background `SafeOwnerCondition`), [governance/stages-over-direct-permissions.md](./governance/stages-over-direct-permissions.md) (decision: explicit stages over direct grants; the admin-route creation grant, nested-calldata review problem, execute-only-to-a-Safe variant, never-for-an-Aragon-multisig rule), [governance/proposal-simulation.md](./governance/action-simulation.md) (simulate the plugin calling `execute`, bypassing governance deliberately), [design/voting-terminal.md](./design/voting-terminal.md) (the staged-process proposal-page surface, named), [accounts/connecting-a-safe.md](./accounts/connecting-a-safe.md) (operating the app as the Safe via Safe Apps + WalletConnect), and the guide [guides/multisigs-in-advanced-governance.md](./guides/multisigs-in-advanced-governance.md). Enriched: staged-proposals (Voting Terminal, multisig stages, execution-vs-advancement), stage & body (a body need not be a plugin), process (creation-eligibility pointer), proposal (Simulate surface; Execute restrictability + ineligibility guard), proposal-actions (basic/decoded/raw presentation levels), governance-designer (creation step grounded; Safe-as-body designer support flagged unresolved), permission-model (SPPRuleCondition + SafeOwnerCondition instances; who-can-create question answered and folded), account (kernel-like execution-intermediary framing), plus the governance/accounts/design/guides/root indexes. Backlog: **optimistic governance** gap opened (owner asked to be asked), **permission viewing/editing surface** gap opened, kernel framing folded into the executor/ACL gap, advanced-designer-boundary and guides-layer-purpose highlights added, sections ordered. Terminology settled against protocol-doc: "SPP rule condition" = `SPPRuleCondition`, "Condition Factory" = `ConditionFactory` (routing unconfirmed → open question), SafeOwnerCondition mechanism linked not restated. Run as a multi-agent pass (writers → adversarial reviewers → integration). Inbox emptied; `wiki check` run.

- **2026-07-21** — Mined the product-owner linked-accounts briefing from `inbox/`. Five new drafts: [accounts/linked-account.md](./accounts/linked-account.md) (application-layer abstraction — one primary + flat linked accounts; dashboard aggregation, and how linked data merges into existing partitions or gets its own per-account tabs), [accounts/linked-account-signaling.md](./accounts/linked-account-signaling.md) (two mutual-acknowledgement permission grants indexed as a signal, not used for authorization; no product UI to establish), [accounts/linking-does-not-imply-control.md](./accounts/linking-does-not-imply-control.md) (decision: link is display-only; real control is a separate Execute/Root OSx config), [accounts/executing-on-a-linked-account.md](./accounts/executing-on-a-linked-account.md) (the WalletConnect nested-execution flow + inner-call decoding), and [governance/target.md](./governance/target.md) (a process's plugin target: own account = process, SPP = body-only). Enriched: proposal.md / body.md (linked-account proposals & members merge flat, with a linked-account indicator), proposal-actions.md (control-surface framing, nested-call unnesting, WalletConnect-reversed link), process.md & body.md (target links), assets.md / transactions.md / treasury+accounts+governance indexes, permission-model.md (permissions as signals and as cross-account control), account.md, value-proposition.md, root index. Backlog: **linked accounts** gap closed; two new gaps opened (the executor/account/ACL/`auth` semantic model; `app-cms`); permission-IDs highlight added; `app-cms` recorded in repositories.md. Inbox emptied; `wiki check` run.

- **2026-07-20** — Mined the product-owner wizard-system briefing from `inbox/`. New draft [design/wizard.md](./design/wizard.md) defines when transaction input needs a wizard, the single-input non-wizard boundary, dialog-vs-full-screen containers, and nested dialog wizards. Enriched: full-screen-wizard.md (destination, focus, working-memory, and nesting criteria), dialog-taxonomy.md (dialog-wizard definition; remaining general/alert taxonomy questions narrowed), and proposal.md (Create is a destination wizard; Vote stays inline). The dialog-taxonomy backlog highlight was resolved; its remaining questions stay on the page. Inbox work item removed.

- **2026-07-16** — Mined the parked governance-flows briefing from `inbox/` (verbal, captured under degraded mode; owner corrections already folded in). New draft [governance/stage.md](./governance/stage.md) (per-stage semantic layer: duration, expiration, early advance, approval/veto mode, body thresholds — "multisig of bodies"). Enriched: governance-designer.md rewritten around the basic/advanced split (entry points settings-page or new-DAO dashboard; basic = single-plugin bodies targeting the DAO, no orchestration; advanced = UI for the staged proposal processor, gated/internal, stages≥1 / bodies≥0 / processes≥0, bodies reuse the basic config but target the SPP); proposal.md ("proposal type" vocabulary = per-process tabs); body.md (members list partitioned by body, proposals by process); process.md (zero-or-more processes per DAO); plugin.md (plugin-scoped utilities/components in app code). Backlog: designer basic-flow and advanced-designer highlights resolved; advanced-flow gap closed — its residue is an owner-requested checkbox on stage.md to compare the semantic layer against the SPP repo. Inbox emptied; `wiki check` run (pass, plus retroactively for the 2026-07-14 treasury batch that couldn't run it).

- **2026-07-16** — Verbal product-owner briefing on treasury/assets mined into a new `treasury/` area: [vault](./treasury/vault.md) (the account itself is the vault; its arbitrary-executor nature is why), [assets](./treasury/assets.md) (indexer watches ERC-20 `Transfer` events to/from the DAO then calls `balanceOf`; CoinGecko prices on an interval; tokenized-DeFi-only, no NFTs; holdings surface on the dashboard total and in proposal creation), [transactions](./treasury/transactions.md) (deposits/withdrawals plus executions listed by transaction hash; proposal-created executions also show on the proposal). Backlog: treasury gap closed; chain-state gap narrowed (assets indexing is now a documented instance); new **linked accounts** gap added (owner to brief later). Note: the `wiki` CLI could not be installed in this session (network policy blocked brew/npm/crates) — links were hand-checked; run `wiki check` next session.

- **2026-07-14** — Mined the modularity strategy document (owner-pasted; its "Context" section treated as historical motivation per the present-tense rule, not restated as current behavior). New draft [governance/action.md](./governance/action.md) (what-vs-how framing, native/external, prepared/shown contract). Enriched: [principles.md](./principles.md) grew principles 5–10 from the doc's WIP list (modularity-first, atomic modules, interoperability, scalability, permissioned-to-permissionless, educational) — closing the "fuller set of principles" knowledge gap, pending owner confirmation (backlog highlight added); proposal-actions.md (form views basic/advanced/byte, JSON import, source-conflict question on verified-vs-any contracts); plugin.md (known-plugin bar: Aragon Governance Standards + Open Design System, both left as unresolved promises). Raw worklist emptied.

- **2026-07-14** — Mined the original product vision document (owner-pasted; its competitive-positioning aside dropped at owner request, never committed). New drafts: root [value-proposition.md](./value-proposition.md); [governance/staged-proposals.md](./governance/staged-proposals.md) and [governance/proposal-actions.md](./governance/action-builder.md); [accounts/explore-page.md](./accounts/explore-page.md) (resolves the long-promised link). Enriched: proposal.md (create/vote/execute surfaces), governance-designer.md (plugin placement standalone-or-body, live-account editing, wizards-reused-as-proposals), account-creation.md (creation is a wizard, decoupled from governance design). Advanced-flow gap narrowed to the designer side; traversal drafted pending validation.

- **2026-07-14** — Present-tense pass, per owner feedback: pages describe current behavior and principles only; history stays in git and this log. `accounts/last-process-removal.md` stripped of its hard-block/brick backstory (now: the rule, why, consequences); backlog resolved-breadcrumbs removed. WORKFLOW: `decision` redefined as "current rule + rationale" and the present-tense writing rule added to Types.

- **2026-07-14** — Product-owner briefing answered the admin-removal highlight. "Brick" framing retired: the risk page reworked into the decision [accounts/last-process-removal.md](./accounts/last-process-removal.md) (warn, never block; validated, draft status removed). New draft concept [governance/plugin.md](./governance/plugin.md) (plugins as the substrate of process/body; known vs unknown plugins). Principle 4 added to [principles.md](./principles.md) (accounts are autonomous; the app never restricts them). Referring pages rephrased (admin-flow, admin-management, admin-plugin-by-default, accounts index); backlog highlight and the process-based-brick clarification marked resolved.

- **2026-07-13** — Consistency review pass (5 dimensions: terminology, cross-linking, protocol grounding, conventions, source fidelity; 34 verified findings applied). Notable: brick condition restated as process-based; proposal-tab fact corrected to per-process; body definition restored ("cannot create proposals or..."); "condition-factory" corrected to the public `aragon/conditions` condition library; account/account-terminology dedup; "admin state" unified to "admin flow"; WORKFLOW Structure tree rewritten to the real layout; orientation guide re-marked draft. Backlog highlights refreshed; semantic clarifications listed there for owner confirmation.

- **2026-07-13** — First mining pass over `../aragon-knowledge-base` (its 2026-07-06 first-slice material: product concepts, capabilities, decision, risk, design patterns, source repos, brain-dump Q&A). Added areas `accounts/` (account, account-creation, admin-flow, admin-management, admin-plugin-by-default decision, brick risk) and `design/` (full-screen-wizard, dialog-taxonomy, plugin-slots, account-terminology); added governance/governance-designer + proposal; enriched process, body, permission-model, principles; added root repositories.md. KB protocol/mechanism pages deliberately not brought over (protocol-doc owns them); refs remapped to protocol-doc GitHub URLs. Everything `status: draft`. Not yet mined: KB ontology (base-internal — except operating-principle #1, carried into principles.md as principle 3), backlog verification tasks (carried as in-page open questions).

- **2026-07-13** — Base scaffolded from the `product-docs` template. Shape locked in WORKFLOW.md: single product (Aragon platform), areas `governance/` + `access-control/`, root `principles.md`, protocol references via GitHub URLs. Seed pages: process, body, permission-model, orientation guide — all `status: draft`, pending product-owner validation. Source of the seed content: initial briefing + protocol-doc; `aragon-knowledge-base` deliberately not mined yet.
