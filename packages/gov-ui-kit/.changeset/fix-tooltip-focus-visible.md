---
"@aragon/gov-ui-kit": patch
---

Stop tooltips from opening on focus that no key press preceded, so an `AddressOutput` or `Clipboard` inside a dialog no longer reveals its tooltip as soon as the dialog autofocuses it.
