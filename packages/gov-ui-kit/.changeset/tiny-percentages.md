---
'@aragon/gov-ui-kit': minor
---

Extend `PERCENTAGE_LONG` and `PERCENTAGE_SHORT` number formats to display percentages smaller than the configured fraction-digit precision using significant digits, instead of rounding them to `0.00%` / `0%`.
