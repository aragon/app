import { Meta } from '@storybook/blocks';
import LinkTo from '@storybook/addon-links/react';

<Meta title="Core/Documentation" />

# Core

The `Core` package of the Aragon ODS library includes all the basic components and utilties needed to build a web2
application.

Check the documentation and usage of the core components and utilities under the `/components` and `/utilities`
sub-folders.
