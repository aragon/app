name: "Setup"
description: "This action checks out the commit, sets up Node and installs the dependencies using pnpm."

inputs:
  fetch-depth:
    description: "The number of commits to fetch when checking out the repository"
    required: false
    default: "1"
  token:
    description: "GitHub token used for authentication"
    required: false
    default: ${{ github.token }}
  ref:
    description: "The branch, tag or SHA to checkout."
    required: false
  registry-url:
    description: >-
      URL of the registry used for Node setup. Left empty by default so that
      actions/setup-node does NOT write a user-level .npmrc with a placeholder
      NODE_AUTH_TOKEN, which would shadow OIDC trusted publishing. Publishing
      authenticates via OIDC (id-token), not via a registry auth token.
    required: false
    default: ""
  node-version:
    description: "The version of Node.js to use for the workflow"
    required: false
    default: "24"
  node-cache:
    description: "Specifies which package manager to use for caching"
    required: false
    default: "pnpm"

runs:
  using: "composite"
  steps:
    - name: Checkout repository
      uses: actions/checkout@v6.0.1
      with:
        fetch-depth: ${{ inputs.fetch-depth }}
        token: ${{ inputs.token }}
        ref: ${{ inputs.ref }}
    - name: Install pnpm
      uses: pnpm/action-setup@41ff72655975bd51cab0327fa583b6e92b6d3061 # v4.2.0
      with:
        run_install: false

    - name: Enable corepack
      shell: bash
      run: corepack enable

    - name: Setup Node
      uses: actions/setup-node@v4.2.0
      with:
        node-version: ${{ inputs.node-version }}
        cache: ${{ inputs.node-cache }}
        registry-url: ${{ inputs.registry-url }}

    - name: Install dependencies
      shell: bash
      run: pnpm install --frozen-lockfile
