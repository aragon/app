/**
 * Minimum valid epoch ID for the capitalDistributorTest DAO.
 * Epochs below this value are invalid because the system was not deployed yet.
 */
export const capitalDistributorTestMinEpochId = 1460;
