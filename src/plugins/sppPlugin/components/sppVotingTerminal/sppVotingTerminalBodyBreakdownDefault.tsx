export interface ISppVotingTerminalBodyBreakdownDefaultProps {}

export const SppVotingTerminalBodyBreakdownDefault: React.FC<ISppVotingTerminalBodyBreakdownDefaultProps> = () => {
    return (
        <div className="flex h-full w-full flex-col items-center justify-center">
            <p className="text-neutral-500">This plugin does not support the voting breakdown view.</p>
        </div>
    );
};
