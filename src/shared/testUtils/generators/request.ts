export const generateRequest = (request?: Partial<Request>): Request => ({
    cache: 'default',
    credentials: 'omit',
    destination: '',
    headers: {} as Headers,
    integrity: '',
    method: '',
    mode: 'cors',
    redirect: 'error',
    referrer: '',
    referrerPolicy: '',
    url: '',
    keepalive: false,
    signal: {} as AbortSignal,
    clone: jest.fn(),
    body: null,
    bodyUsed: false,
    arrayBuffer: jest.fn(),
    blob: jest.fn(),
    formData: jest.fn(),
    json: jest.fn(),
    text: jest.fn(),
    bytes: jest.fn(),
    ...request,
});
