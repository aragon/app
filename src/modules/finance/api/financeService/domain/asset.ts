export interface IAsset {
    /**
     * Address of the asset.
     */
    address: string;
    /**
     * Network of the asset.
     */
    network: string;
    /**
     * Symbol of the asset.
     */
    symbol: string;
    /**
     * Name of the asset.
     */
    name: string;
    /**
     * Logo url.
     */
    logo: string;
}
