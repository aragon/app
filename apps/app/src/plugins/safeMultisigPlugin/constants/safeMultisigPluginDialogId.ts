/**
 * Dialog ids of the Safe body plugin. Every plugin's definitions are merged into one record, so the
 * values are namespaced rather than bare action names.
 */
export enum SafeMultisigPluginDialogId {
    CONFIRM_SIGNATURE = 'SAFE_MULTISIG_CONFIRM_SIGNATURE',
}
