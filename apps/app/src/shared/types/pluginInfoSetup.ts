export interface IPluginInfoSetup {
    /**
     * Translation key of the plugin name to be displayed during the plugin setup.
     */
    nameKey: string;
    /**
     * Translation key of the plugin description to be displayed during the plugin setup.
     */
    descriptionKey: string;
}
