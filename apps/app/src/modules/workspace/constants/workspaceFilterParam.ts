/**
 * URL parameter holding the selected account on the workspace pages.
 */
export const workspaceAccountFilterParam = 'account';

/**
 * URL parameter holding the selected body on the workspace members page.
 */
export const workspaceBodyFilterParam = 'body';
