import type { IProposal } from '@/modules/governance/api/governanceService';

/**
 * Indexed proposal as returned by the workspace proposals endpoint.
 */
export interface IWorkspaceProposal extends IProposal {}
