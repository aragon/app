/** Single source of truth for the Synpress wallet cache directory name.
 * Consumed by both wallet.setup.ts (via walletCacheKey.d.mts) and syncWalletSetupCache.mjs.
 */
export const WALLET_SETUP_CACHE_KEY = 'wallet-setup';
