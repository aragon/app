import type { IDocsIndexArtifact } from '../docsIndexArtifact';

// Types the generated, git-ignored `docsIndex.js` next to this file (written by
// `pnpm build:docs-index`), so type-checking never needs the built index. Jest maps the import to
// a fixture instead (see jest.config.cjs).
export declare const docsIndexArtifact: IDocsIndexArtifact;
