---
'@aragon/app': minor
---

Add the MPC systems proof of concept (`/mpc`, feature flag `mpcSystems`): mock co-signer API, Shamir 2-of-3 mock provider, sign requests with policies and approvals, a default workspace per account with a visual transaction-policy editor (formal check + simulation through the `mpc-poc` policy engine) and enforcement of the active workspace policies on transaction requests.
