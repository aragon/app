---
'@aragon/app': minor
---

Add redirect functionality for DAO members page when no body plugin exists. This prevents crashes by redirecting users to the dashboard and hiding the members navigation link when body plugins are unavailable.