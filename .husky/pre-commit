#!/bin/sh

# Stage 1: Fast lint/format of staged files only
echo "🔍 Running lint-staged (staged files only)..."
pnpm lint-staged

# Stage 2: Full parallel checks with caching
echo "⚡ Running full checks with Turbo (parallel + cached)..."
pnpm turbo lint type-check test
